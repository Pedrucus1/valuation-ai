# PropValu — Backlog de Tareas
> **Última actualización:** 01 Sep 2026 — **Bug "Continuar Reporte" (503 mudo) CERRADO:** el motor
> ahora lee la página completa (JSON-LD) de los resultados de búsqueda en vez de solo el snippet
> corto, que nunca traía precio (#168). Bug de ads (URL relativa + http en vez de https) CERRADO
> (#169). Log de actividad/errores para el admin: backend hecho, falta frontend (#170). Sin resolver:
> comparables inventados en zonas sin cobertura (#171) y `enrich-stream` que nunca existió en el
> backend (#172). Detalle completo → `ESTADO.md` (sección "LO MÁS CALIENTE").
> **Última actualización [previa]:** 19 Ago 2026 (noche) — **Plan de fuente única del Identificador de Edad CERRADO completo** (Fases 0-3, sesión maratónica en el repo del Manual ZMG): 461 opciones en el catálogo (315→461), elemento nuevo `instalacion_gas`, bug de datos corregido (cantera de fachada mal puesta en muro), y **panel de detalle nuevo** en `IdentificadorEdadDialog.jsx` (muestra valor como fechador / cómo se identifica / confusiones frecuentes al marcar una opción, usando `CAMPOS_RICOS` del catálogo). `acabados_selectores.json` resincronizado. Detalle completo → memoria `project_manual_arquitectura.md` (el trabajo grueso vive ahí, no en PropValu). Deuda técnica identificada: el catálogo se sincroniza a mano entre los 2 repos — pendiente evaluar servirlo desde el backend de PropValu (Mongo) para que ambos lo consuman en vivo, sobre todo pensando en expansión multi-ciudad.
> **Última actualización [previa]:** 19 Ago 2026 — **#17 CERRADO:** Identificador de Edad del Manual ZMG probado end-to-end en navegador real (ValuationForm + EdadesZonaPage), puntaje pasado a peso continuo 1/n (menos empates falsos) + toggle de orden recorrido/discriminante. Plan de fuente única: **Fase 0 y Fase 1 cerradas** (`acabados_master.json` + `AdminAcabados.jsx` en `/admin/acabados`, Mongo `acabados_propuestas`, script `aplicar_hallazgos_acabados.py`). Fase 2 (Atlas/Programa) bloqueada hasta que el perito mapee las taxonomías a mano.
> **Última actualización [previa]:** 9 Ago 2026 — Scraper mensual corriendo desatendido desde el 07-ago (agente en background): CASAS_Y_TERRENOS/PROPIEDADES_COM/VIVANUNCIOS/INMUEBLES24 avanzando (miles de props nuevas acumuladas), **MITULA terminó su cola (#158, bloqueo 401 confirmado)**. IA de reportes: se evaluó y descartó self-hosting de modelos open-weight (Gemma/Llama) — no es viable a este volumen, se mantiene Gemini+DeepSeek vía API.
> **Última actualización [previa]:** 4 Ago 2026 (madrugada) — **#149 CERRADO:** `quality_costs` (server.py) unificado a las 7 llaves reales de `construction_quality` (antes siempre caía al default $16,000, la calidad elegida no afectaba nada). Desplegado (`9dbc8c4` / Railway `e74d127b`). Verificador de zona: rango de remodelación +31-40/40+ años, campo nuevo "calidad de construcción" por comp (guarda en `mercado_props.calidad_construccion`, motor JS aún no lo consume — ver #150).
> **Última actualización [previa]:** 3 Ago 2026 (cierre) — **Colonias fechadas por el perito: la cola por impacto quedó VACÍA.** `colonias_decada.json` 4,749 → 3,202; `fuente: manual` de 249 a 1,259; municipios 54% → 79%; homónimas y colisiones sin resolver en 0. Método reusable: cruzar contra los comps y preguntar solo por las colonias con anuncios encima (85% de los comps ya trae década verificada). Campo nuevo `por_etapas` (19) con ventana real. Medido y descartado: DeepSeek acierta 41% en municipios y Maps 48-62% — no usarlos para geografía, sí para clasificar texto. Parqueados con handoff: décadas sugeridas en el formulario (#16 de ESTADO) y selector de acabados (#17). Detalle → `BACKLOG_ARCHIVE.md`.
> **Última actualización [previa]:** 3 Ago 2026 (noche) — **Bug depreciación física backend (#148):** Escorpión 3518 daba el mismo `estimated_value` con y sin remodelación (tope de depreciación saturado). Fix desplegado a prod + fórmula real del perito (Ross-Heidecke, vida útil 70) portada del motor JS como `result_lab_rh` en observación. Bug relacionado sin arreglar: `construction_quality` no hace match backend/frontend.
> **Última actualización [previa]:** 3 Ago 2026 — **Sesión (2/3 ago):** homónimas CERRADAS (107 pares, 0 pendientes) y manual de arquitectura de 168 a 82 marcas. Criterio nuevo: la década es la de la edificación, no la del trazo. Corrección: `colomos providencia` es 1960s, no 1910s.
> **Última actualización [previa 2]:** 09 Jul 2026 (tarde) — **Herramienta "Edades por zona" HECHA y verificada** (#134): captura edad/remodelación→edad efectiva/conservación/coto, cascada Estado→Muni→Colonia, corregir colonia con SEPOMEX+CP, propagación al coto (variantes ABIE), acceso valuador/inmobiliaria/admin. **Colonias PINCALI depuradas a español con DeepSeek** (6,604 + 179 recuperadas + limpieza dirigida 42/112) — REGLA REFORZADA: nunca regex sobre colonias (corrompí 219, revertí con DeepSeek). Dedup PINCALI 76 marcadas. Enricher de-riesgado (breadcrumb+SEPOMEX, #135). Detalle → `project_propvalu_edades_crowdsource`. — [previo:] cluster A edad-exacta → PROD **+1.9 ±20** [83.5%]; techo de fórmula ~82-83% = DATO; Serper→Tavily; prior de edad parqueado. **PENDIENTE: deploy Railway** [+ TAVILY_API_KEY]. Snapshot vigente → `ESTADO.md`. Bitácora → `BACKLOG_ARCHIVE.md` (NO leer al iniciar).
> Marcar ✅ completado · 🔄 en progreso · ⏳ pendiente · 👁️ pendiente revisión personal. Las descripciones largas de tareas viven en las tablas de abajo; el detalle del motor en `MOTOR_ANTECEDENTES.md` (grep, no leer completo).

---

## Flujo de Usuario / Cobro

| # | Estado | Tarea |
|---|---|---|
| 1 | ✅ | **Checkout público general** — flujo: formulario pasos 1-2-3 → paso 4 (pago integrado en ValuationForm) → ads → análisis. |
| 8 | ✅ | **Checkout con upsells** — add-ons Revisión por Perito +$350 y Verificación de m² +$600. |
| 19 | ✅ | **Checkout Valuador e Inmobiliaria** — `/checkout/pro` con planes por rol. Modal de pago simulado. |
| 7 | ⏳ | **Integración pasarela de pagos** — Stripe (tarjeta), OXXO, SPEI, Mercado Pago. |
| 67 | ✅ | **Accesos autorizados (cortesía / pruebas)** — módulo admin `/admin/accesos` para dar acceso gratis a emails internos o de prueba (valuador/inmobiliaria). Por categoría (interno/valuador/inmobiliaria), acceso total o N avalúos gratis, modalidad (solo valuación / con valuador=addon $350 / con visita=addon $600) y vigencia opcional. Backend: colección `authorized_access`, CRUD + `GET /access/status` + consumo de cupo al generar reporte. Frontend: `AdminAccesos.jsx` + nav (Finanzas y datos) + banner de cortesía en ValuationForm que salta el pago. Probado contra Atlas; build OK. Pendiente: replicar salto de pago en ProCheckoutPage (planes pro) si se requiere. |

---

## Publicidad (Ad-Engine)

| # | Estado | Tarea |
|---|---|---|
| 2 | ✅ | **Ads en investigación + generación** — Slot 1/2/3 con countdown, fallback a house ads PropValu. |
| 22 | ✅ | **AdRenderer.jsx** — countdown, link web/WhatsApp, fallback. |
| 23 | ✅ | **Carga de creatividades** — subir JPG/PNG/MP4, preview, estado pendiente de revisión. |
| 11 | ✅ | **Consola de anunciantes** — `/anunciantes`, `/anunciantes/registro`, `/anunciantes/consola` (3 tabs). Métricas Resumen y Facturación conectadas a endpoints reales. Saldo a favor calculado desde budget-spend. Mocks eliminados. |
| 145 | ✅ | **Fix video ads slot1 se veía negro (28-jul)** — causa real: metadata de color no estándar heredada de Remotion (`yuvj420p`+`color_range=pc`+colorspace SD sobre HD), combinación que el decodificador de hardware de Chrome/Windows renderiza negro sólido aunque el archivo sea válido. Fix: `_compress_video` en `ads.py` fuerza `bt709`+rango limitado; endpoint puntual `/admin/ads/reprocesar-video/{id}` para arreglar creatividades ya subidas; video de respaldo estático también recomprimido. Verificado con `ffprobe` antes/después contra el archivo real de producción. Commits `8a96c60`+`ea6f876`+`ce3e51c`. |
| 146 | ⏳ | **Slides de texto (house ads PropValu) muy rápidas** — el usuario reportó (07-ago) que el tiempo que se muestran las slides de texto en los slots de ads no alcanza para leer todo el contenido. Subir a 15 segundos. Pendiente ubicar el countdown/duración en `AdRenderer.jsx` (ver #22) y aplicar. |

---

## Páginas y Autenticación

| # | Estado | Tarea |
|---|---|---|
| 3 | ✅ | **Páginas legales** — `/privacidad`, `/terminos`, `/contacto`, etc. |
| 4 | ✅ | **Registro y login** — email+password, bcrypt, cookie sesión, redirect por rol. |
| 39 | ✅ | **Fix auth propio** — reemplazar `auth.emergentagent.com` por sistema login propio. |
| 25 | ✅ | **Registro valuador — paso 2** — ModoSelector, servicios, cobertura, experiencia con medalla. |
| 26 | ✅ | **Registro valuador — paso 3** — docs con hints, comprobante_experiencia obligatorio, términos. |
| 27 | ✅ | **Subida de docs al registrarse** — `/kyc/upload` tras crear cuenta. |
| 46 | ✅ | **Registro sin fricción** — docs opcionales, draft en localStorage. |
| 47 | ✅ | **CTA inmobiliarias en ReportPage** — card verde tras resultado OPI. |
| 28 | ✅ | **Programa afiliado en ModoSelector** — beneficios, compromisos, requisitos. |
| 9 | ✅ | **KYC valuadores** — expediente por etapas, botón entrevista, admin ratifica. |

---

## Dashboards

| # | Estado | Tarea |
|---|---|---|
| 5 | ✅ | **Dashboard Valuador** — tabs: Resumen, Valuaciones, Perfil, Expediente, Facturación. |
| 6 | ✅ | **Dashboard Inmobiliaria** — tabs: Resumen, Mercado, Valuaciones, Equipo, Documentos, Perfil, Reseñas, Facturación. |
| 48 | ✅ | **Perfil inmobiliaria rediseñado** — header verde, grid 4 col, todos campos visibles. |
| 49 | ✅ | **Reseñas de clientes** — tab Reseñas con promedio, distribución, respuesta del titular. |
| 50 | ✅ | **Equipo real de asesores** — GET /inmobiliaria/equipo, tabla, mock preview. |
| 51 | ✅ | **Editar perfil inline** — ✏️ abre form con redes, dirección, asociación, galardones. |
| 52 | ✅ | **Refresh sesión al montar** — fetch /auth/me al cargar, merge con localStorage. |
| 53 | ✅ | **Sistema de billing** — tab Facturación en ambos dashboards, tarjeta Próximo corte. |
| 54 | ✅ | **MercadoTab inmobiliaria — rediseño completo** — tabla con colonias por tipo, sticky, filtros, análisis dinámico, segmentos 7 niveles, PDF vía window.print(). |
| 55 | ✅ | **Plan card junto a tabs** — badge + precio + beneficios + créditos + botón en misma fila que nav. |
| 56 | ✅ | **StatCards 5 fichas** — grid-cols-5, Próximo corte siempre visible. |
| 57 | ✅ | **ValuadorDashboard: MercadoTab + ResumenActividad + plan card colores + tab order** — portado completo desde InmobiliariaDashboard. Starter plan verde, tabs ordenados igual. |
| 59 | ✅ | **AdminScraper: tab Mercado + tarjetas por portal + botón Ejecutar todos + filtros** — portales como cards individuales con estado propio, ExecutarPanel modal eliminado, backend 409 fix para portales individuales. |
| 20 | ✅ | **Panel Admin** — 17 módulos. |
| 40 | ✅ | **Admin Inmobiliarias** — 4 tabs, endpoints detalles. |
| 41 | ✅ | **Admin Ads mejorado** — campañas con lightbox, moderación inline. |

---

## Sistema de Credenciales (Medallitas)

| # | Estado | Tarea |
|---|---|---|
| 30 | ✅ | **BADGE_DEFS** — mapeo doc_tipo → credencial. |
| 31 | ✅ | **Ratificación por admin** — POST /admin/kyc/ratificar/{doc_id}. |
| 32 | ✅ | **Badges en dashboard** — sección Credenciales verificadas. |
| 33 | ✅ | **Medallitas en directorio público** — burbujas emoji con tooltip. |

---

## Features de Negocio

| # | Estado | Tarea |
|---|---|---|
| 10 | 🔄 | **Fichas Studio (Promociones)** — tab Promociones reescrito como diseñador split-panel: CRUD propiedades manuales (`propiedades_inmobiliaria`), upload fotos base64, 5 paletas, puntos verificados auto, propiedad demo, 6 estilos (Clásico/Minimalista/Noir/Moderno/Colección AI/Obsidian) × 4 formatos (A4/horizontal/reels/post), paletteUtils contraste, export PDF/JPG. **Pendiente:** integrar diseños nuevos de Stitch vía MCP (reiniciar sesión), pase de calidad a Moderno/Stitch, export TikTok 3 modos. |
| 12 | 👁️ | **Módulo financiero / Payouts valuadores** — colección encargos, split 80/20, admin AdminPayouts, FacturacionTab con tabla de encargos — **pendiente revisión personal para afinar detalles** |
| 13 | 👁️ | **Sistema de calificación de valuadores** — CTA en ReportPage, ReseñasTab ya estaba en ValuadorDashboard — **pendiente revisión personal para afinar detalles** |
| 14 | ✅ | **Base de datos histórica de avalúos** — es `db.valuations` en MongoDB, se guarda automáticamente. Usada como comparables internos (Sección 0). |
| 15 | 👁️ | **Newsletter fase 1** — suscriptores en MongoDB, sección en LandingPage, AdminNewsletter, endpoints subscribe/unsubscribe. Envío real pendiente (SendGrid) — **pendiente revisión personal para afinar detalles** |
| 21 | ✅ | **Google Sheets como fuente de comparables** — scraper conectado. |
| 64 | ✅ | **MongoDB como fuente primaria de comparables** — `mongo_comparables.py` filtra `mercado_props` por municipio/tipo/m2 ±40%, integrado en server.py step 0.5. |
| 65 | ❌ | **Sync Sheets → MongoDB — OBSOLETO (08-jul)** — Google Sheets llegó al límite de 10M celdas y se RETIRÓ del scraper (Mongo es la única base, commit 34fb66e). El scraper ya escribe directo a Mongo. No correr sync-sheets. |
| 66 | ✅ | **Correr scrapers pendientes** — Todos completados: CYT ✅, MITULA ✅ (re-scrape 20-may con fix m2_terreno), INMUEBLES24 ✅, PINCALI ✅, PROPIEDADES_COM ✅, VIVANUNCIOS ✅. Total: 84,597 props en MongoDB. |
| 34 | ⏳ | **Email notifications** — SendGrid. |
| 35 | ⏳ | **WhatsApp notifications** — Twilio. |
| 133 | ✅ | **UI rediseñada + desplegada (15-jul):** promo -50% en la misma fila del cuadro Plan Pro (solo en su tab, alto contraste); **tabla tipo Excel "Mi inventario subido"** (`GET /api/inmobiliaria/propiedades`, refresca al confirmar) con **muestra de ejemplo** si vacío y **sin scroll horizontal**; barra de subida delgada (Descargar Plantilla a la izq + texto, ícono clicable, condiciones anti-dup mensuales, contador "N útiles · sin repetir"). **Plantilla +3 columnas OPCIONALES: Coto/Edificio, Piso, Amenidades** (texto) en `core/data_exchange.py` (COLUMNAS/EJEMPLO/normalizar/NUMERICOS) + guardado al CRM en `routers/data_exchange.py`. Test 10/10. Commits `5c9b59d`+`b58ba71`. — **PRUEBA EN VIVO PASADA (10-jul).** e2e contra staging (backend local + Bearer): `/plantilla` descarga xlsx OK; `/analizar` preview correcto sin escribir (casa+terreno válidas, 1 inválida rechazada, terreno sin año NO va al pool, descuento 20%); `/confirmar` escribe 2 al CRM + 1 al pool (`portal_origen=DATA_EXCHANGE`, `fuente=data_exchange`, tipo canónico, antigüedad derivada) + `data_exchange_pct=20`; dedup OK (re-subir=duplicadas, re-confirmar=400, no doble-escritura). Datos de prueba limpiados. — **Enfoque final: plantilla propia (NO IA)** — damos plantilla .xlsx con columnas fijas (1ª col=Tipo), parseo determinista. `core/data_exchange.py` (plantilla/parseo/validación por tipo, test 8/8) + `routers/data_exchange.py` (`/plantilla`, `/analizar` preview, `/confirmar`). **Dual-write** (integra DocuProp): CRM `propiedades_inmobiliaria` (origen=data_exchange, todas las válidas) + pool `mercado_props` (fuente=data_exchange, solo con precio+año, protegido por #135b) + bandera `users.data_exchange_pct=50`. Tab montado en InmobiliariaDashboard (titular). Build OK. **PENDIENTE: prueba en vivo** (subir plantilla real con backend+staging, verificar escrituras). Fase posterior: Estatus/Precio de cierre (oro para motor); fallback IA de mapeo si no usan plantilla; DocuProp checklist; enganche real al carrito cuando exista. |
| 134 | ✅ | **Herramienta "Edades por zona" (crowdsourcing v1 interna)** — panel `/edades-zona` + celda Edad en avalúo. Captura edad (rangos finos Ross-Heidecke + 10-en-10 hasta los 40s + año exacto + "Nuevo" autocompleta), **remodelación → edad efectiva ponderada** (fórmula dictamen del perito, grado→p 0.20/0.35/0.55/0.95), conservación, coto. Cascada Estado→Municipio→Colonia (combo buscable), ficha se bloquea tras guardar, propagación "aplicar al grupo del coto" (agrupa variantes ABIE), corregir colonia con **SEPOMEX oficial+CP**. Acceso desde dashboards valuador/inmobiliaria + panel admin. Backend `routers/edades.py`, `EdadesZonaPage.jsx`. Verificado end-to-end. Ver `project_propvalu_edades_crowdsource`. |
| 135 | ✅ | **Enricher PINCALI — guardia SEPOMEX en breadcrumb** — HECHO 09-jul. El breadcrumb ya se parsea del HTML `/en/home/` ya descargado (no hace falta fetch extra a `/inmueble/`, misma info). Agregada guardia: el último nodo (colonia) solo se acepta si `_colonia_valida_sepomex(candidato, crumb[-2])` (colonia real del municipio en `sepomex_v2.json`) → rechaza nombres de municipio ("San Pedro"). Si rechaza, NO escribe (no degrada; colonia previa intacta). Test `test_pincali_colonia_135.py` (OK). **Backfill masivo NO se hizo**: medido en Mongo solo 6/42337 docs con colonia==municipio (DeepSeek ya depuró); no vale re-fetch de 42k páginas. La #135b protege además correcciones humanas. |
| 135b | ✅ | **CRÍTICO — correcciones del perito "sticky" (no se pisan al re-scrapear)** — id_unico = md5(url) → NO duplica, pero el scraper/enricher escriben `colonia` en cada corrida y **NO respetan `colonia_fuente`**. HECHO 09-jul: `FUENTES_PROTEGIDAS={perito_correccion,usuario_correccion,perito_crowdsource}` en scheduler.py y enricher.py. `scheduler._guardar_en_mongo` lee el doc existente y poda colonia/conjunto/colonia_fuente si colonia_fuente protegida, y anio_construccion/edad_fuente si edad_fuente protegida. Enricher: gate en flags `falta_colonia`/`falta_ano_const` (no re-extrae ni pisa lo protegido; proyección +colonia_fuente/edad_fuente). Test `test_sticky_135b.py` (OK). Compila. |
| 136 | ⛔ | **BLOQUEADO POR DATOS (medido 09-jul):** 0 comps en prod tienen `edad_efectiva`/`grado_remodelacion`/`edad_fuente` (crowdsource v1 sin envíos reales). Wirear el motor ahora = código especulativo NO medible por el validador + riesgo doble conteo (calcEdadEfectiva fija + factorConserv). NO construir hasta que haya volumen de edad crowdsource en comps. Motor: `calcEdadEfectiva(edad,conserv)` + `FACTORES_CONSERVACION` operan sobre el SUJETO uniforme a comps; el wiring de edad POR-comp no existe aún. |
| 137 | ⛔ | **BLOQUEADO POR DATOS (medido 09-jul):** 0 docs con `conjunto` o `colonia_fuente` en prod (nadie ha corregido colonias ni taggeado cotos aún). El flywheel coto→colonia aprende de esas correcciones; sin ellas no hay qué aprender. NO construir hasta que peritos usen la herramienta y generen correcciones. |
| 138 | ✅ | **Edades: sugerir colonias de la misma zona por CP** — HECHO 09-jul. Chips "Misma zona · CP" sobre el input de colonia: colonias oficiales que comparten el CP (guardado, o el de la colonia actual si coincide con oficial SEPOMEX). Input+datalist sigue buscando cualquier colonia por nombre; CP se ajusta a la elegida. Backend `/comps-sin-edad` manda `codigo_postal`. Nota: solo 2783/112k props traen CP → el ancla suele derivarse del nombre de la colonia actual. |
| 139 | ⏳ | **Edades crowdsource: modelo de CONSENSO (trivia) — DISEÑO, no construir aún (0 estimaciones reales)** — hoy `/edad-estimada` hace `$set` = ÚLTIMO voto pisa al anterior; no hay consenso ni concurrencia segura. Cuando se abra a varios/público: (1) NO sobreescribir, **acumular** cada estimación en arreglo con `$push` atómico (resuelve concurrencia: dos a la vez no se pisan); (2) canónico = **mediana** (año/edad, publicar con N≥2-3 y baja dispersión, si no "en disputa") / **mayoría** (colonia, empate espera desempate); (3) **peso por rol** perito>inmobiliaria>público; correcciones perito siguen sticky (#135b) sobre el consenso; (4) **anti-gaming**: propiedades de control con respuesta conocida, descartar votos de quien falla. Hacer el `$set`→`$push` ANTES de abrirlo a varios usuarios. |
| 140 | ⏳ | **Tokenización por calidad + reputación (DISEÑO, depende de #139)** — premiar ACERTAR, no llenar. (a) puntos por campo ponderados por valor (año exacto > rango; corrección confirmada > confirmar existente); (b) **provisional→confirmado**: el envío da puntos provisionales, se vuelven canjeables solo al corroborarse por consenso/control; dato errado→0 + baja confianza; (c) "No sé" siempre gratis; (d) **score de confianza/acierto por usuario** calibrado con propiedades de control. **Meta canje v1: 75–150 propiedades = 1 opinión de valor gratis ($320, costo marginal ~$0). NO abrir canje sin anti-gaming (#139).** v1 plano (1 pt/propiedad, No sé=0); v2 ponderado. |
| 141 | ⏳ | **Paneles de reputación/ranking (DISEÑO, depende de #140)** — visibles en dashboard del asesor/perito Y de la inmobiliaria, **agregado (empresa) y particular (sujeto)**: tasa de acierto, grado de confianza, comparación/ranking vs otros asesores. Dimensión adicional: **uso/actividad del sistema** (engagement) — pero SOFT: sirve como nivel/racha que sube confianza más rápido o da bonos chicos, NUNCA como valor de canje directo (uso ≠ calidad; premiar actividad cruda se farmea). El canje siempre atado a aportes CORRECTOS. Los números salen del engine #139/#140 → NO construir antes (paneles vacíos). **Consumidores además del usuario: Admin PropValu** (quién/qué empresa es más fiel/activa y aporta mejor calidad → cuentas, soporte, upsell, detectar quién envenena) **y asociaciones/colegios** (ranking/credencial de calidad de miembros, monetizable B2B). Misma data, 3 audiencias. |
| 143 | ✅ | **NOCNOK tipo_propiedad sin normalizar — CERRADO 10-jul.** NOCNOK saltaba el chokepoint (upsert propio) → 87.6% no-canónico (Casa/Local Comercial/exóticos). (1) Fix scraper: pasa por `normalizar_tipo_propiedad`. (2) Guardia sticky #135b agregada a su write (lee colonia_fuente/edad_fuente/tipo_fuente, no pisa protegidas; `FUENTES_PROTEGIDAS` local para evitar import circular). (3) Backfill aplicado: 2441 corregidos → **0 no-canónico**. Verificado. |
| 144 | ✅ | **Alta manual de 1-10 propiedades sin plantilla (27-jul)** — formulario visual (`PropiedadManualForm.jsx`, modal compartido) con 2 puntos de entrada: tab **Data Exchange** de la inmobiliaria (`POST /api/inmobiliaria/data-exchange/manual`, escribe CRM+pool, cuenta para el descuento) y **Verificación por Zona** (`POST /api/comparables/manual`, perito/inmobiliaria/admin vía doble auth `_quien()`, solo pool). Reusa `normalizar_fila`/`validar_fila`/dedup de `core/data_exchange.py`. Campos: edad por año exacto o rango, check de remodelación (grado+año), conservación, amenidades con los iconos ya existentes de Promociones (pastillas icono+texto), link de origen con verificación en vivo best-effort + dedup por URL. **Bug de raíz encontrado y arreglado de paso:** `index.css` tenía `--background`/`--primary`/etc duplicadas (hex plano vs HSL-triplete) que dejaba `bg-background`/`bg-primary` transparentes en TODO el sitio — renombradas a `--hex-*`. Tests 13/13. Commits `4dc68f5`+`42924a2`. **28-jul:** exigía casi todos los campos (recámaras/baños/medios baños/estacionamientos/niveles/conservación) para casa/depto/local/oficina/bodega — se dejaron opcionales (`1a6a723`); se agregó nota visible en ambos modales explicando que el alta de Verificación por Zona es anónima (solo pool) y la de Data Exchange sí cuenta para el descuento (CRM+pool). |
| 142 | ⏳ | **Data Exchange: descuento tokenizado por CALIDAD (parte real HOY)** — hoy el 50% es plano. Ponderar la subida por: **no duplicados** (contra el pool + subidas previas de la misma inmobiliaria por id/dirección), **completos** (score de campos llenos por tipo, ya validado en `core/data_exchange.py`), y **verificables** (cruce con pool/realidad, más adelante). El % de descuento sale de esa calidad. Dedup+completitud son construibles YA (hay datos reales: sus uploads); el enganche al carrito espera a que exista carrito. |
| 146 | ✅ | **Créditos con expiración real (28-jul)** — ledger `creditos_ledger` en `User` (`backend/core/creditos.py`). Gamificación (150 pts=1 crédito) expira a 3 meses; créditos pagados mensuales expiran fin de mes y se REEMPLAZAN (no acumulan). Choke point de lectura en `core/auth.py::get_current_user` (`saldo_efectivo`, calculado al vuelo, sin cron). Verificado local con escenarios reales + idempotencia. Commit `bb949f1`. |
| 147 | ✅ | **Bolsa de Requerimientos entre asesores (28-jul)** — tab nuevo en dashboard inmobiliaria. Publica requisitos de cliente, busca coincidencias YA existentes en `mercado_props` (contacto resuelto) al vuelo; sin match, activa 15 días, recálculo de "nuevas" LAZY al abrir `/mias` (mismo patrón que créditos). Layout: KPIs → mapa de zonas con demanda (`MapaAvaluos.jsx` + `Modulo Drive IA/_geo/proximidad.py` para coords) → feed de otros primero → mis requerimientos → captura en modal ficha. Backend `core/requisiciones.py`+`routers/requisiciones.py`. Commit `330ada6`. |
| 148 | ⏳ | **Manual de Arquitectura ZMG — 82 marcas pendientes** (eran 168). Cerrado: las 13 tablas de m2, las 13 décadas con colonias por segmento, y 34 fechadores 1995-2026 en el catálogo de materiales. Falta: 22 de cartografía (INEGI/IIEG/Visor Urbano), 15 de fotos, 19 de ampliar y ~26 verificaciones. Carpeta fuera del repo: `C:\Users\pedru\valuation-ai\Manual-Arquitectura-ZMG`. |
| 148 | ✅ | **Fix "Activar análisis IA" en reporte — falso positivo de cuota (28-jul)** — diagnosticado con logs reales de Railway: NO era falta de cuota (gemini-2.5-flash usa 1-2 de 20/día). Falla real = JSON truncado puntual + los "respaldos" `gemini-2.0-flash`/`-lite` tienen cuota 0 PERMANENTE en el proyecto GCP (nunca protegieron nada). Fix: `response_mime_type=json` en Gemini + **DeepSeek real como respaldo** (`httpx` directo, sin dependencia nueva). Verificado end-to-end ambos caminos (Gemini normal + Gemini roto→DeepSeek). Commit `0410734`. |
| 149 | 🔄 | **Ficha A4 EstateElite — Hoja 1 aprobada, Hoja 2 RECHAZADA (28-jul)** — Hoja 1 (`LayoutA4EstateElite.jsx`) con fuentes reales del export de Framer (Archivo Black/Barlow Condensed/Bodoni Moda en `public/fonts/estate-elite/`) y datos reales sin fabricar claims — usuario la vio bien. Hoja 2 (`LayoutA4EstateElite2.jsx`, galería+amenidades+contacto) construida desde un screenshot del editor de Framer — usuario la rechazó explícitamente ("está terrible"). **Retomar próxima sesión: pedir dirección de diseño ANTES de tocar código, no asumir referencia.** No desplegado a producción. |

---

## Skills y Herramientas Dev

| # | Estado | Tarea |
|---|---|---|
| 36 | ✅ | **Skills globales** — `/backup`, `/recordar`, `/ayuda`. |
| 37 | ✅ | **Skills PropValu** — `/ctx`, `/status`, `/restart-backend`, `/check-errors`, `/end-session`, etc. |
| 38 | ✅ | **Skills Scraper** — `/logs`, `/reset-scraper PORTAL`. |
| 42 | ✅ | **Script seed/datos demo** |
| 43 | ✅ | **Permisos automáticos Claude Code** |
| 44 | ✅ | **Statusline con barras visuales** |
| 45 | ✅ | **Statusline placeholders** |

---

## QA / PDF

| # | Estado | Tarea |
|---|---|---|
| 17 | ❌ | **Marca de agua PropValu en PDF** — descartado, no necesario. |
| 18 | ✅ | **Pruebas y ajustes finales del PDF de valuación** |
| 58 | ✅ | **PDF de Mercado** — reemplazado html2canvas por window.print() con @media print |
| 60 | ✅ | **Responsive móvil (03-Jun)** — AdvertiserConsole grid de fotos `grid-cols-4`→`grid-cols-2 sm:grid-cols-4` (botón borrar tappable en móvil). AdminAdsAnalytics tabla anunciantes `overflow-hidden`→`overflow-x-auto`+`min-w-[640px]` (antes recortaba columnas en móvil). Barrido confirmó: era el único grid 4-col fijo en todo el frontend; los grid-cols-3 son stats chicos (OK 3-up). |
| 164 | ✅ | **Mini-reporte de 1 hoja, solo venta (24-ago)** — pedido por 2 clientes (flipping, bodegas en renta) que solo quieren el valor sin el reporte completo de 5 páginas. `generate_mini_report_html()` en `report_generator.py` (folio, dirección, valor+rango, terreno/construcción/edad/tipo, mini-mapa, "N comparables en zona"), endpoint `POST /valuations/{id}/generate-mini-report` (sin IA, sin gasto de créditos — reusa el resultado ya calculado), botón "Resumen 1 hoja" en `ReportPage.jsx` (reusa el mismo flujo de descarga/print que ya existe vía `/gracias`). **Sin bloque de renta** — ver #165. Verificado end-to-end en navegador con avalúo real (`val_c8e86a51563e`). **+ARV (25-ago):** `generate-mini-report` corre el motor una 2ª vez forzando `estadoConservacion:"remodelacion_completa"` (edad efectiva→5), agrega `arv_estimado` al doc y una caja "Valor Estimado Remodelada (ARV)" en el mini-reporte, con leyenda explícita (no tooltip — no sirve en PDF impreso). Verificado con la misma propiedad: as-is $1,804,153 → ARV $3,312,085 (45 años, coherente con revertir depreciación física). |

---

## Infraestructura / Deploy

| # | Estado | Tarea |
|---|---|---|
| 61 | ✅ | **Deploy backend** — En **Railway** (no Render). Proyecto `propvalu-backend`, online en https://propvalu-backend-production.up.railway.app. Dockerfile + railway.json + start.py. Deploy manual vía `railway up` (auto-deploy GitHub no configurado en esta rama). |
| 62 | ⏳ | **Agregar secret `PROPVALU_BACKEND_URL` en GitHub** — URL de Render una vez desplegado, para que el workflow de scraper pueda hacer el sync final. Ir a github.com/Pedrucus1/valuation-ai → Settings → Secrets → Actions |
| 63 | ✅ | **Consolidación de ramas** — Resuelto 02-Jun. `master` y `feature/search-api` tenían historias DISJUNTAS (sin ancestro común; master era solo el respaldo inicial de feb). En vez de force-push se creó rama **`main`** con el código actual y se puso como **default en GitHub**. Rama local renombrada a `main`. Pendiente opcional: borrar `master` y `feature/search-api` redundantes en origin (no urge). Deploy sigue siendo `railway up` (branch-agnóstico). |
| 64 | 🔄 | **Auditoría de seguridad del portal — sustancialmente completa** — Ver **`SEGURIDAD_ARQUITECTURA_ANTECEDENTES.md`** (fuente de verdad). Cerrados 02-Jun: **IDOR** avalúos (4e15e10), **escalada privilegios** upgrade-role (4e15e10), **S1+S2+S3** hash bcrypt+migración+timing-safe+expiry tokens (21bca3e), **S4** rate limiting login/registro 10/min (70653dc), **S5** validación+topes+rate limit en públicos feedback/reseñas/newsletter (b13acb1). S6 barrido IDOR sin más casos. Ya OK de antes: CORS restringido, cookies httponly/secure, upload KYC allowlist+5MB+ownership, secrets fuera del repo. **DESPLEGADO A PROD 02-Jun** (railway up, build f783c269; smoke test 5/5: IDOR 403, anónimo 200, rate limit 429). S7 (auth legacy responder_resena) y S4b (rate limit avalúo público 30/h) DESPLEGADOS 02-Jun (build 9f3943a1, commit 1ffed47, prod 404×30→429). **Todos los hallazgos de seguridad cerrados.** Quedan solo infra (#66.3/4/5) y higiene de ramas (#63). |
| 65 | 🔄 | **Escalabilidad** — **ÍNDICES HECHO 10-jul** (`208970a`): `mercado_props` (~100k) solo tenía `_id_` → COLLSCAN verificado con explain en cada request de mercado + dedup data_exchange. Agregados a `_ensure_indexes`: `{activo,tipo_operacion,tipo_propiedad}`, `id_unico`, `inmobiliaria_id`, `{municipio,tipo_propiedad}` + `propiedades_inmobiliaria.user_id`. **Se crean al reiniciar backend (falta deploy).** Colonia NO se indexa (usa $regex). **Pendiente:** paginar endpoints con `.to_list(None)`/`.to_list(100000)` (encargos, newsletter, data_exchange dedup — colecciones chicas hoy, no urgente); cold starts Railway; plan de crecimiento. |
| 66 | 🔄 | **Revisión de arquitectura y deploy** — REVISADA 01-Jun-2026. Hecho (quick wins): índices MongoDB en rutas calientes, CORS restringido a dominio propio + cuenta Vercel, handler de error sin filtrar internals. Ver sub-tareas #66.x para lo pendiente. |
| 66.1 | 🔄 | **Partir el monolito** — EN PROGRESO (server.py 4482→**1786, −60%**). 17 routers extraídos (incl. mercado, ads, encargos, inmobiliaria/equipo). Restante: SOLO valuations (~1230 líneas, diferido hasta probar avalúos) + glue de app/scheduler (mercado-admin snapshot/sync entrelazado con jobs — puede quedarse). Detalle abajo ↓. **Fundación:** `models.py`, `core/db.py`, `core/auth.py` (get_current_user/require_auth/require_admin/pwd_context), `core/accesos.py`, `core/pricing.py` (PRECIOS_DEFAULT), `core/config.py` (UPLOADS_DIR/KYC_DIR/ADS_DIR/SCRAPER_DIR). **13 routers extraídos:** access, newsletter, cms, feedback, admin_config (precios/mantenimiento/blacklist/cobertura), admin_misc (alertas/stats), admin_inmobiliarias, admin_reportes (valuadores/reportes), directorio, auth, admin_usuarios, kyc, admin_scraper. Patrón: `routers/<dominio>.py` con `APIRouter(prefix="/api")` + `app.include_router(...)`. Verificado en prod. **PENDIENTE:** anuncios/anunciantes (~830 líneas, advertiser auth), mercado público (usa caché `_cache_get/_set` → mover a core/cache.py), mercado-admin/encargos, `/inmobiliaria/equipo`, y **valuations al final** (se está probando). Técnica para bloques grandes: script Python que copia líneas por marcadores y reemplaza @api_router→@router. **DEFERIDO (03-Jun, decisión usuario):** el extract de valuations (~1410 líneas reales, `generate_comparables` solo ~580) es mecánicamente factible (orquesta módulos externos + core/*), pero es el core de avalúos bajo calibración activa (motor #101/#102/#104) — refactor puro sin beneficio funcional que mete churn donde se está afinando. Retomar en sesión propia con verificación de avalúo real (no basta el smoke-test de import) cuando el motor esté estable. |
| 66.2 | ✅ | **Unificar auth admin** — Hecho en commit c9bdaf4. `require_admin` (core/auth.py) valida token contra colección `admins` activo:true. No quedan comparaciones `token==ADMIN_SECRET` sueltas fuera del login. |
| 66.3 | ✅ | **Separar jobs pesados del proceso web (03-Jun)** — Scheduler embebido ya era opt-in (`ENABLE_SCHEDULER=1`, off en Railway). Cron externo cerrado: `require_admin_or_job` (acepta `X-Job-Token`==`JOBS_SECRET` o sesión admin) en sync-sheets + generar-snapshot; workflow `scraper_mensual` dispara ambos por curl. Arreglado bug: el curl usaba `ADMIN_SECRET` que `require_admin` ya no acepta post-#66.2. **Config pendiente:** secret `JOBS_SECRET` en GitHub + env en Railway. |
| 66.4 | ✅ | **Entorno de staging (03-Jun)** — cluster M0 separado `cluster1.avle5ez` (proyecto Atlas aparte=gratis). Seeded prod→staging (10k props + app data, `backend/seed_staging.py`). Backend local→staging (`backend/.env`); scraper→prod. Scripts mantenimiento con `db_target.py` fail-closed + avisan cluster. |
| 66.5 | ✅ | **Observabilidad (03-Jun)** — Sentry (errores) ya estaba. Métricas básicas HECHO: `core/metrics.py` (in-memory, sin deps: conteo/latencia/errores por ruta + uptime) + middleware HTTP + `/api/metrics` admin-only (snapshot + ping Mongo + conteos colecciones). Bonus: eliminado `/health` duplicado muerto. Single-instance (no agrega entre réplicas — para eso Prometheus). |
| 66.6 | ✅ | **Higiene de repo** — Hecho en commit c9bdaf4. `server.py.bak` (2935 líneas) y `uvicorn.log` eliminados del repo; `.gitignore` cubre `*.env`/`*.bak`/`*.log`/`memory/`. |
| 65.1 | ✅ | **Índices MongoDB** — `_ensure_indexes()` en startup: users(email,user_id), user_sessions(session_token,expires_at), valuations(valuation_id,user_id), authorized_access(email), admins(token,email). Creados en Atlas. |

---

## Scraper / Automatización mensual

| # | Estado | Tarea |
|---|---|---|
| 151 | ⏳ | **PINCALI español — buscar alternativa** — `/inmueble/` sigue devolviendo 422/202. **Confirmado 24-ago (headers reales):** es AWS WAF (`x-amzn-waf-action: challenge`), y bloquea **tanto ES como EN** (`/en/home/`), incluso con `requests` plano — no es específico del español, es sitewide. Sin ejecutar el challenge JS (browser real) no hay forma de pasarlo. Diferido a propósito: arreglar TODO lo ya scrapeado de Pincali para que quede bien en español es un trabajo grande aparte. Fix aplicado 24-ago en `enricher.py`: el detector de bloqueo daba **falso positivo de éxito** con la página de challenge (título vacío no matcheaba "cloudflare/403/captcha") — ahora se detecta. Investigar cuando se retome: si el fetch vía Playwright (ya usado para otros portales) logra resolver el challenge de forma sostenida sin quemar la IP (pruebas del 24-ago mostraron que pasa aislado pero se bloquea agresivo bajo ráfaga). |
| 152 | ✅ | **Scraper mensual NO trae datos nuevos — CERRADO 07-ago.** Causa real: 3 bugs encadenados en `scheduler.py` (marcaba tareas completadas para siempre, sin cooldown de 30 días como el enricher). Fix `_tarea_vencida()` aplicado en 3 puntos (filtro selección, guardia de concurrencia, chequeo de loop). Verificado con scraping real en vivo (CASAS_Y_TERRENOS, PROPIEDADES_COM, VIVANUNCIOS, INMUEBLES24 con props nuevas confirmadas). Commits `cefcd24`,`4b89b67`,`955a8cc`. |
| 156 | ⏳ | **Algo mata los procesos del scraper como a la hora de corriendo (hallado 07-ago, corriendo el fix de #152)** — confirmado repetido varias veces entre 07 y 09-ago (siempre ~1h), y de nuevo 10→11-ago (scheduler murió ~22h sin dejar rastro: descartado reboot, crash de Windows Event Log y fallo de auth Mongo — los tres verificados, el proceso simplemente desapareció). La CAUSA sigue sin confirmar (¿PowerShell/consola cerrada?, ¿límite de sesión?). **Mitigación nueva 12-ago:** watchdog Bash independiente (`Monitor` persistente, chequeo cada 4 min) que relanza `scheduler.py` si el proceso muere Y el log no dice "Todas las tareas completadas" — más robusto que el relanzamiento proactivo cada 55 min porque no depende de adivinar el intervalo, reacciona a la caída real. Vive solo en la sesión de Claude que lo lanzó, no es un mecanismo permanente — considerar pasarlo a tarea de Windows si el patrón sigue. |
| 158 | ⏳ | **MITULA/Lamudi bloqueado sistémicamente — 401 Unauthorized en el 100% de sus URLs** (confirmado, terminó su cola completa el 09-ago con casi puro fallo). No es rate-limit ni proxy, algo cambió en la autenticación/headers de Lamudi o bloqueó la IP de forma dura. Requiere revisión de código de `scrapers/mitula.py` (headers/auth), no reintentos. |
| 157 | ⏳ | **VIVANUNCIOS: sospecha de límite de paginación mal detectado (hallado 07-ago)** — casi todas sus tareas (Tlaquepaque/Zapopan/Chapala, venta y renta) devuelven exactamente "27 props, página 1/1", incluso en municipios grandes. Podría ser volumen real o un bug de paginación que trunca resultados. No confirmado, no tocado — revisar `scrapers/vivanuncios.py` cuando se retome scraper. |
| 153 | ⏳ | **GitHub Actions `scraper_mensual.yml` — 2 corridas canceladas por timeout** — Cron mensual (día 2, 3am UTC) SÍ existe y SÍ se dispara (corridas 02-jul y 02-ago confirmadas en `gh run list`), pero ambas terminan `cancelled` a las 5h (timeout del job). Además el workflow NO incluye PROPIEDADES_COM (solo INMUEBLES24/CASAS_Y_TERRENOS/MITULA/VIVANUNCIOS/PINCALI en scrape+enrich). Investigar si esta corrida en la nube (no depende de que la PC esté prendida) es más confiable que la tarea de Windows local para "automático de verdad" — pero antes hay que resolver #152 (mismo problema de progreso/reset aplicaría aquí, o peor: VM efímera = sin progreso persistido = intenta las 366 desde cero cada vez, por eso el timeout). |
| 154 | ⏳ | **Tarea de Windows `ScraperMensual` no se disparó el 07-ago** — Configurada correctamente (mensual, día 7, 10am) pero en modo "Solo interactivo" (logon type) — 1 missed run registrado. Cambiar a "ejecutar esté o no la sesión iniciada" requiere guardar credenciales de Windows en la tarea. Baja prioridad si se decide usar GitHub Actions (#153) como mecanismo principal en vez del Task Scheduler local. |
| 155 | 🔄 | **Bugs encontrados en la corrida del 07-ago (agente)** — (1) ✅ **FIX 12-ago:** encoding "Tlajomulco de Zúñiga"/"Tonalá" era mojibake TRIPLE vía cp1252 (no simple latin-1 como se creía) → `scheduler.py::_normalizar` ahora aplica hasta 3 rondas de `cp1252` iterativo contra las zonas canónicas; verificado en vivo, las 64 tareas "Zona no encontrada" pasaron a intentarse de verdad. (2) ✅ **FIX 12-ago:** CASAS_Y_TERRENOS `'str' object has no attribute 'get'` — MeiliSearch a veces devuelve `hit["slugs"]` como string en vez de dict; guardado con `isinstance(slugs, dict)` en `scrapers/casas_y_terrenos.py::_normalizar_hit`. (3) ⏳ **sin tocar:** el auto-enricher interno que dispara `scheduler.py` al terminar un portal usa una ruta relativa que resuelve mal (`C:\Users\pedru\OneDrive\Documents\enricher.py`, no existe) — nunca corre. |

---

## Motor de Valuación IA (Modulo Drive IA)

| # | Estado | Tarea |
|---|---|---|
| 172 | ⏳ | **`enrich-stream` no existe en el backend (01-sep)** — `ComparablesPage.jsx:177` abre un `EventSource` a `/valuations/{id}/enrich-stream` para mostrar el anuncio (`AdPopup`) mientras "enriquece" comparables. Confirmado con `git log -S` que ese endpoint NUNCA se creó en `server.py` ni en ningún router — 404 instantáneo, `es.onerror` apaga el popup en la misma fracción de segundo (por eso "no salía el ads" ahí). El usuario pidió construir el endpoint real (streaming SSE, enriquecimiento con IA en vivo) — queda para la siguiente sesión, no se tocó hoy. |
| 171 | ⏳ | **Comparables inventados cuando faltan reales (01-sep, sin decisión)** — `server.py:934-994`: si hay <10 comparables reales, el sistema fabrica los faltantes con matemática aleatoria y les pega nombre de portal real + URL con formato real pero falsa. Mostrado al usuario en vivo (captura "El Roble" con varios inventados) — preguntado qué hacer (no fabricar y mostrar aviso de cobertura insuficiente, vs. etiquetar claro como "estimado sin fuente real"), sin decidir todavía. Relacionado: la selección de comparables de esa pantalla NO alimenta `calculate-remi` (el motor busca por su cuenta) — confuso mencionado explícitamente por el usuario. |
| 170 | 🔄 | **Log de actividad/errores para el admin (01-sep)** — pedido explícito ("el admin está ciego, no sabe dónde ni cuándo sale un error"). Backend hecho: middleware `_ActivityLogMiddleware` en `server.py` persiste a `db.activity_log` toda respuesta `status>=400` (path, status, mensaje, email, IP, duración) — a propósito NO loguea clicks/movimientos (ruido + privacidad). Endpoint `GET /api/admin/activity-log` en `routers/admin_activity.py` (nuevo archivo, protegido `require_admin`). Bug propio detectado y arreglado la misma sesión: faltaba `app.include_router(admin_activity_router)`. **Falta:** página `AdminActividad.jsx` en frontend (plan completo en `C:\Users\pedru\.claude\plans\adaptive-giggling-pixel.md`) y los ~8-10 `insert_one` de eventos de negocio (login/registro/valuación/reporte) que el plan contemplaba. |
| 169 | ✅ | **Bug de ads invisibles (01-sep)** — `file_url` salía como ruta relativa (`BACKEND_URL` vacía en Railway, frontend en otro dominio pedía `/uploads/...` a sí mismo → 404) y de ahí en adelante con `http://` en vez de `https://` (Railway termina TLS en el proxy, `request.base_url` no lo respeta sin `ProxyHeadersMiddleware`). Fix en `routers/ads.py`: arma la URL desde `X-Forwarded-Proto` + `request.url.hostname`. Verificado en prod tras deploy. |
| 168 | ✅ | **Bug "Continuar Reporte" — 503 mudo en zonas sin comparables (01-sep)** — cadena real: Tavily/Serper solo devuelven snippets de páginas de listado sin precio visible → DeepSeek correctamente no inventa → 0 comps → el motor colgaba sin ningún timeout hasta que Python lo mataba a los 30s (503 sin ningún log, Python descartaba el stdout/stderr del proceso muerto). Fixes: timeout explícito en Tavily/Serper/Brave (8s)/Gemini(15s)/DeepSeek(12s); margen subprocess 30s→45s; captura de stderr parcial en el `except TimeoutExpired`. **Fix de fondo:** `buscarCompsPaginasReales()` en `motor_remi_api.js` — lee la página completa de cada resultado de Tavily y extrae el JSON-LD `RealEstateListing` (dato estructurado real, sin IA) en vez de depender del snippet corto. Verificado en vivo con "El Roble, El Arenal": 4 comps reales de PINCALI, resuelve en 2s. Gotcha: PINCALI etiqueta terrenos como `SingleFamilyResidence` en su JSON-LD — filtro real es recámaras>0 + m² 30-1200, no el `@type`. |
| 167 | ✅ | **Calculadora de viabilidad de flipping (25/26-ago)** — página independiente `/flipping` (`FlippingCalculatorPage.jsx`), opcionalmente conectada a un avalúo vía `?valuation_id=` (jala `arv_estimado` de #164 y la dirección). Desglosa precio de compra, deudas (agua/predial/luz/cable/crédito), remodelación (tabla `quality_costs` como referencia, no autocálculo), costos de venta/financiero/administración/cierre → margen neto + cálculo inverso de oferta máxima al dueño. **Se descartó la regla del 70% de EE.UU.** (no aplica a México) — defaults calibrados con datos reales: ISAI+notarial ~4% (promedio nacional ISAI 2.87%), comisión inmobiliaria 5%, margen objetivo 25% (rango recomendado 20-30%), ISR hasta 35% con nota de exención rara en flip rápido. Cálculo 100% en frontend (aritmética, sin round-trip); backend (`routers/flipping.py`, mismo patrón que `requisiciones.py`) solo persiste el snapshot en `flipping_calcs` por `user_id`. Sin créditos ni IA por ahora (a propósito — el usuario dijo "quizás" para ambos). Botón de entrada en `ReportPage.jsx`. Verificado en navegador: precarga desde avalúo real, aritmética correcta a mano, modo standalone sin avalúo, falla con gracia sin sesión. Commit `0b1502e`. Idea relacionada para después → **#166** (calculadora de renta para estudios/aparta-estudios). |
| 166 | ⏳ | **Calculadora de inversión para renta (estudios/aparta-estudios, "Vive de las Rentas", 25-ago)** — idea del usuario, para DESPUÉS de la calculadora de flipping (#167). Estilo Carlos Devís: rentabilidad bruta/neta, cash-on-cash, flujo de caja mensual sobre un estudio o aparta-estudio destinado a renta (no a venta). Sin diseño aún — solo anotado para no perderlo. |
| 165 | ⏳ | **Motor de OPI de renta independiente (DISEÑO, 24-ago)** — hoy la renta es `valor_venta × rental_factor` derivado (`scraper.py:410,478`, `ai_comparables.py:355,405`), NO un estudio de comparables de renta propio. Analizadas las fórmulas reales del perito en `opi_perito.xlsx` (hojas "OPI Loc Com" y "OPI Rentas"): la doble comprobación real es venta-homologada×factor **vs** comparables de renta homologados directos, comparando ambos ratios. Diseño propuesto: `valuarRentaPropiedad(prop)` en `motor_remi_api.js` (mismo estilo que `valuarPropiedad()`, filtra comparables por `operacion='renta'`) + `factorRentaVenta()` con ratio real observado (mejora sobre el 0.008 fijo del Excel). Detalle completo en memoria Claude `project_propvalu_opi_renta_independiente`. Aún sin escribir código — pendiente decidir si va como función nueva o motor separado. |
| 146 | ✅ | **Purga de titulares de anuncio en `colonias_decada.json` + núcleos históricos (3-ago)** — 4,749→**3,896**; con municipio 54%→**63%**. El 46% sin municipio eran ~2,100 registros que no son colonias sino titulares de anuncio en inglés del scraper. Arreglado dentro de `limpiar_colonias_decada.py`: `canonica()` despega la cola en inglés antes de agrupar (114 grupos fusionados) y `es_junk_colonia` descarta 728 **con indulto de SEPOMEX** (falsos positivos: `2001` por 3+ dígitos, `san miguel de huentitan el alto 1a secc` por 34 caracteres). Campo nuevo **`nucleo_historico`** en 79 entradas vía el campo `tipo` de SEPOMEX (Pueblo/Ranchería/Ejido/Barrio/Hacienda): su década es la del registro municipal, no la de la edificación. **`heuristica-anillo` sigue en 71% y ninguna edad fue verificada.** |
| 147 | ⏳ | **Verificar la bolsa Guadalajara-1990s de `heuristica-anillo`** — worksheet en `Manual-Arquitectura-ZMG\Peticion_Verificar_Heuristica_GDL_1990s.md` (26 colonias). Falsa casi por construcción: documentadas 0% ahí y la ciudad decrece desde 1990. El usuario trae las fechas; procesarlas y reasignar. Incluye 16 titulares de anuncio pendientes de borrar (2 dudosos: `florencia gardens`→Jardines de Florencia, `new providence`→Nueva Providencia). |
| 145 | ✅ | **Colonias limpias en origen + normalizador único (2-ago)** — `backend/core/colonias.py` concentra `norm_col_key`/`limpia_decor`/`norm_muni`/`es_junk_colonia` (antes 4 copias divergiendo) y expone `decada_de(nombre, municipio)`. Truncaciones del scraper se restauran (`omos X`→`colomos X`, `inas X`→`colinas X`): **Colomos Providencia no es Providencia**, el código viejo mezclaba colonias distintas. `coto`/`condominio`/`privada` ya no se borran (edad propia). `colonias_decada.json` 5,018→4,749 con `municipio` en el dato, homónimas `nombre|municipio`, 102 marcadas `homonima_en`, acotado a ZMG+Ribera (-59). `cache_index` 5,019→2,809 y maestro 4,988→4,781, **760 comps recuperados**. Auditoría: 0 llaves inalcanzables, 0 contradicciones. Scripts: `limpiar_colonias_decada.py`, `consolidar_colonias_idx.py`, `auditar_colonias_lectura.py`. |
| 146 | ✅ | **Homónimas CERRADAS (3-ago)** — 107 pares fechados en `homonimas_resueltas.json`, 0 pendientes y ninguno bajo 60 pts; `por_municipio` de 30 a 107. Se resolvió con investigación documental del usuario (planes parciales + % de edificación por ortofoto), integrada con `integrar.py`. Criterio fijado: la década es la de la **edificación**, no la del trazo. |
| 147 | ⏳ | **Cerrar antes/después del validador tras la consolidación** — baseline 2-ago: ±10 49.8% · ±15 62.3% · ±20 72.5% · errAbs 15.2% (207 OPIs). El "después" quedó corriendo al cerrar. Decisión del usuario: si sale peor NO revertir, revisar. |
| 148 | 🔄 | **Bug depreciación física backend Python (`server.py::calculate_valuation`, no el motor JS) — FIX desplegado + lab en observación (3-ago, `998d7b8`)** — Caso real Escorpión 3518 La Calma: 2 OPIs con `estimated_value` IDÉNTICO (6,394,032.00) con y sin remodelación. Causa: `age_depreciation=min(edad/60,0.50)` + `total_depreciation` topado a 0.60 saturaban igual en ambos casos (edad≥30 años). Fix aplicado en prod: quitado el tope intermedio de 0.50, tope final subido a 0.85. **Hallazgo mayor:** la hoja real del perito (`Modulo Drive IA/opi_perito.xlsx` → sheet "Ross Heideke") usa vida útil **70 años** (no 60) y deprecia a **0% a los 70 años** vía tabla de doble entrada edad×conservación (VLOOKUP, `OPI Constr`/`OPI Loc Com b`/`Mercado`). El motor JS (`motor_remi_api.js::getRH/FACTORES_CONSERVACION/calcEdadEfectiva`) YA tiene una curva calibrada contra `cerebro_datos.json` que reproduce esa tabla casi exacto (`getRH(54,70)=31.7%` vs 30.9-31.7% real). Se portó esa fórmula a Python como `_depreciacion_lab()`/`result_lab_rh` en `server.py` (**NO reemplaza `estimated_value` todavía** — solo se guarda en paralelo para comparar). Validado offline contra 26 OPIs reales de prod: 4 casos con delta >20pp, 6 entre 15-20pp. Regla nueva del usuario: vida útil 70 años para calidad Lujo/Superior/Medio Alto, 60 para Medio Medio/Medio Bajo/Económico/Interés Social (asumido el corte en Medio Alto, no confirmado explícito). **Bug relacionado encontrado (sin arreglar):** `construction_quality` del frontend (`Lujo/Superior/Medio Alto/Medio Medio/Medio Bajo/Económico/Interés Social`) no hace match con las claves de `quality_costs` en `server.py` (`Interés social/Media/Media-alta/Residencial/Residencial plus`) → el costo por m² SIEMPRE cae al default $16,000 (Media), la calidad seleccionada no afecta nada. **Pendiente:** (a) decidir si `result_lab_rh` reemplaza `estimated_value` en prod tras más validación: (b) ~~arreglar el mapeo de `construction_quality`↔`quality_costs`~~ **hecho, ver #149**. Detalle en memoria `project_propvalu_depreciacion_fisica`.

**ACTUALIZACIÓN 24-ago:** investigación a fondo con un avalúo SHF real (folio 26080015287). Confirmado que Ross-Heidecke cuadrático SÍ es la fórmula real (`Fed=0.64` del PDF coincide exacto con `getRH(34,70)`), pero con **vida=70 fija — la regla vieja "70 solo Lujo/Superior/Medio Alto" quedó INVALIDADA** (el caso real era Interés Social y usó vida=70). `_depreciacion_lab()` en `server.py` corregido a vida=70 fija (commit `97e8620`, campo `result_lab_rh` sigue en observación, no toca `estimated_value`). También se confirmó que el perito real **NO blendea físico+mercado** — usa 100% mercado, físico solo de referencia — mientras `server.py` sí hace blend 80/20 fijo; esa es la pieza de arquitectura mal diseñada, no la curva. Impacto medido en 22 avalúos reales de PropValu: <1% en promedio (el blend+clamp ±30% amortiguan casi todo). Detalle completo en `MOTOR_ANTECEDENTES.md` (entrada "RESULT_LAB_RH 24-Ago") y memoria `project_propvalu_depreciacion_fisica`. **Pendiente sigue abierto:** decidir si se quita el blend 80/20 (usar 100% comparativo + físico como referencia aparte, como el perito real) antes de promover `result_lab_rh`. |
| 149 | ✅ | **`quality_costs` (server.py, 2 copias) unificado con `construction_quality` del frontend (4-ago)** — mismo bug que dejó pendiente #148: el dict solo reconocía `Interés social/Media/Media-alta/Residencial/Residencial plus`, el frontend manda `Lujo/Superior/Medio Alto/Medio Medio/Medio Bajo/Económico/Interés Social` (7 valores, `ValuationForm.jsx::CONSTRUCTION_QUALITIES`) — nunca hacían match, costo/m² siempre $16,000 (Media) sin importar la calidad elegida, en TODO avalúo histórico. Unificado a las 7 llaves: Interés Social $12k, Económico $14k, Medio Bajo $16k, Medio Medio $19k (default), Medio Alto $23k, Superior $30k, Lujo $45k. `CALIDADES_VIDA_70` ya usaba la escala correcta, no se tocó. Desplegado manual (`railway up --detach` — este proyecto no tiene auto-deploy de GitHub) commit `9dbc8c4`, Railway `e74d127b` SUCCESS. |
| 150 | 🔄 | **Verificador de zona (`EdadesZonaPage.jsx`): rango de remodelación +31-40/40+ y campo nuevo "calidad de construcción" por comp (4-ago)** — antes el rango de antigüedad de remodelación topaba en 26-30 años. Nuevo Select de calidad (mismas 7 llaves que #149) conectado end-to-end: payload → `POST /edad-estimada` → `routers/edades.py` valida contra `CALIDAD_CONSTRUCCION_VALIDAS` y guarda `calidad_construccion`+`calidad_construccion_fuente` en `mercado_props`. **Falta:** el motor JS no consume esta calidad de comps todavía (tampoco usaba `conservacion` de comps) — requiere validador offline antes de wirear, mismo criterio que #136. |
| 159 | ✅ | **Migración caché motor Sheet→Mongo REACTIVADA (12-ago) + recalibrar `pm2t_semilla.json` (#120)** — El Sheet CONSOLIDADO está muerto desde el retiro de Sheets del scraper (08-jul, límite 10M celdas): `actualizar_cache_consolidado.js` llevaba 5+ semanas sirviendo una foto congelada mientras el scraper solo escribía a Mongo — la data nueva NUNCA llegaba al motor. Re-probado `actualizar_cache_desde_mongo.py` (ya trae el filtro `es_duplicado_secundario` desde 27-jun, revertido en su momento SIN validar) contra 118,115 docs actuales de Mongo (vs 83-91k del intento de junio): **24,676 comps limpios, cobertura de año 44.6%**. Validador 62 OPIs (`--n 200 --desde 2025-07`) Sheet-vs-Mongo: ±10% 54.8→56.5 (+1.7pp), ±15% 72.6→71.0 (-1.6pp), ±20% 83.9→80.6 (-3.3pp), errAbs 12.2→11.7 (mejor), mediana +9.2→-9.2 (mismo tamaño, signo invertido) — **empate técnico, YA NO es la "regresión fuerte" documentada en jun (que era ~21 fuera ±20% vs 11; ahora es 12 vs 10)**. Backups pre-swap: `cache_consolidado.PRE_MONGO_TEST_20260812_031803.bak.json` / `cache_index.PRE_MONGO_TEST_20260812_031803.bak.json`. **Decisión del usuario: quedarse con Mongo, NO revertir a Sheets.** **Diagnóstico del sesgo residual:** aislado casi 100% al pool `lote_grande_cus`/`suma_partes` (18/86 casos, sesgo -13% a -32%); el pool `exacta` (53/86, el grueso) quedó casi neutro (mediana -2.0%, promedio -0.5%) — la migración en sí NO tiene sesgo global. Chequeo rápido de pm2T terreno (colonia exacta y mediana municipal, Guadalajara/Tlaquepaque/Tlajomulco) no mostró una causa única limpia (Guadalajara municipal subió 36,865→39,239, no bajó) — seguir cazando colonia por colonia cae en el patrón "NO reintentar" ya documentado (`feedback_motor_no_cazar_atipicos`). **Causa real identificada:** `pm2t_semilla.json` (calibración de #120, generada 29-jun) se calibró contra el pool viejo de Sheets y nunca se refrescó — mismo problema que tenía el caché general, aplicado a esa función específica. **ACTUALIZACIÓN 24-ago — CERRADO:** `pm2t_semilla.json` regenerado desde `cerebro_datos.json` actual (515→562 colonias) + ponderación nueva por antigüedad de la OPI (`build_pm2t_semilla.py::peso()`, folio `OPI-YY-M-D-...`, peso 1.0 primeros 3 meses, decae por trimestre — dato reciente pesa más que viejo). Validador (108 OPIs, 2025-01+): agregado casi neutro (errAbs 13.2→13.1%) pero **cero regresión y mejora consistente en TODOS los `lote_grande_cus` que cambiaron** (Secc 12122 +12.2→+11.2%, Tinajitas +14.3→+12.3%, Insurgentes −26.7→−25.7%). Backup `pm2t_semilla.PRE_REFRESH_20260824.bak.json`. Esto es mantenimiento recurrente (refrescar con datos frescos), no una mejora puntual — repetir periódicamente. Pendiente futuro opcional: recalibrar contra el pool Mongo directo en vez de solo `cerebro_datos.json`. |
| 161 | ⏳ | **Tavily agotado (432 "exceeds plan limit", confirmado en vivo 12-ago) + Serper muerto desde 09-jul → motor busca web "a lo tonto" sin resultado** — causa raíz probable: campañas de calibración masivas (`validar_lab_mercado.js`/`validar_40_opis.js` con `--n` grande) iteran cientos de OPIs y cada uno sin comps en caché dispara 2 búsquedas Tavily; medido 495 comps encontrados en 1 solo día (02-ago) en 87 colonias. Agregado Brave Search como 3er escalón de la cascada `buscarWeb()` en `motor_remi_api.js` (commit `dfe2d7a`) — **falta la API key real** (usuario debe sacarla del tier con $5 crédito gratis/mes de Brave y pasarla para agregar a `.env` como `BRAVE_API_KEY`). Con la migración a Mongo (#159) el caché ya no debería estar tan delgado, lo que en teoría baja la frecuencia de fallback web — medir tras la próxima corrida del validador. |
| 162 | ⏳ | **Tarea de Windows `ScraperMonitorLocal` apunta a la carpeta VIEJA del scraper** (`valuation-ai\scraper-inmuebles` en vez de `Pagina-Valuacion-con-Ai--main\scraper-inmuebles`), con credenciales de Mongo obsoletas — cada corrida (cada 5 min) falla con "bad auth" y omite todos sus diagnósticos. Por esto nunca detectó ni reinició la caída del scheduler de 22h del 12-ago. No arreglado, solo diagnosticado — el watchdog que se montó esa sesión (Bash + `Monitor` de Claude Code) es un parche temporal que muere con la sesión, no reemplaza la tarea de Windows real. |
| 160 | ✅ | **Pausa entre tareas del scheduler calibrada por portal (12-ago)** — `PAUSA_MIN/MAX` (12-22 min) era global desde que se introdujo, nunca calibrada per-portal pese a que el enricher SÍ tiene esa calibración desde 03-jun (`DELAYS_PORTAL`, verificado en vivo sin 403/429/DataDome). Agregado `PAUSA_PORTAL` en `scheduler.py`: PINCALI/CASAS_Y_TERRENOS/MITULA (sin antibot real) → 2-5 min; PROPIEDADES_COM (Akamai pero absorbido por Node) → 5-10 min; INMUEBLES24/VIVANUNCIOS (antibot real, Playwright/DataDome) se quedan en 12-22 min. 146 de las tareas de la corrida mensual caen en portales rápidos — recorta bastante el tiempo total sin tocar los portales que sí necesitan cautela. |
| 70 | ✅ | **Remi-Scraper como motor principal** — Homologación Directa $/m²C con comps del scraper. 6/9 casos en Guadalajara dentro de ±20% error. Archivos: `comparar_metodologias.js`, `validador_masivo.js` |
| 71 | ✅ | **Selección de comps corregida** — Sigue reglas de `sheets_comparables.py`: tipo coincidente, m²C exigido, área ±50%, CUS ±35%, top 10 por similitud. CONSOLIDADO tab como fuente única. |
| 72 | ✅ | **Anti-remate bidireccional** — Filtro ±40% de mediana (antes solo filtraba abajo). Descarta remates Y colonias caras que distorsionan. |
| 73 | ✅ | **Gemini como fallback** — `test_gemini_comps.js` implementado. Miguel Hidalgo: +19.7% con Gemini vs +47% con scraper. Las Conchas y Lagos de Oriente requieren más trabajo. |
| 79 | ✅ | **Motor calibrado en 40 OPIs — 31/40 ±20%, error promedio 12.6%** — Mejoras: factorRH dinámico por conservación, prima vivienda pequeña (<65m²C), Beta-OPI preferido cuando colonia es vaga, factor obsolescencia urbana terreno (edad>30 + CUS<0.85). DeepSeek como agente primario de análisis. Script `deepseek_dev.js` creado. |
| 80 | ✅ | **Comparar metodología perito vs motor propio vs precio real scraper** — `comparar_10_scraper.js`: 10/10 en ±20%, error 11.8%. Motor Remi gana al método perito en todos los casos. |
| 74 | ✅ | **Base de datos de colonias similares** — `build_colonias_similares.js` corrido exitosamente con 712 OPIs. 0 colonias nuevas (ya existían todas), 3 actualizadas. colonias_similares.json tiene 1052 entradas. |
| 75 | 🔄 | **Enricher propiedades.com — RESUELTO vía HTTP simple Node (01-Jun PM).** La "puerta": `GET` con fetch nativo de Node da 200 con `__NEXT_DATA__` (incl. `amenities.age`), SIN Akamai/Chrome/CDP. `scrapers/plain_fetch.js` + `enricher.py fetch_html_node()`. Verificado: 47 props/página, 6/6 detalles con antigüedad (2003-2025). Enricher corriendo a escala (`--max 1000`, log `logs/enricher_pcom_run.log`). **Pendiente:** correr más lotes para llenar cobertura, luego refrescar cache. (La lección vieja "propiedades.com necesita CDP" quedó OBSOLETA: requests-Python recibe tarpit TLS, Node no.) |
| 82 | ✅ | **Fix m2_terreno en scraper Mitula** — Corregido en `mitula.py`. |
| 83 | ✅ | **Scheduler mensual corregido** — `run_mensual.ps1` con ruta, Python correcto y PROPIEDADES_COM. |
| 84 | ✅ | **MongoDB-first en scheduler.py** — Escribe MongoDB primero, Sheets secundario. |
| 76 | ✅ | **Afinar Remi** — **100% ±20%** (38/38 OPIs calibrados), error promedio 9.8%. Set extendido 80 OPIs: 61.3% ±20%. Fixes: build_colonias_similares.js, 6 similares actualizadas. Fallos en set extendido: colonias vagas, IDX antiguo, ejidales. |
| 87 | ✅ | **optimizar_similares_ds.js** — Script que detecta fallos >20% y pide a DeepSeek las colonias similares óptimas del IDX. Flags: --apply para guardar cambios. |
| 88 | ✅ | **Ampliar validación y calibración** — **COMPLETADO 26-May**: 89/99 ±10% (89.9%), 100% ±20%, error abs 5.0%. Comando: `node validar_40_opis.js --n 200 --desde 2025-07`. FLOOR_EDAD_SIMILARES diferenciado por conservación implementado. |
| 89 | ✅ | **Validación ampliada todo 2025+2026** — 157 OPIs: 82.8% ±10%, 98.1% ±20%, error 5.9%. 2 nuevos EXCLUIR (Del Sur micro + Zapopan-municipio). Motor confirmado robusto. |
| 93 | ✅ | **SEPOMEX/IDX + SIM fixes post-rebuild 26-May** — +1,410 colonias SEPOMEX al IDX (8,703 total). SIM corregidas: san elias (similares premium→medio-alto), oblatos (garbage→analco/san juan bosco), zalatitan (sin datos→tonala/santa paula). Conservation mapping completo en server.py (11 estados vs 4 antes). Resultado: **85.4% ±10%, 99.0% ±20%, error 5.2%** (96 OPIs H2 2025+2026). |
| 90 | 🔄 | **Opción 2: edad relativa a la zona** — **Maquinaria LISTA y probada neutral (01-Jun):** `build_cache_index.js` calcula `edadMedianaZona` por colonia; `motor_remi_api.js` usa ancla relativa en `factorEdad` (fallback a 10 sin dato → idéntico). **DESBLOQUEADA** (ver #75/#91): propiedades.com ya da antigüedad. Se activa al refrescar cache con cobertura nueva. Falta: medir mejora en colonias de casas viejas. |
| 91 | 🔄 | **Capturar edad en scraper — RESUELTO PARA LOS 6 PORTALES (02-Jun PM).** **Enricher modo `--mongo`** (commit ebeb4bc): lee y escribe DIRECTO en `mercado_props.anio_construccion` (canónico sin ñ), sin tocar Sheets (Mongo es base oficial). Antes era Sheets-céntrico → la edad nunca llegaba a Mongo (solo 58/84,733). **Extracción arreglada por portal:** PROPIEDADES_COM (amenities.age + 'Nuevo'→año actual, commit 3581448); INMUEBLES24 + VIVANUNCIOS (edad en JSON Navent CFT5 `"label":"antigüedad"`, NO en texto; 'A estrenar'→nuevo, commits e758abb/de25d2a); PINCALI residencial (regex no abarcaba "construcción", e758abb); CASAS_Y_TERRENOS (features.age>0; age=0='sin dato', correcto); MITULA parcial. **Verificado en prod:** anio_construccion 58→212 y subiendo en vivo. Lección: NO concluir "portal no expone edad" desde script — el dato suele estar en JSON embebido (memoria feedback_extraccion_falso_negativo). **Enrichers corriendo (02-Jun ~22h):** PCOM 500, INM24/PINCALI/MITULA 500 c/u, VIVANUNCIOS 600 + CYT 1500. **Pendiente al terminar:** refrescar cache del motor (cache_index/colonias_maestro) desde Mongo + medir mejora en colonias de casas viejas (#90). |
| 92 | ❌ | **Replicar calibración en comparar_metodologias_v2.js** — DESCARTADO: producción ya usa `motor_remi_api.js` directamente desde server.py. comparar_metodologias_v2.js es script legacy de prueba. |
| 77 | ✅ | **Integrar Remi al backend** — `motor_remi_api.js` + endpoint en server.py. |
| 81 | ✅ | **Migrar a cache_index.json** — IDX[muni][tipo] 1.6MB vs 50MB anterior. |
| 78 | ✅ | **Re-extraer cerebro con colonias de comparables correctas** — Completado 22-may. 712 OPIs recuperados. build_colonias_similares.js corrido. |
| 85 | ✅ | **actualizar_cerebro.js optimizado** — Ya implementado (item obsoleto): `ultimosMeses(N)` default 3, flag `--meses N`, merge de manifiesto sin reemplazar (`[...anterior, ...nuevos]`). Verificado 03-Jun. |
| 86 | 🔄 | **avaluos_referencia.json** — Archivo existe con 1,168 OPIs. Pendiente: integrar en motor_remi_api.js para enriquecer sujetoColonia cuando el extractor no la encuentre. |
| 94 | ✅ | **Consolidación en archivo maestro** — `construir_maestro.js`: 6 fuentes por-colonia → `colonias_maestro.json` (1 archivo, ~33% menos bytes/avalúo). Motor lee solo de ahí, cascada idéntica + fallback legacy. `ARQUITECTURA_DATOS.md` documenta capas. |
| 95 | ✅ | **Capas ganada/derivada + temporalidad + flywheel del perito** — cascada NSE v1→perito→v2→idx. `construir_calibraciones_perito.js` deriva NSE verificado de avalúos reales (split temporal para medir). Neutral pero +115 colonias de cobertura, cero regresión. |
| 96 | ✅ | **Acumulación de comps web + score de confianza** — `acumularComps()` guarda comps web reales (con URL) en `comps_acumulados.ndjson`. `consolidar_comps_acumulados.js` dedup + clasifica verificado/por_verificar/sospechoso cruzando contra mediana de zona. |
| 97 | ✅ | **Cap de comps por calidad de pool** — COMP_CAP 15 (exacta/similares) / 10 (general). +1 ±10%, cero regresión. |
| 98 | ✅ | **Fix municipio mal capturado** — `corregir_municipios_cerebro.js` (guardrail pm2c-coincide-otro-muni). Real del Valle Zapopan→Tlajomulco: −27%→−6.6%. Detector reutilizable. |
| 99 | ✅ | **Fix gate suma_partes (clave exacta→fuzzy)** — usaba IDX[col].count exacto, subcontaba comps fuzzy. Cortijo San Agustín usó sus 6 comps reales (+21%→−10%). |
| 100 | ✅ | **Benchmark honesto 2025-2026 + descartar 2023-2024** — validar contra avalúos viejos = adivinanza (plusvalía). Factor inflación 2024 corregido 1.07→1.13. Colli/Alta California reclasificadas MERCADO (respaldadas por 9/42 listings). |
| 101 | 🔄 | **Puente NSE de comps acumulados** — clasificar cada comp `verificado` por NSE (pm2c→nivel) y fusionar a cache_consolidado/cache_index para que el motor los use. Aditivo, medible. **Avance 03-Jun (flywheel comps web):** el motor (`motor_remi_api.js` `enriquecerCompsWeb`) ahora abre cada comp REAL de Serper vía `enrich_urls.py` (Node→Python), saca edad/m²/recámaras/baños/tel y los hace upsert a `mercado_props` con `generar_id_unico` (sin duplicar) + `$setOnInsert` (no pisa scrape). Así lo que se busca en web queda guardado con calidad de scrape → próxima vez sale del scrape. Validado el puente (an=2001) y sin regresión del motor (Providencia $7.43M). OJO: la ruta AI de `server.py` (OpenAI/Gemini) genera URLs alucinadas → su enriquecimiento inline quedó apagado (flag ENRICH_WEB_COMPS_INLINE); el real vive en la ruta Serper del motor. **Pendiente:** (a) ajuste per-comp por edad dentro de `remiSobreComps` (hoy usa edad del sujeto, no `c.an`) — cambio de fórmula, requiere validador_masivo. **BLOQUEADO POR DATOS (03-Jun):** el caché del motor tiene **0% de comps con `an`** (cache_consolidado/cache_index) → per-comp por edad es no-op hoy; el `an` vive en Mongo (enrichers subiéndolo) pero meterlo al caché = rebuild Mongo / migración #107 que regresa. Deferido hasta que `an` llegue al caché. Ver MOTOR_ANTECEDENTES (#101a). (b) test en vivo del fallback Serper — independiente; bajo ROI como caso aislado (el puente ya se validó: an=2001, Providencia $7.43M). |
| 102 | ✅ | **Fórmula suma_partes sobrevalúa (03-Jun)** — Era `suma_partes_mix` (60% sumaDePartes + 40% pool) inflando lotes terreno-pesados. Fix: cota asimétrica `mix ≤ pool×1.25` (recorta solo sobrevalúo, no toca subvalúo). Offline: ±20% 86.7→88.7%, err 10.9→9.7%; set curado ±15% 86.1→88.9%, ±20% 100% sin cambio. Emiliano Zapata +82→+16.5 ✅, La Experiencia +36.5→+19.4 ✅. Sin regresión. Ver MOTOR_ANTECEDENTES. |
| 103 | ❌ | **Bucket colonia vacía — PROBADO Y REVERTIDO 02-Jun.** Guard de longitud en el ancla de banda regresó el benchmark 71.6/83.9/92.9 → 69.7/81.3/91.0 (+3 violadores). Los listings sin colonia aportan ancla de zona útil. NO reintentar. Ver MOTOR_ANTECEDENTES.md. |
| 104 | ❌ | **Similares premium/baratos — LÍMITE ESTRUCTURAL, NO reintentar (03-Jun)** — Heliodoro -52% (SIM baratas) y San Carlos +30.5% (SIM premium) tiran en direcciones OPUESTAS, ambos pool:similares n=3. No hay fix de fórmula sin overfit; curar SIM a mano = cazar atípicos prohibido (regla #4). Mejora real = datos scrapeados propios de esas colonias. Ver MOTOR_ANTECEDENTES. |
| 105 | ✅ | **Aislar Chrome scraper propiedades.com (03-Jun)** — Resuelto de fondo: el path vivo (scraper `_node_get` + enricher `fetch_html_node`) usa `plain_fetch.js` (fetch nativo de Node, sin Chrome) → ya NO abre el navegador personal ni marca tu sesión a Akamai. Alineé los 2 defaults CDP legacy 9222→9333 (`enricher.fetch_html_cdp` muerto + `propiedades_com.CDP_PORT`); `config.PROPIEDADES_CDP_PORT`=9333 + `lanzar_chrome_scraper.bat` ya existían. |
| 106 | ✅ | **Esquema de campos UNIFICADO (03/04-Jun)** — Mismo dato = mismo nombre legible en todas las capas. Claves de caché mín→legible (`p→precio, c→m2c, t→m2t, tp→tipo, co→colonia, mu→muni, an→anio, fs→fecha`…) en `cache_consolidado.json`, `cache_index.json`, `edad_mongo.json`, `comps_verificados.json`, `motor_remi_api.js`, `build_cache_index.js`, los 3 `actualizar_cache_*`, `construir_puente_comps.js`. Renombre PURO: validador IDÉNTICO antes/después (±10% 69.2 / ±15% 76.6 / ±20% 88.8). Diccionario único `ESQUEMA_CAMPOS.md` (raíz) + chequeo `verificar_esquema.cjs`. Memoria `reference_esquema_campos.md`. **Edad: campo canónico `anio_construccion` SIN ñ; el de ñ está 99.96% vacío (PROHIBIDO).** |
| 107 | ✅ | **Dedup intra-portal (04-Jun)** — `fusionar_duplicados.py` ahora marca también la MISMA propiedad re-scrapeada en el mismo portal (firma estricta: portal+colonia+tipo+m²+precio EXACTO+rec+baños). No destructivo (`dup_intra=True`, reversible `--reset`). Resultado: secundarias 963→**5,522 (6.8%)**, inventario real 80,833→**76,263**. Cross 698 grupos + intra 3,334. **Pendiente:** re-correr cuando enrichers llenen el 19.8% no evaluable (sin colonia/m²) → caerán más; y caso "re-listada a otro precio" (huella por dirección/agente). |
| 108 | 🔄 | **Drenar enrichers a toda la base (04-Jun, CORRIENDO)** — Causa del "ya terminó" falso: los lotes corrían con `--max` chico (15% de la base). Rendimiento de año verificado ALTO en los enriquecidos: INMUEBLES24 98%, PROPIEDADES_COM 93%, VIVANUNCIOS 93%. Relanzados los 5 con `--max` grande para drenar (~70k pendientes venta+renta, sin duplicados). Fix: la consulta del enricher ahora excluye `es_duplicado_secundario` (no re-procesa duplicados). **Proyección:** ~8k→~43k con año al terminar → activa #90/#91. Procesos `nohup` siguen tras /clear. Al terminar: refrescar caché + re-correr dedup + medir #90. |
| 109 | ✅ | **Edad pendiente NO es bug del extractor (04-Jun)** — Verificado bajando HTML crudo: el residuo ~67k es estructural (PINCALI/comercial sin año, CYT `age=0`, MITULA `/adform` sin datos, I24 `antigüedad=None`; lo que parecía falso-neg era CSS). El extractor capta el año cuando existe. Único fix: `En construcción`/`Preventa` → año actual en I24/VIVA (commit `ff959ac`). Backfill productivo restante = PCOM colonia (~11k). Memoria `feedback_edad_pendientes_no_son_bug`. |
| 110 | ✅ | **Límites de creatividades de ads (04-Jun, `9ed6374`)** — Tamaño por slot (video 25/12/6 MB, imagen 10 MB) + tope por anunciante (5 videos, 25 imágenes) validado en BACKEND (`routers/ads.py`, no bypasseable) y front. Leyendas de tamaño/duración y uso de almacenamiento en la consola. SLOT_SPECS maxSizeMB 50/20/10→25/12/6 + maxDuration 60/30/15. |
| 111 | ✅ | **Fix consola anunciante adDuration (04-Jun, `3fd5e10`)** — `spec.adDuration` (inexistente, NaN) → `adDuration`. Pausar/reactivar + badges aprobación ya estaban OK (verificado e2e). Datos de prueba en staging: advertiser `test-ads@propvalu.test` + 2 campañas slot1/2 + 2 MP4 (borrables). |
| 112 | ✅ | **Cobertura similares: 231 cotos sin SEPOMEX resueltos + 2 alucinaciones corregidas (04-Jun, `6d179b7`/`70ed7e5`)** — `resolver_similares_municipios.js` (Gemini, rate-limit) resolvió municipio/zona de 231/336 colonias nuevas (resto bloqueado por cuota Gemini, checkpoint guardado). Corregidas 2 alucinaciones del LLM (`colotlan`,`huejuquilla` mal mapeadas a AMG→Otro). Validador IDÉNTICO (sin regresión); valor = cobertura futura. |
| 113 | ❌ | **Cross-municipio por adyacencia de MUNICIPIO — PROBADO Y REVERTIDO (04-Jun, `ec36a11`)** — `MUNI_VECINOS` solo en `enSim` regresó el validador (±15 76.6→73.8, ±20 88.8→86.9, fuera 12→14): la adyacencia a nivel municipio es muy gruesa. NO reintentar así. Camino correcto = proximidad por CP (ver #114). Ver MOTOR_ANTECEDENTES. |
| 114 | ✅ | **Herramienta de proximidad por CP/coords (04-Jun, `c272359`, Sonnet)** — Infraestructura lista en `Modulo Drive IA/_geo/`: `cp_coords.json` (2,025 CPs Jalisco con lat/lon, GeoNames), `colonia_cp.json` (6,178 colonias, 97.3% match, SEPOMEX), `proximidad.py` (haversine + `colonias_cercanas`). Self-test OK (cruza Tlaquepaque↔GDL). **COMPLETADO:** Integrado al motor (`motor_remi_api.js`) y al build (`construir_maestro.js`). Validación: baseline subió a 89.7% en ±20%. |

| 115 | ✅ | **Fix obra nueva genérico todos los portales (05-Jun, `42cf3e5`)** — El fix anterior solo cubría I24/VIVA. Agregado fallback: texto libre "obra nueva/en construcción/preventa/a estrenar" → año actual en PINCALI, PCOM, MITULA, CYT. |
| 116 | 🔄 | **Enrichers relanzados con fixes (05-Jun)** — Relanzados con `python -u` (fix de buffering). Fix 09-Jun: scheduler.py línea 680 le faltaba `--mongo` → enriquía en modo Sheets y tiraba 429. Corregido. Enrichers relanzados con `--mongo`: PINCALI 12k · INMUEBLES24 8.4k · PROPIEDADES_COM 16.8k pendientes CORRIENDO. Al terminar: refrescar caché + re-dedup + medir #90/#91. |
| 117 | ✅ | **Scraper NOCNOK completo (09-Jun)** — API JSON pura `/api/properties/search` con `stateId=14` + `countyIds` por municipio. 7 municipios GDL metro mapeados (IDs verificados). Split por precio para municipios >500 props (Zapopan/GDL). Detalle via `_next/data/{buildId}` para `yearBuilt`, `county`, `settlement`. Sin enricher separado — todo en un paso. Comando: `python scheduler.py --portal NOCNOK`. Corriendo en background (80+ props en Mongo al iniciar, ~2k total esperado). |
| 118 | ⏳ | **Scraper Monopolio** — Explorado 09-Jun: API usa AWS Cognito con `explicit deny` para peticiones directas. Solo funciona desde browser autenticado. Para scraperlo se necesita login + JWT; fuera del alcance actual. Diferir. |
| 119 | 🔄 | **Limpiar colonias basura IDX (29-Jun)** — Diagnóstico: 60-70% colonias únicas en GDL/Zapopan/Tlajomulco/Tlaquepaque son strings con dirección completa. Fuente: PINCALI 71.6%, PCOM 29.5%, VIVANUNCIOS 12%. Fix aplicado: (a) `enricher.py` re-procesa PINCALI/PCOM/VIVANUNCIOS con colonia basura; (b) `cleaner.py` rescata colonia via patrón "Col. NOMBRE" + catálogo SEPOMEX 2,827 colonias; (c) `_reset_enrich_portal.py` para resetear cooldown. Enrichers CORRIENDO (PINCALI 34k + PCOM 13k). Al terminar: re-correr `fusionar_duplicados.py` + rebuild `cache_index.json` + re-validar motor. |
| 120 | ✅ | **Gate lote grande (CUS<0.50) homologación CUS — IMPLEMENTADO+VALIDADO+MERGEADO (30-jun)** — En motor_remi_api.js: `valuarLoteGrandeCUS` (homologación CUS + semilla pm2T + tope NSE muni-correcto). + anti-colisión NSE por municipio + fix normMuni "tlaquepaquetlaquepaque" + filtro de segmento de precio. **Validado en 103 OPIs base fresca: ±10% 40.8→47.6%, error 19.9→15.7%, sin regresión.** OPI-26-5-16 −52%→−1.3%. Re-extracción Drive: colonias comps 4%→88%. Commiteado+pusheado (main). Detalle: memoria `project_motor_homologacion_cus`. |
| 121 | ✅ | **Selección/ponderación de comps por similitud (DV) — IMPLEMENTADO+VALIDADO+MERGEADO (30-jun, `e5cb542`)** — Papers leídos (Krause&Kummerow completo + Copiello + SIEV + arxiv). Adaptación a datos MX: ponderación INVERSA a distancia estandarizada sobre {m²C,m²T,rec,baños,año}, top-8 más parecidos, var faltante no penaliza; conserva ajustes tamaño/edad/conserv. SIN el OLS por avalúo (muestra chica). **Geo DESCARTADA** (redundante con pre-filtro colonia/similares). **Estac**: sin efecto medible hoy (cobertura comps rota por bug PINCALI — ARREGLADO `32f30d2`, backfill ~15.7k corriendo). build_cache_index ahora arrastra rec/baños/estac. **Validado 103 OPIs 2025-26: ±15 57.3→60.2, ±20 68.9→73.8, errAbs 15.7→15.6, 0 regresiones** (rescata errores grandes, no afina ±10). Backup motor_remi_api.PRE_DV.bak. Detalle: `reference_avm_metodologia`. PENDIENTE: re-medir estac tras backfill (deptos) + variable NIVELES (OPI AF85:AM86, ~10% precio en deptos). |
| 122 | ✅ | **Piso espejo del techo `poolTipo=exacta` — GRADUADO (23-jul noche #5, `1a87870`)** — Mismo 5%/gate n≥10 que el techo ya existente, ahora también hacia abajo del blend. Aislado de 2 candidatos negativos (piso-NSE, bono-edad — no graduados, retirados del código). +1.0/+2.9/+1.0pp en ±10/15/20 sobre 207 OPIs, reproducido en corte 2024-2026. |
| 123 | ✅ | **Bug de captura m²c PINCALI limpiado a escala (23-jul noche #5, `e36b2a6`)** — 744 registros casa/depto en TODO el caché con m²c roto (mayoría PINCALI preventa: 5-55m² a precios de millones, $/m² hasta $60M). Limpiados en 309 celdas vía parche directo a `cache_index.json` (NO rebuild completo — ver #124). Mejora en las 4 métricas del validador, cero regresión. **PENDIENTE: arreglar el bug de EXTRACCIÓN en la fuente** (enricher/scraper PINCALI) — seguirá generando basura nueva mientras no se corrija ahí. |
| 124 | ❌ | **Split NSE nuevo/usado en motor JS — 8 variantes probadas, TODAS descartadas (23-jul noche #4/#5)** — Cap-mirror (forzado, subject-aware, n≥8, corte 2/5/6 años) Y selección-de-comps (franja binaria, corte 2/3/4/5/8 años): ninguna mejora el validador. Causa: casi ninguna colonia tiene ≥5 comps con edad conocida en la misma franja que el sujeto — no hay volumen con qué segmentar, no importa el mecanismo ni el corte. **NO reintentar sin antes subir cobertura de edad por colonia** (scraper dirigido a colonias débiles). Ver memoria `project_propvalu_nse_nuevo_usado`. |
| — | 📌 | **REGLA NUEVA (23-jul noche #5): nunca `build_cache_index.js` completo para un fix puntual.** El `cache_index.json` commiteado ya estaba desactualizado respecto al `cache_consolidado.json` commiteado — cualquier rebuild completo arrastra TODA la deriva acumulada y regresa el validador, aunque no cambies datos (confirmado: rebuild sin ningún edit dio la misma "regresión" que con el fix). Fix correcto: parchar la celda específica directo en `cache_index.json`, sin correr el builder completo. |

---

## Estrategia de Negocio

| # | Estado | Tarea |
|---|---|---|
| N1 | ✅ | **Plan de Negocio 2026** — `memory/PLAN_NEGOCIO.md` creado. Pivot SaaS inmobiliarias, cascada descuentos, DocuProp, portal. |
| N2 | ✅ | **Análisis de negocio completo** — `memory/ANALISIS_NEGOCIO_COMPLETO.md`: TAM/SAM/SOM Jalisco, P&L 3 años, unit economics, GTM, legal, equipo. |
| N8 | ✅ | **Análisis competencia avalúos** — Tabla comparativa YALS/Monopolio/Romina/AvaClick con precios reales verificados. |
| N11 | ✅ | **Análisis Nexti** — CRM inmobiliario con módulo de avalúos. $499/mes ilimitados. Homologación 0.80-1.20, PDF con UID, cita NMX-R-081 implícita. Para agentes: certificaciones CONOCER (EC0110.02/EC0903.01), curso gratis, respaldo de perito, entrega automática por correo. Diferenciador PropValu: comparables automáticos del mercado real vs captura manual; rigor técnico vs ecosistema generalista. |
| N12 | ✅ | **NMX-R-081-SCFI-2015 revisada** — Motor PropValu alineado con principios: enfoque mercado (Romina ≥3 comps) + costo (Suma de Partes INDAABIN). Frase segura: "metodología alineada con los principios de la NMX-R-081-SCFI-2015". |
| N3 | ⏳ | **Constituir SAPI de CV** — Prerequisito para cobrar con Stripe. Notario + RFC + cuenta bancaria empresarial. |
| N4 | ⏳ | **Integrar Stripe** — Cobro real de suscripciones. Prerequisito: SAPI constituida. |
| N5 | ⏳ | **10 pilotos gratuitos** — Validar product-market fit antes de gastar en marketing. |
| N6 | ⏳ | **Contactar AMPI Guadalajara** — Solicitar presentación en asamblea mensual. Canal principal GTM. |
| N7 | ⏳ | **Firmar acuerdos con abogados** — Red DocuProp: 3-5 abogados socios con split 75/25. |
| N9 | ✅ | **Scraper NOCNOK** — `inmuebles.nocnok.com` — API JSON pura, sin Playwright. Ver #117. |
| N10 | ⏳ | **Scraper Monopolio** — `monopolio.com.mx` — AWS Cognito explicit deny. Requiere login+JWT. Ver #118. |
| #20 | ✅ | **IPRoyal DESCARTADO 100%** (causa conflicto con páginas + 402). Comentado en `.env`; scraper corre proxy-free. Regla en `scraper-inmuebles/INDICE_SCRAPER.md`. NO reactivar. (06-Jul) |
| #21 | 🔄 | **Backfill año PINCALI** — DESTRABADO: corre proxy-free (no requiere IPRoyal), corriendo en terminal del usuario. Fix año MEJORADO 07-jul (`57442ae`): `/inmueble/` da 422 → ahora saca "Year Built" del `/en/` ya descargado, resiliente. (07-Jul) |
| #22 | 🔄 | **Motor: selección de comps por SEGMENTO** — GRAN AVANCE: ancla segmentada tipo+edad portada a prod (`b5430fc`), MITULA fuera del caché (`b044f0a`), consolidación 537 colonias (`774b016`). **±20 73.8→79.6 / ±15 →70.9.** EN CURSO (agente): ancla segmentada MATERIALIZADA por colonia×tipo×segmento viejo/nuevo (para NSE-mixto tipo Seattle). (07-Jul) |
| #23 | ✅ | **Seguridad: de-hardcodear 9 archivos** — HECHO (`ab7ad50`), leen MONGO_URL de .env. 0 literales en el repo. Incidente cerrado. (07-Jul) |
| #24 | ⚠️ | **Ancla segmentada NSE-mixto — PROBADO, CERO recuperación (07-jul).** Infra construida (`build_seg_anclas.py`, `cache_seg_anclas.json`, flag `LAB_SEG_CLUSTER`, 75 colonias, en lab documentado). Los 5 OPIs target NO mejoraron: fallan por ajustes **POST-selección**, no por selección de comps. **Seattle: cuello real = `factorConserv="malo"=0.55`** (−45% a comps). 2ª vez que fórmula/ancla sobre estos 5 sale en cero (antes LAB_EDAD_NSE) → NO re-perseguir (regla no-cazar-atípicos). Si acaso, revisar `factorConserv` malo=0.55 como caso general. (07-Jul) |
| #25 | 🔄 | **`catalogo_cotos.json` — CONSTRUIDO 10-jul** (`Modulo Drive IA/catalogo_cotos.json`, 1,762 zonas). **2 niveles:** zona (`colonia_oficial` canonicalizada vía SEPOMEX + fix mojibake + truncación; `tipo_sepomex`, `es_coto_privado`, `variantes[]`, `pm2_mediana`) y conjunto (coto/edificio interior extraído de títulos, **canonicalizado con DeepSeek**: fusiona Zanthe/Zanthé, descarta ruido "En/De/Con Vigilancia"; 68 zonas, 174 cotos internos). Registrado en INDICE_MOTOR. Hallazgo clave: el coto interior vive en el `titulo`, no en `colonia`; variación de precio entre cotos de un mismo fracc ~1.2x (chica). **PENDIENTE: wirear al motor** (validar/normalizar colonias, selección de comps por zona/conjunto) — data lista, aún no conectada. Discriminador coto/oficial = match SEPOMEX (determinista, ~85%). **TEST DE WIRING (10-jul):** probé normalizar colonias del motor con el catálogo → validador offline **NEUTRAL** (±15/±20 idénticos 70.9/79.6, ±10 −1 OPI, errAbs +0.1). **NO se wirea** — las fusiones seguras (154) no aportan; el mapa completo (con 92 fusiones malas por sufijo, ej. tesistan+san francisco tesistan) habría regresado. Catálogo queda como REFERENCIA, no como entrada del motor. `variantes` regeneradas con regla estricta (sin sufijo). Palanca real del motor sigue siendo DATOS/segmentación. (07/10-Jul) |
| #26 | ⏳ | **Búsqueda dirigida en segmentos vacíos** (casas viejas/poca cobertura). **MÉTODO decidido (10-jul): browser Google GRATIS, NO Serper/Tavily** (ahorra créditos; el usuario lo pidió así). Comps encontrados → escribir a `mercado_props` (Mongo Atlas, independiente de Railway). **Targets ya identificados (offline) — arrancar por estos OPIs fuera ±20 con pocos comps:** OPI-26-5-09-AV (El Vergel Tlaquepaque, casa 107m² 21a), OPI-26-4-15-AV (Educadores Jaliscienses Tonalá, 251m² 30a), OPI-25-9-15-OF (Monumental GDL, 74m² 33a), OPI-25-6-17-LM (Loma Bonita Ejidal Zapopan, 112m² 51a), OPI-25-5-14-OF (Arcos de Zapopan, 161m² 21a). Diferido a próxima sesión (poco crédito). **OJO payoff incierto** (consolidación previa casi no movió ±20). (07/10-Jul) |
| #27 | ✅ | **REDEPLOY Railway — HECHO 10-jul.** `railway up` desplegó el motor mejorado a prod (deployment `765fbc38`, era `9372a439`); healthcheck 200. **TAVILY_API_KEY corregida**: Railway tenía la key con un typo en el medio (mismo prefijo/sufijo `tvly-dev-3vs7…6cL`, distinto medio) → sincronizada con la canónica del registro (validada 200 contra API Tavily). 41 commits pusheados a origin. |
| #28 | ✅ | **Scraper mensual automatizado** — tarea Windows `PropValu_ScraperMensual` (diaria, lanzador Python decide 1 corrida/mes random día 2-10). MITULA quitado del scraper (`796e23c`). (07-Jul) |
| #29 | ⏳ | **🔴 PROD CAÍDA — trial Railway venció 10-jul** (30 días), app `propvalu-backend` suspendida (404). Deploy de índices #65 (commit `208970a`) NO subió por esto. **Opciones:** (a) Railway Hobby $5/mes (incluye $5 crédito, alcanza: 1 servicio FastAPI, Mongo está en Atlas aparte); (b) **migrar gratis a Render.** **VERIFICADO en dashboard 10-jul:** existe servicio `valuation-ai-1` (`srv-d6eorg15pdvs73fvuse0`, URL `https://valuation-ai-1.onrender.com`, Free, Docker) pero es el **backend VIEJO de Node.js** (rama `master`, env vars: GEMINI_API_KEY viejo + GOOGLE_MAPS_API_KEY + NODE_ENV + OPENAI_API_KEY; SIN MONGO_URL). Reusarlo = reconfigurar casi de cero: (1) cambiar rama `master`→`main`; (2) borrar NODE_ENV, refrescar GEMINI, y AGREGAR las que faltan: `MONGO_URL`,`DB_NAME`,`ADMIN_SECRET`,`ADMIN_EMAIL`,`JWT_SECRET`,`JOBS_SECRET`,`TAVILY_API_KEY`; (3) IP Render en Atlas allowlist; (4) redeploy con Dockerfile FastAPI actual. Opcionales off: SENTRY/SHEETS/SMTP/SCRAPER_*/ENABLE_SCHEDULER. Frontend Vercel: `REACT_APP_BACKEND_URL`→URL Render + redeploy. **Alternativa: Railway Hobby $5 = 0 reconfiguración (ya está todo).** **PROGRESO 10-jul (reconfig Render en curso, usuario quiere respaldo gratis):** ✅ rama cambiada `master`→`main` (auto-disparó build de `8ba759f`, falla health hasta tener env vars=esperado). PENDIENTE usuario: env vars (borrar NODE_ENV, refrescar GEMINI, agregar MONGO_URL[→prod cluster0]/DB_NAME/ADMIN_SECRET/ADMIN_EMAIL/JWT_SECRET/JOBS_SECRET/TAVILY_API_KEY) — Claude NO puede teclear secretos. DECISIÓN pendiente: Atlas prod cluster0 necesita `0.0.0.0/0` (Render free sin IP fija) O apuntar Render a staging cluster1. Luego: redeploy + Vercel `REACT_APP_BACKEND_URL`→`https://valuation-ai-1.onrender.com`. (10-Jul) |

| #163 | ✅ | **Federación de datos PropValu/Manual/atlas-colonias — Fases 0-6 construidas y desplegadas (24-ago).** Plan completo en `Manual-Arquitectura-ZMG/PLAN_FEDERACION_ECOSISTEMA.md`. PropValu: `routers/atlas_colonias.py` (jala feed público de atlas-colonias, espejo en `colonia_classifications_atlas`/`classifier_profiles_atlas`), `core/auth.py::require_admin_or_credentialed_contributor` (colaborador acreditado del Atlas puede proponer acabados sin ser admin), página pública `/colaborar/acabados`. Hallazgo de seguridad alto cerrado en la misma sesión: `/auth/register` no verificaba correo — ver `User.email_verified` + `/auth/verify-email`. `ATLAS_COLONIAS_FEED_URL` ya en Railway apuntando al deploy independiente de Cloudflare (`atlas-colonias-zmg.pedrucus.workers.dev`). Sin datos reales todavía (base nueva vacía) — ver ESTADO.md punto 1. |

---

## Leyenda
- ⏳ Pendiente
- 🔄 En progreso
- ✅ Completado
