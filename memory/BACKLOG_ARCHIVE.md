# PropValu — Archivo de Bitácora (changelog histórico del BACKLOG)

> Narrativas de sesión movidas aquí para no inflar el arranque. NO se lee al iniciar sesión.
> El estado vigente vive en `BACKLOG.md` (tablas) y `ESTADO.md` (snapshot). Consultar aquí solo si se necesita el detalle histórico de una sesión.

---

## 3 Ago 2026 — Homónimas cerradas y manual de arquitectura

Sesión de datos e investigación, sin tocar features. Empezó con 35 pares homónimos sin
fechar y 24 con confianza baja; terminó con **107 pares fechados, 0 pendientes y ninguno
bajo 60 puntos**.

**Lo que resolvió el problema no fue el OCR.** Los PDF del Periódico Oficial llevaban varias
sesiones sin rendir. La vía que sí funcionó: el usuario investiga cada colonia —plan parcial
por distrito, detonante urbano y sobre todo el **porcentaje de edificación por corte censal
en la ortofoto**— y Claude integra con `integrar.py`, contrasta contra la cobertura INEGI y
marca lo que no cuadra. El dato pedido en dos líneas por colonia; el formato largo con
fuentes y tipología no cambiaba ninguna fecha.

**Dos criterios quedaron fijados.** Primero: **la década es la de la edificación, no la del
trazo**. Colinas de San Javier se traza en 1968 y es de los setenta; Puerta de Hierro se
urbaniza en 1987 y es de los noventa. Segundo: `cov2000 = 1.00` en el cruce con INEGI no
prueba que la colonia estuviera construida —el polígono cae dentro de la mancha
metropolitana, que ya cubría los baldíos—, y ni siquiera los ceros valen en municipios
periféricos, donde el censo no traza mancha urbana aunque haya casas. Eso lo demostró
`puerta del sol|ixtlahuacan`: cero en 2010 y 50-60% edificada en la ortofoto.

**Cerrar por cobertura fechaba sistemáticamente tarde.** De 24 pares revisados con ese
método, 19 se corrigieron hacia atrás; `el zalate|tlaquepaque` estaba cuatro décadas fuera.

**Corrección de datos:** `colomos providencia` pasa de 1910s a 1960s. El 1910s venía del
normalizador heredado, que confundió los manantiales de Los Colomos, de principios de siglo,
con la fundación de la colonia. La regla de normalización sigue vigente —se restaura a
Colomos Providencia y no se colapsa con Providencia—; lo que era falso es la década.

**Incidente de concurrencia.** Otra sesión sobrescribió `colonias_decada.json` con una versión
anterior y borró los 106 pares de golpe. Se recuperó del respaldo previo sin pérdida; la
versión que lo pisó quedó como `.SOBREESCRITO-2335`. Regla nueva: contar los pares antes y
después de cada escritura. También se descubrió que el contador de `integrar.py` engaña
—imprime `pares_antes + len(aplicados)` y reaplica todo en cada corrida, así que decía 209
cuando en el archivo hay 107—.

**Manual de Arquitectura ZMG: de 168 marcas a 82.** Se cerraron las 13 tablas paramétricas de
m² con datos periciales del usuario (destapando que el interés social de 2010-2019 estaba en
45-75 m² cuando son 65-95, y que el townhouse de 2020 tenía invertidos terreno y
construcción); las 13 décadas quedaron con sus colonias clasificadas por segmento; y el
catálogo de materiales se extendió con **34 fechadores de 1995 a 2026**, con dos hallazgos de
campo: el polo a tierra es obligatorio desde la NOM-001-SEDE-1999 —ancla dura equivalente al
tinaco de polietileno de 1989— y el criterio de que "las instalaciones no se remodelan" falla
entre 1995 y 2026, con tres puntos de inspección para salvarlo. Apareció además una categoría
nueva, **pueblos absorbidos**: 25 de los 101 registros de los setenta son núcleos históricos
que el municipio inscribió cuando los alcanzó la ciudad, no fraccionamientos de la década.


---

## 2 Ago 2026 — Colonias limpias en origen (Tarea 6 cerrada)

Sesión de datos, sin tocar features ni UI. Objetivo: la **década de urbanización por colonia**, que alimenta el identificador de edad.

**El defecto de raíz eran cuatro normalizadores divergiendo.** `_norm_col_key` vivía copiado en `backend/routers/edades.py`, en el auditor del manual, con una variante propia (`normCol`) en el motor JS, y Codex validaba contra la suya. Ninguno entendía las truncaciones del scraper, así que `colonias_decada.json` tenía 228 llaves que existían pero el motor no encontraba, y 113 décadas contradictorias. Se movió todo a `backend/core/colonias.py` como fuente única; `edades.py` solo re-exporta y el auditor ahora importa en vez de copiar.

**Corrección de datos que estaba activa en producción.** Al revisar la lista completa de fusiones antes de aplicar, se vio que el normalizador **borraba** la primera palabra truncada cuando a veces esa palabra era parte del nombre: `omos providencia` no es una variante de Providencia (1960s), es **Colomos Providencia** (1910s, plan parcial), otra colonia; `inas de atemajac` es **Colinas** de Atemajac y quitarle el prefijo dejaba `de atemajac`, que no es nombre de nada. Ahora se restauran (`omos X`→`colomos X`, `inas X`→`colinas X`) y solo se quita la palabra cuando era genérica (`colonia`, `fraccionamiento`). Verificado contra el catálogo: las 21 restauraciones apuntan a colonias reales (Colomos Patria, Colomos Independencia, Colinas del Rey).

**Segunda corrección de criterio:** `coto`, `condominio` y `privada` dejaron de tratarse como decoradores. `coto del fresno` es 2010s y `del fresno` es 1960s — un coto nuevo dentro de una colonia vieja tiene su propia edad, que es justo lo que el manual quiere fechar.

**`colonias_decada.json`: 5,018 → 4,749.** 192 grupos consolidados, 0 contradicciones de década, 0 llaves inalcanzables. En ninguna fusión ganó la estimación sobre la investigación (0 casos), y los empates reales se dejaron sin fusionar en vez de inventar un ganador.

**El municipio entró al dato.** Antes los homónimos se distinguían por accidente: una de las dos llaves había quedado deformada por el scraper. Ahora cada entrada lleva `municipio` + `municipio_fuente` (cascada maestro → SEPOMEX → cache_index, 2,484 de 4,749) y las homónimas reales se llavean `nombre|municipio`. Al medirlo apareció que **no eran 2 homónimas sino 209**: Altamira existe en Tonalá, Zapopan, Degollado y Lagos de Moreno con una sola década. Se filtraron las que solo eran ambiguas fuera de la ZMG (113) y quedaron **102 marcadas con `homonima_en`**, que `decada_de()` devuelve con `_ambiguo=true` en vez de aplicar una década a ciegas.

**Acotado a ZMG + Ribera de Chapala:** 59 colonias eliminadas (Puerto Vallarta 17, Bahía de Banderas 7, Lagos de Moreno 4, Mazamitla, Tapalpa, Manzanillo…). El borrado exigió tres candados porque la versión ingenua destruía datos buenos: (1) el padrón debe incluir las variantes con que aparecen escritos los municipios (`tlajomulco` a secas, `ajijic` usado como municipio) o se borraban 130 colonias buenas, 126 de Tlajomulco; (2) el campo `municipio` del maestro trae basura (`valle dorado inn`, `. tlaquepaque`, `col miramar zapopan`), así que se valida contra el padrón real de SEPOMEX y lo que no es municipio cuenta como *desconocido*, no como *fuera*; (3) se elimina solo si NINGUNA fuente la ubica en la ZMG — eso salvó `las juntas` (figuraba en PV pero también en Tlaquepaque), `virreyes` y `loma bonita`. Único caso dudoso verificado en el catálogo: "San Martín de las Flores" a secas sí es de Tapalpa; las de Tlaquepaque son "de Abajo" y "de Arriba" y se conservan.

**`cache_index.json` 5,019 → 2,809 celdas y `colonias_maestro.json` 4,988 → 4,781.** No se escribió un limpiador nuevo: ya existía `consolidar_colonias_idx.py`, que fusiona bien (junta los anuncios y **recalcula** medianas). Le faltaban los prefijos truncados y una segunda pasada para los 118 fragmentos que solo viven en el maestro y nunca tuvieron celda en el índice. **760 comps recuperados** — eran comparables que el motor no veía porque estaban bajo una llave rota (`bugambilias` tenía 90 anuncios y `ionamiento bugambilias` otros 48, la misma colonia partida en dos muestras). Dos candados nuevos en el camino: `strip_muni_suffix` ya no deja el nombre colgando de una preposición (`brisas de chapala` no se vuelve `brisas de`) y `casa en venta` ya no produce una colonia llamada "venta".

**Validador del motor sin cerrar.** Baseline medido y guardado: ±10 49.8% · ±15 62.3% · ±20 72.5% · errAbs 15.2% (207 OPIs). El "después" quedó corriendo al cerrar la sesión. Decisión explícita del usuario: si sale peor **no revertir**, revisar qué pasó — esto es afinación y tiene estira y afloja, y el objetivo de la sesión era la década de las colonias, no el motor de precios (que además no lee `colonias_decada.json`).

Commits: `0c21bea`, `9c902ca`, `e584ce1`, `44cac42`, `40bac70`, `c78ae50`.

---

## 2026-07-28 — Créditos + Bolsa de Requerimientos + 5 bugs de producción

## 🔥 SESIÓN 28-JUL — Créditos + Bolsa de Requerimientos + 5 bugs de producción + A4 EstateElite (parcial)

**Features nuevas (desplegadas):**
- **Créditos con expiración real** (`bb949f1`): ledger `creditos_ledger` en `User` (`backend/core/creditos.py`) — gamificación (150 pts=1 crédito) expira a 3 meses, créditos pagados mensuales expiran fin de mes y se reemplazan (no acumulan). Choke point de lectura en `core/auth.py::get_current_user`.
- **Bolsa de Requerimientos** (`330ada6`): tab nuevo en dashboard inmobiliaria. Un asesor publica requisitos de cliente, el sistema busca coincidencias YA existentes en `mercado_props` (con contacto resuelto) al vuelo; sin match, la requisición queda 15 días y el recálculo de "coincidencias nuevas" es LAZY (al abrir `/mias`, mismo patrón que `saldo_efectivo`). Layout final (tras feedback del usuario): KPIs arriba → mapa de zonas con demanda (reusa `MapaAvaluos.jsx`, coords vía `Modulo Drive IA/_geo/proximidad.py`) → feed de otros asesores primero → mis requerimientos → captura como modal "ficha" (no formulario siempre visible). Backend: `backend/core/requisiciones.py` + `backend/routers/requisiciones.py`.

**5 bugs reales de producción encontrados (reportados por usuarios reales, no por mí) y corregidos:**
1. **Alta manual pedía casi todos los campos** (`1a6a723`): `REQUERIDOS_POR_TIPO` en `core/data_exchange.py` exigía recámaras/baños/medios baños/estacionamientos/niveles/conservación para casa/depto/local/oficina/bodega — se dejaron opcionales (solo ubicación+precio+edad+m² son obligatorios, lo que usa el motor).
2. **Video de ads slot1 se veía negro** (`8a96c60`+`ea6f876`+`ce3e51c`): causa real = metadata de color no estándar heredada de Remotion (`yuvj420p`+`color_range=pc`+colorspace SD sobre HD) — combinación que el decodificador de hardware de Chrome/Windows renderiza como negro sólido aunque el archivo sea válido (se ve bien en VLC/ffplay). Fix: forzar `bt709`+rango limitado al recomprimir (`_compress_video` en `ads.py`) + endpoint puntual `/admin/ads/reprocesar-video/{id}` para arreglar creatividades ya subidas + video de respaldo estático también recomprimido. Verificado con `ffprobe` antes/después en el archivo real de producción.
3. **"La Calma" seguía con pendientes tras "verificarla completa"** (sin fix de código — diagnóstico only): NO es bug. La colonia tiene 390 propiedades reales en producción (`cluster0`), la sesión previa solo cubrió 94 — quedaban ~25-27 genuinamente nunca tocadas. Se agregó un contador real "X de Y pendientes en esta zona" en Verificación por Zona (`GET /comps-sin-edad` ahora devuelve `total_pendientes`) para que no se repita la confusión de "ya no queda nada" cuando solo se vio un lote parcial.
4. **Alta manual no aparece en Data Exchange** (sin fix de código, es diseño intencional): `/api/comparables/manual` (Verificación por Zona) solo escribe al pool anónimo (`inmobiliaria_id=None`), nunca al CRM — a diferencia del alta manual DENTRO de Data Exchange, que sí escribe ambos. Se agregó una nota visible (prop `nota` en `PropiedadManualForm.jsx`) en ambos modales explicando la diferencia, para que no se preste a confusión.
5. **"Activar análisis IA" en Perfil del entorno/Equipamiento del reporte** (`0410734`): diagnosticado con logs reales de Railway — NO era falta de cuota (gemini-2.5-flash solo usa 1-2 de 20/día). La falla real fue un JSON truncado puntual de Gemini, y los "respaldos" `gemini-2.0-flash`/`-lite` tienen cuota **0 permanente** en el proyecto GCP (nunca protegieron nada). Fix: `response_mime_type=json` en Gemini (reduce truncamiento) + **DeepSeek real como respaldo** (mismo proveedor que ya usa el motor JS, sin dependencia nueva — `httpx` directo a `api.deepseek.com`). Verificado end-to-end en ambos caminos (Gemini normal + Gemini roto a propósito → DeepSeek).

**Pendiente / RECHAZADO por el usuario:**
- **Ficha A4 de EstateElite — Hoja 1 lista y aprobada** (`LayoutA4EstateElite.jsx`, fuentes reales Archivo Black/Barlow Condensed/Bodoni Moda copiadas del export de Framer a `public/fonts/estate-elite/`, datos reales sin fabricar claims tipo "Escrituras listas"/"Verificada"). **Hoja 2 rechazada explícitamente por el usuario** ("está terrible, no sé de dónde sacaste ese diseño tan malo") — se construyó a partir de un screenshot del editor de Framer (galería 6 fotos + amenidades + contacto) pero el resultado no gustó. **Retomar en la próxima sesión con mejor dirección de diseño antes de tocar código** — no asumir que el patrón de `LayoutFichaTecnica.jsx` (Clásico) ni el screenshot del editor son la referencia correcta; preguntar primero qué debe llevar.
- Todo el código de hoja 2 (`LayoutA4EstateElite2.jsx`) sigue en el repo, funcional pero con mal diseño — no se desplegó a producción (el formato "Ficha A4" de EstateElite no se anunció al usuario como listo, solo se mostró en local).

---

## 28 Jul 2026 — Créditos wireados + Bolsa de Requerimientos + 5 bugs reales de producción + A4 EstateElite (hoja 2 rechazada)

Sesión larga con dos features nuevas planeadas y cinco bugs de producción reportados en tiempo real por usuarios reales (no encontrados por mí), todos investigados con evidencia (logs de Railway, Mongo real, ffprobe) antes de tocar código.

1. **Créditos con expiración real** (commit `bb949f1`): pendiente de sesiones anteriores — "avalúos ganados" nunca se acreditaban de verdad, solo la UI prometía la política. Se construyó `backend/core/creditos.py`: campo `creditos_ledger` en `User` (array de `{monto, otorgado_en, expira_en, origen}`), función `saldo_efectivo()` que suma el ledger filtrando expirados al vuelo (mismo patrón lazy-read usado luego en Requisiciones), sin cron. Gamificación: 150 pts = 1 crédito, expira a 3 meses, idempotente vía contador `creditos_edad_otorgados`. Créditos pagados mensuales: cada asignación de plan REEMPLAZA (no acumula) la entrada anterior, expira fin de mes. Usuarios existentes con `credits` int plano no se migran de golpe — siguen con el fallback hasta su próxima asignación real. Verificado con 5 escenarios unitarios + prueba HTTP end-to-end (doble llamada no duplica el crédito). Desplegado a Railway tras aprobación directa del usuario en la misma sesión.

2. **Bolsa de Requerimientos entre asesores** (commit `330ada6`): idea nueva del usuario — cruzar requisitos de clientes contra el pool `mercado_props` que ya suben todas las inmobiliarias. Diseño inicial (backend `core/requisiciones.py`+`routers/requisiciones.py`, frontend `RequisicionesTab.jsx`): al crear una requisición, busca coincidencias inmediatas (con contacto real resuelto vía `resolver_contacto`); sin match, la requisición queda activa 15 días y el recálculo de "coincidencias nuevas" es LAZY (se recorre en `GET /mias`, comparando contra `matches_vistos`, sin cron ni trigger en los 3 caminos de alta a `mercado_props`). El usuario probó el primer layout (formulario arriba, feed abajo) y lo encontró "aburrido" y mal ordenado — pidió: feed de otros asesores primero, luego mis requerimientos, captura como modal tipo ficha (no formulario siempre visible), foto del asesor en cada tarjeta, y un mapa de zonas con demanda. Se re-investigó y confirmó: `MapaAvaluos.jsx` ya acepta `lat`/`lng` directos (reusable sin cambios), y `Modulo Drive IA/_geo/proximidad.py::coords_de_colonia()` ya resuelve colonia→coordenadas (mismo patrón de import por path que `mongo_comparables.py`). Verificado en vivo con Chrome: requisición real en Chapalita/Guadalajara resolvió coordenadas reales, mapa mostró el punto, feed y mis-requerimientos en el orden correcto.

3. **Bug: alta manual exigía casi todos los campos** (commit `1a6a723`): el usuario reportó que el formulario "Agregar propiedad" no dejaba guardar sin llenar TODO. Causa: `REQUERIDOS_POR_TIPO` en `core/data_exchange.py` (compartido por Data Exchange masivo, alta manual de Data Exchange, y alta manual de Verificación por Zona) exigía recámaras/baños/medios baños/estacionamientos/niveles/conservación para casa/departamento/local/oficina/bodega — prácticamente todo excepto piso/amenidades/descripción/link. Se relajó a solo lo que el motor de valuación realmente usa (ubicación+precio+edad+m²), consistente con que el resto del sistema ya tolera `None` en esos campos (docs scrapeados frecuentemente los traen vacíos). Verificado con curl (fila mínima guarda OK) y en el navegador real (formulario completo con solo esos campos, guardó sin bloquear).

4. **Bug: video de anuncios (slot1) se veía negro** — reportado por el usuario en producción real. Investigado con un agente en background: descartadas las hipótesis obvias (no falta el anuncio activo, el backend sirve el archivo con Range/206 correctamente, el fix de mute/autoplay de sesiones anteriores ya estaba desplegado). Causa real encontrada con `ffprobe`: el video (exportado originalmente con Remotion) trae `pix_fmt=yuvj420p`, `color_range=pc` (rango completo estilo JPEG) y `color_space=bt470bg` (SD) sobre contenido HD — combinación documentada que el decodificador de hardware de Chrome/Windows (Media Foundation) renderiza como negro sólido, aunque el archivo sea perfectamente válido en VLC/ffplay/curl. Fix en dos partes: (a) `backend/routers/ads.py::_compress_video` ahora fuerza `-color_range tv -colorspace bt709 -color_primaries bt709 -color_trc bt709` al recomprimir cualquier video nuevo (commits `8a96c60`); (b) para el archivo YA subido, se agregó un endpoint puntual `POST /admin/ads/reprocesar-video/{creative_id}` (commit `ea6f876`) que recomprime en sitio con las mismas banderas — se ejecutó contra la campaña real de producción y se verificó con `ffprobe` que quedó en `yuv420p`/`tv`/`bt709`; (c) el video de respaldo estático (`frontend/public/ads/propvalu-emo-horizontal.mp4`) tenía el mismo problema — se recomprimió localmente y se redesplegó (commit `ce3e51c`), verificado en producción tras el deploy de Vercel.

5. **Falso bug: "La Calma sigue con propiedades pendientes tras verificarla completa"** — el usuario insistió varias veces (con razón) en que mi primer diagnóstico estaba mal, porque yo había consultado la base de **staging** (`cluster1.avle5ez`, solo 29 docs de esa colonia) en vez de **producción** (`cluster0.9eliadx`, donde realmente trabaja la app). Al consultar producción con la URI comentada en `backend/.env`: La Calma tiene **390 propiedades reales**, la sesión de verificación anterior (20 y 24 de julio, mismo `user_id`) solo procesó 94 de ellas — quedaban ~25-27 genuinamente nunca tocadas (confirmado caso por caso: ninguna tenía `edad_estimador`). No es un bug de pérdida de progreso; la colonia simplemente es mucho más grande de lo que el perito recordaba, y el verificador nunca mostraba "cuántas faltan del total". Se agregó `total_pendientes` a la respuesta de `GET /comps-sin-edad` (`backend/routers/edades.py`) y un aviso "Mostrando X de Y pendientes en esta zona" en `EdadesZonaPage.jsx` para que no se repita la confusión. **Lección para futuras sesiones: verificar SIEMPRE contra `cluster0` cuando el reporte es sobre lo que ve un usuario real** — staging y producción pueden tener datasets radicalmente distintos para la misma colonia.

6. **Aclaración (no bug): alta manual de Verificación por Zona no aparece en Data Exchange** — por diseño, `/api/comparables/manual` escribe solo al pool anónimo (`inmobiliaria_id=None`), a diferencia del alta manual DENTRO de Data Exchange que sí escribe CRM+pool con atribución. Se agregó una nota visible (prop `nota` en `PropiedadManualForm.jsx`) en ambos modales para que la diferencia sea explícita y no se preste a confusión futura.

7. **Bug: "Activar análisis IA" en Perfil del entorno / Equipamiento del reporte** — el usuario preguntó "en qué se gastaron los 20 [de cuota]" ya que solo se habían generado 2 reportes. Investigado con logs reales de Railway (deploy `9c09387a`, timestamp exacto 2026-07-28T01:49:06Z) y con la consola de Google Cloud (el usuario compartió pantalla en vivo, navegamos juntos a Cuotas y límites): **no se gastó ninguna cuota** — `gemini-2.5-flash` llevaba 1 de 20 usos ese día. La única llamada de ese momento a `gemini-2.5-flash` sí respondió, pero con un JSON truncado a la mitad (`Expecting ',' delimiter: line 1 column 3977`) — un fallo de formato, no de cuota. Los 2 modelos de "respaldo" en el código (`gemini-2.0-flash`, `gemini-2.0-flash-lite`) tienen **cuota 0 permanente** en este proyecto de GCP (confirmado en la pestaña "Cuotas y límites del sistema": `Request limit per model per day for a project in the free tier` = 0 para ambos) — nunca han funcionado como respaldo real, ni antes ni ahora. El usuario propuso DeepSeek como alternativa; se investigó su viabilidad (ya usado como motor principal en el resto del proyecto vía API compatible OpenAI, `httpx` ya es dependencia del backend) y se implementó (commit `0410734`): `generation_config={"response_mime_type": "application/json"}` en la llamada a Gemini (reduce el riesgo de truncamiento) + una función `_call_deepseek()` como único respaldo real si Gemini falla. Verificado end-to-end en ambos caminos: reporte normal generó "Perfil del entorno" real vía Gemini; con la key de Gemini deliberadamente invalidada, el mismo reporte generó contenido real (con texto distinto, confirmando que vino de DeepSeek) sin caer al placeholder.

8. **Ficha A4 de EstateElite** — el usuario pidió una hoja A4 imprimible para el estilo EstateElite del Diseñador de Promocionales, a partir de un export de Framer que compartió por zip. El primer zip resultó ser un snapshot vacío del editor de Framer (sin contenido, típico de "Guardar página como" sobre la URL del editor en vez de exportar el sitio real) — se identificó y se pidió el zip correcto. Con el zip correcto (`free-embeds-329601-b763bf0f0.framer.app.zip`), se extrajo el diseño real (una sola hoja: header+foto+atributos+cuerpo+galería+datos+contacto+pie) y se tradujo a React (`LayoutA4EstateElite.jsx`) mapeando a datos reales del avalúo — reemplazando el copy fabricado del mockup ("Escrituras listas", "✓ Verificada" ×2) por datos reales (operación real, specs reales) según decisión ya documentada en el plan aprobado. Fuentes reales (Archivo Black, Barlow Condensed, Bodoni Moda) copiadas del zip a `frontend/public/fonts/estate-elite/`. El usuario aprobó esta hoja 1 tras verla renderizada con una propiedad OPI real. Luego pidió una "hoja 2" que él creía que venía en el mismo zip — se verificó dos veces (contenido del zip + sitio publicado en vivo) que el zip **solo tiene una hoja**; la hoja 2 solo existe en el editor de Framer (nunca publicada/exportada). Se navegó al editor en vivo (link que compartió el usuario), se documentó visualmente una vista con galería de 6 fotos + amenidades, pero resultó inconsistente entre cargas (el canvas de Framer es un iframe cross-origin sandboxed, no se pudo extraer HTML confiablemente). El usuario finalmente mandó un screenshot real de la hoja 2 (galería 6 fotos asimétrica + franja de amenidades con íconos + pie de contacto con foto/nombre/rol del asesor y marca/teléfono/correo) — se construyó `LayoutA4EstateElite2.jsx` fiel a ese screenshot, reusando los íconos de amenidades ya existentes del proyecto (se movieron a `promociones/amenidadesIcons.js` para evitar un import circular con `PromocionesTab.jsx`). Se integró el mecanismo de "Hoja 1 / Hoja 2" ya existente para el estilo Clásico (`tieneHoja2`/`hasHoja2` en `PromocionesTab.jsx`), extendiéndolo para EstateElite+formato `a4_ee`. **Al ver el resultado, el usuario la rechazó de forma tajante** ("está terrible, no sé de dónde sacaste ese diseño tan malo") — pidió cerrar la sesión para retomarlo en la siguiente. El código quedó commiteado (funcional, sin errores) pero NO desplegado a producción; queda pendiente pedir dirección de diseño real antes de volver a intentarlo.

**Reflexión de la sesión:** de los 5 problemas reportados por usuarios reales, solo 2 fueron bugs de código genuinos (campos obligatorios de más, color del video) — los otros 3 fueron diagnósticos que requirieron desmentir una primera hipótesis equivocada mía (La Calma en staging en vez de prod; el "gastar cuota" que en realidad era JSON truncado) o aclarar comportamiento intencional que no estaba comunicado (alta manual anónima vs CRM). En los tres casos, la evidencia real (logs de Railway, consultas directas a Mongo de producción, consola de Google Cloud navegada en vivo con el usuario) fue lo que permitió corregir el rumbo — ninguno se resolvió por intuición o suposición.

---

## 27 Jul 2026 (noche) — Pulido de UI: gamificación compacta + avisos colapsables con campana (commit `e9b5dd7`)

Ronda de retoques de UX pedidos por el usuario sobre `EdadesZonaPage` (Verificación por Zona) y `InmobiliariaDashboardPage`, iterando pantalla por pantalla con capturas reales antes de cada commit:

1. **Récord/gamificación (`GamificacionVerificador.jsx`):** la caja "Ayuda a completar los datos de tu zona…" vivía separada del cuadro de récord con tamaños distintos — el usuario pidió integrarla y unificar el fondo. Primero se fusionó como segunda línea dentro de la misma barra verde; luego, a pedido de comprimir filas, esa línea se reemplazó por un ícono `Info` con tooltip, dejando la barra en una sola fila. Se agregó texto de vigencia/canje a "avalúos ganados" — el usuario confirmó la política real: **se acreditan como créditos adicionales en la cuenta, vigencia 3 meses** — reflejado en el tooltip de la barra, el panel "Mi récord y concurso" y el festejo de trofeo al cruzar la meta. La gráfica de barras de "Últimos días" pasó de solo-tooltip a mostrar la cantidad y el día de forma visible (dos filas: cantidad sobre la barra, día del mes debajo).
2. **Verificación por Zona (`EdadesZonaPage.jsx`):** el bloque "Verifica y Gana" se movió a la fila del título (antes ocupaba su propia fila) para liberar espacio vertical. La fila de filtros (Estado/Municipio/Colonia/Tipo + botones Buscar/Agregar propiedad + checkbox Datos raros) se comprimió (`h-9→h-8`, `text-sm→text-xs`) para caber todo en una sola línea en vez de partirse en dos. Se detectó que la caja de Colonia (`ColoniaCombo`, un `<Button variant="outline">` haciendo de combobox) no compartía altura ni estilo de borde con los `<Select>` de shadcn de al lado — se unificó explícitamente (mismo `h-8`, mismo verde al tener valor seleccionado). Se recortó también el margen entre el título y la tarjeta de filtros, que quedaba con doble espaciado acumulado (`mb-6` en ambos).
3. **Dashboard inmobiliaria (`InmobiliariaDashboardPage.jsx`):** la fila de avisos (Falta de documentos / Programa Data Exchange / Renovar plan) ganó un toggle colapsable para no competir por espacio con el resto del dashboard. Primera versión: botón "Avisos" con chevron dentro del flujo del dashboard. El usuario pidió moverlo a la fila del logo/perfil en el header, como una campana — y que al colapsar no quedara ni rastro de espacio (nada de fila vacía). Se implementó con una firma persistida en `localStorage` (`showKycBanner-docsCompletos`) para decidir si mostrar la campana en rojo ("no leído": el contenido cambió desde la última vez que el usuario expandió la fila). De paso, el usuario notó que el chip de perfil (`displayName = company_name || name || email`) nunca mostraba empresa Y asesor juntos — se agregó un segundo chip pequeño (foto+nombre del asesor) al lado del chip de empresa, para diferenciar cuentas abiertas en pestañas distintas.
4. **Bug de rules-of-hooks introducido y corregido en la misma sesión:** al mover la lógica de "no leído" cerca de otras variables (`showKycBanner`, `docsCompletos`), el `useEffect` quedó después de un `if (!session) return null;` ya existente en el componente → React tiró `Compiled with problems` (hook llamado condicionalmente). Se detectó de inmediato al recargar el navegador (capturas de pantalla muestran el overlay de error de CRA) y se corrigió moviendo el efecto antes del early-return, recalculando las variables necesarias inline con `session?.` para no depender de las declaradas después del guard.
5. **Verificación:** cada cambio se probó en vivo con claude-in-chrome (`/edades-zona` y `/dashboard/inmobiliaria`) — capturas de pantalla y zooms antes/después, clic real en el toggle de campana con recarga de página para confirmar que el estado "visto" persiste. Solo se commitearon los 3 archivos de frontend tocados (`git status` mostraba también gitlinks de `.claude/worktrees/*` de otras sesiones, dejados fuera a propósito). Deploy final a Vercel prod (`vercel --prod`, solo frontend — sin cambios de backend esta ronda).

---

## 27 Jul 2026 — Alta manual de propiedades (#144) + bug de raíz CSS sitewide + ronda larga de pulido UX

**Feature (commits `4dc68f5`, `42924a2`):** el usuario pidió poder dar de alta 1-2 propiedades sueltas al catálogo (`mercado_props`) desde 3 lugares — perito en revisión de comparables, inmobiliaria en su catálogo, revisores internos — sin tener que armar la plantilla masiva de Data Exchange (#133). Investigación previa (graphify + lectura directa) mostró que `mercado_props` (pool de comps) y `propiedades_inmobiliaria` (CRM de fichas para promocionar) son colecciones separadas, y que Data Exchange (`core/data_exchange.py` + `routers/data_exchange.py`) ya tenía toda la lógica de validación/dedup/escritura por tipo, solo atada a un archivo CSV/XLSX. Plan aprobado por el usuario vía EnterPlanMode: reusar esa lógica en 2 endpoints JSON nuevos en vez de reinventar — `/api/inmobiliaria/data-exchange/manual` (inmobiliaria, CRM+pool) y `/api/comparables/manual` (perito/admin/inmobiliaria, reusa el patrón de doble auth `_quien()` que ya vive en `edades.py`, solo pool) — en vez de construir 3-4 pantallas nuevas se detectó que **Verificación por Zona ya es compartida por los 3 roles**, así que ahí se montó un solo punto de entrada + otro en el tab Data Exchange de la inmobiliaria. Se extrajeron `fila_a_doc_pool`/`fila_a_doc_crm` de adentro de `confirmar()` para que la carga masiva y la manual compartan exactamente el mismo código de escritura (cero duplicación). Campo `link` de origen opcional con verificación en vivo best-effort (`httpx`, nunca bloquea el guardado) + dedup por URL contra el pool.

**Ronda larga de ajustes UX guiados por captura de pantalla real (muchas iteraciones):** el usuario fue afinando el modal (`PropiedadManualForm.jsx`) y la fila de anuncios del dashboard de inmobiliaria pieza por pieza: (1) agregar campo remodelación (check → despliega grado+año, igual que Verificación por Zona) y edad por año exacto o por rango; (2) amenidades pasaron de texto libre a los mismos íconos que ya existían en `PromocionesTab.jsx` (`AMENIDADES_ICONS`, exportado para reuso, no reinventado) — luego a pastillas ícono+texto en una sola línea a pedido; (3) reordenar Conservación junto a Año sin desplazar lo que ya estaba bien puesto — la lección fue ensanchar el modal (`max-w-3xl→4xl`, grid 6→7 columnas) en vez de reflow; (4) varias rondas de alineación vertical entre cajas de la misma fila.

**Bug de raíz encontrado de paso — no introducido hoy:** al pedir que el título del modal "no se viera transparente", se rastreó hasta `frontend/src/index.css`: `--background`/`--primary`/`--secondary`/`--accent`/`--border` estaban definidas DOS veces — una en hex plano en un `:root` normal, otra en HSL-triplete dentro de `@layer base { :root {...} }` (la que Tailwind necesita para sus clases `bg-background`/`bg-primary`/etc, que hacen `hsl(var(--x))`). Por cómo Tailwind reordena el contenido de `@layer` a la posición de `@tailwind base;`, el hex plano terminaba ganando la cascada → `hsl(#F8F9FA)` es CSS inválido → cualquier componente usando esas clases quedaba con fondo transparente **en todo el sitio**, no solo en el modal nuevo (por eso otros Dialogs no lo mostraban: usan `bg-white` explícito en vez de `bg-background`). Fix: renombradas las variables hex a `--hex-*`, usadas solo por el scrollbar y el `body` (que las consumen sin `hsl()`).

**Otro hallazgo real vía CSS global:** los campos con `placeholder` visible ("—", "2015") se pintaban con borde gris por una regla `input:not(:placeholder-shown)`/`button[role=combobox]:not([data-placeholder])` en `index.css` (verde solo cuando ya tienen valor); los campos sin placeholder eran siempre verdes. Fix puntual: quitar el placeholder del año exacto y forzar verde permanente vía clase+selector con especificidad suficiente (`.pv-force-green[role="combobox"]`) en los selects que sí necesitan placeholder (Conservación, rangos).

**Lección de diseño reforzada varias veces en la sesión (memoria nueva `feedback_diseno_alineacion_filas`):** `items-stretch` en un contenedor de cajas de la misma fila SOLO funciona bien si ninguna caja usa `justify-between` internamente (eso empuja los botones al fondo de la caja estirada, alejándolos del texto) — el orden correcto es apilar con `gap` primero, stretch después. Y dos botones con la misma altura declarada (`h-9`) pueden verse distintos si uno tiene borde visible y el otro no — hay que igualar grosor Y color (verificado con medición real vía `getBoundingClientRect`/`getComputedStyle` en el navegador, no a ojo, cuando el usuario pidió confirmar).

**Verificado en vivo:** login real (claude-in-chrome) como inmobiliaria en `/dashboard/inmobiliaria` → tab Data Exchange → botones medidos por DOM confirmando alineación pixel-exacta. 13/13 tests backend (`test_data_exchange.py`) pasan.

**Desplegado a prod (27-jul, misma sesión, más tarde):** `railway up --detach` (backend) + `vercel --prod` (frontend), ambos en paralelo con Monitor. Backend Railway deployment `91653f5f` → SUCCESS, `/api/health` 200. Frontend Vercel → READY, alias `frontend-rosy-six-74.vercel.app` 200. Incluye TODO lo acumulado desde el 26-jul (EstateElite) + lo de hoy (#144) — el usuario pidió "haz el deploy de una vez" y se ejecutó directo (autorización explícita).

**Pendiente:** probar en vivo el flujo desde Verificación por Zona (solo se probó a fondo Data Exchange); evaluar si la plantilla XLSX de carga masiva también debería exponer `link`/remodelación (hoy solo el alta manual los tiene).

---

## 26 Jul 2026 — Plan EstateElite implementado completo (hojas + export + formatos + zoom) + 2 pendientes obsoletos cerrados

Retomó exactamente el plan aprobado y guardado la sesión anterior (`pure-wobbling-dongarra.md`). Mientras se implementaba y verificaba en vivo, el usuario fue reportando problemas reales de UX que llevaron a varias rondas de diagnóstico-y-fix, todas resueltas en la misma sesión.

**Hojas dinámicas (commit `3934a60`):** backend agrega `puntos_libres`/`puntos_propvalu` a `_PROMO_CAMPOS` (`backend/routers/inmobiliaria.py`); formulario "Subir propiedad" gana un input de 2 líneas para "Puntos destacados" (antes solo editable en el panel efímero del reel, nunca persistía); `PromoReelEstateElite.jsx` reescrito de 3 hojas hardcodeadas a un array `slides` dinámico — Descripción/Puntos/Galería se insertan automáticamente antes de Contacto solo si hay dato real (sin toggle manual, "el usuario decide cuántas" con solo llenar los campos). Verificado creando una propiedad de prueba con descripción+puntos+fotos: las 6 hojas renderizan y navegan bien con clic normal.

**2 bugs de raíz al verificar el export (commit `1398629`):** el usuario probó "descargar JPG" y salió con texto de varias hojas montado encima, ilegible. Diagnóstico: (1) `LayoutEstateElite.jsx` era el ÚNICO layout de toda la app sin `id="pv-ficha-root"` en su wrapper — el buscador de elemento (`document.getElementById`) no encontraba nada, así que JPG/PDF no capturaban absolutamente nada; (2) una vez agregado el id, el resultado seguía saliendo con texto duplicado/montado — causa: `html2canvas` no soporta correctamente la propiedad CSS `zoom` (no estándar) que usa el preview del panel para el zoom in/out, y con las 6 hojas de EstateElite apiladas vía `position:absolute`, esto producía una captura con el contenido duplicado a dos escalas superpuestas. Fix: capturar siempre a zoom 1 (se restaura el zoom del usuario después).

**Generalización a pedido explícito del usuario ("que sirva para todos", commit `56e949f`):** una vez corregido el bug de EstateElite, el usuario preguntó por qué el JPG solo bajaba una hoja si "queremos ver lo que estamos haciendo, no adivinar" — pidió que TODAS las hojas se exportaran, y que el mismo criterio aplicara a otros estilos/formatos. Se generalizó: JPG ahora itera y descarga una imagen por cada hoja tanto en EstateElite (dinámico, vía nuevo estado controlado `activeSlide`/`onSlideChange`/`onSlideCountChange` levantado a `PromocionesTab`) como en Clásico Hoja1+Hoja2 (mismo bug de "solo exporta la visible", nunca reportado antes porque nadie lo había probado a fondo). Se investigó también el formato "Post·3 slides"/"TikTok·3 slides" de Just Listed — esos YA renderizan las 3 hojas apiladas en un solo elemento continuo, así que el JPG ya las capturaba todas juntas (no tenían el bug, diseño distinto). El PDF de EstateElite se reconstruyó de cero: el mecanismo genérico usaba `window.print()` nativo del navegador, que para el formato reel (390×693) no tenía ningún CSS de impresión (`@page`) — imprimía la hoja tal cual sobre una página A4/Carta default, saliendo diminuta y fuera de lugar. Se reemplazó por un PDF real 100% client-side (`jsPDF`+`html2canvas`, ya instalados, cero dependencias nuevas) con una página por hoja. De paso, el botón "Secuencias" (que el usuario reportó mostrando una hoja 2 "muy diferente al original") resultó estar codificado para animar SIEMPRE el diseño Just Listed sin importar el estilo seleccionado — se ocultó para EstateElite en vez de construir una versión nueva (fuera de alcance).

**Formatos nuevos Post 1:1 y Facebook para EstateElite, a pedido del usuario ("aplica también a 1:1/Facebook o el que se agregue", commit `56e949f`):** en vez de rediseñar el CSS de cada una de las 6 hojas para 2 proporciones nuevas, se reutilizó el diseño existente completo: el reel de 390×693 se escala y centra dentro del lienzo del formato elegido (1080×1080 post, 1200×628 Facebook), con el fondo de la hoja activa (oscuro o crema en Contacto) extendido a los costados como letterbox — cero CSS nuevo, cero riesgo de divergencia visual. Verificado en navegador: Post 1:1 se ve bien proporcionado; Facebook funciona pero con mucho espacio vacío a los lados por el choque de proporciones (9:16 contra ~16:9) — marcado con comentario `ponytail:` como mejora futura (ej. fondo con blur de la foto en vez de color sólido), no bloqueante.

**Zoom del visualizador auto-ajustable (commit `3709e89`):** el usuario reportó que en Reel 9:16 no veía los botones de abajo sin hacer scroll, y pidió revisar si pasaba en los demás formatos. Causa: el zoom por defecto (50%) era un valor fijo sin relación al tamaño real de cada formato (693px el reel, 1080px Post 1:1, etc.) ni al espacio disponible del panel. Se implementó un cálculo genérico: mide el tamaño real de `#pv-ficha-root` (dividiendo por el zoom actual para obtener el tamaño nativo, sin necesitar una tabla de tamaños por formato) y el espacio disponible del contenedor scrollable, y ajusta el zoom para que quepa completo — funciona para cualquier estilo/formato existente o futuro sin código adicional. Verificado reduciendo la ventana del navegador a 750px de alto: ya no hace falta scroll ni en Reel 9:16 ni en Post 1:1 (el más alto de los tres).

**Toolbar solo-ícono en pantallas chicas (commit `3f65e8a`):** al preguntar el usuario si todo esto se ve bien en un teléfono, se revisó el código (no se pudo forzar un viewport móvil real con las herramientas de navegador de este entorno — `resize_window` no afecta el viewport que controla la automatización) y se encontró que los botones PDF/JPG/Promo Interactiva/Secuencias mostraban texto siempre, apretándose en 360-390px. Se ocultó el texto bajo el breakpoint `sm` (queda ícono + `title` como tooltip). Pendiente verificar en un teléfono físico real.

**2 pendientes obsoletos cerrados vía agentes en paralelo (a pedido del usuario, "por qué no despliegas agentes para tareas que no se pisen"):** un agente en worktree aislado confirmó que el parche de motor `gap6` (commit `e8ed0fd`) YA estaba mergeado a `main` y desplegado a prod desde una sesión anterior (`git log` mostraba la rama `fix/flujo-avaluo-reporte-jul20` como 100% ancestro de `main`, commit `99cf9ea` documentando el deploy) — no había nada que mergear. Otro agente retomó el re-enriquecimiento PINCALI (`enricher.py --tab PINCALI --max 6600 --mongo`, sintaxis corregida — `--mongo` sí existe, la nota previa de que "ya no existe" era incorrecta) con monitoreo activo: solo 6 de 6,203 pendientes se lograron antes de volver a golpear el mismo soft-block HTTP 202 del 23-jul — confirma que el bloqueo del portal NO se enfrió en 3 días y necesita proxy rotativo o backoff mucho mayor, no solo esperar.

**Estado final:** todo commiteado en `main` (`3934a60` → `3f65e8a`), **sin `git push` ni deploy** — decisión pendiente de la próxima sesión. El usuario mencionó de pasada que para el "próximo estilo" quiere usar la skill `ui-ux-pro-max` (base de datos de diseño) — anotado para cuando se construya un estilo nuevo desde cero (aquí no aplicaba, solo se extendía un diseño ya existente).

---

## 24 Jul 2026 — EstateElite seleccionable en el Diseñador + plan de hojas opcionales/PDF cliqueable

Continuación directa de la sesión anterior ("Promo Interactiva"): el usuario quería que EstateElite dejara de ser un flujo aparte (solo accesible vía el pill que copia el link) y pasara a ser una plantilla más dentro del selector "Estilo de diseño" del Diseñador — la primera de varias presentaciones nuevas que planea agregar ahí (por ahora, la versión "reels TikTok"). Tarea de 2+ archivos con render pipeline → se entró en `EnterPlanMode` por regla del proyecto, con dos agentes Explore en paralelo mapeando `PromocionesTab.jsx` (TEMAS/FORMATOS/grilla/getLayoutComponent/props) y `PromoReelEstateElite.jsx`+`PromoPublicPage.jsx`+backend+diseño de referencia.

**Implementación (3 archivos, commits `aab71fa`+`b358703`):** nuevo `LayoutEstateElite.jsx` (adaptador delgado, traduce `fichaAvaluo`/`asesor`/`slidesFotos`/`amenidades` del panel al shape `ficha`/`asesor` que espera `PromoReelEstateElite` sin tocarlo) + registro en `getLayoutComponent`/`TEMA_THUMB`/`FORMATOS` (formato único "Reel 9:16")/grilla del selector, siguiendo el mismo patrón ad-hoc que ya usan `moderno`/`just_listed` (temas fuera del objeto `TEMAS`).

**2 bugs de raíz encontrados solo al verificar visualmente en el navegador (no eran evidentes por lectura de código):**
1. El `<style>{STYLES}</style>` inline de `PromoReelEstateElite.jsx` quedaba con `sheet.cssRules.length === 0` — el navegador aceptaba el nodo pero nunca parseaba las reglas al montarse anidado dentro de todo el árbol del panel (Cards, flex, `zoom`). Confirmado copiando el mismo texto a un `<style>` creado a mano en `document.head`, que sí parseaba (68 reglas). Fix: mover la inyección de CSS a `document.createElement("style")` + `document.head.appendChild()` en un `useEffect` idempotente (mismo patrón ya usado para las Google Fonts del componente).
2. Al ver el reel en el panel, el texto se veía diminuto contra la foto — el wrapper nuevo usaba `1080×1920` (la convención de export estático en alta resolución que usa `LayoutModerno` para PNG/JPG), pero los tamaños de fuente de `PromoReelEstateElite` (34px título, etc.) están calibrados para un **ancho de teléfono real** (~390px, como el `stage` de `PromoPublicPage.jsx`: `width: min(100vw, 56.25dvh)`), no para una imagen de 1080px. Confirmado comparando contra el HTML de referencia original (Stitch) y contra el código fuente que el usuario compartió del prototipo. Fix: `REEL_W=390, REEL_H=693`.

**Verificación end-to-end con datos reales:** la propiedad de prueba "Cuarzo 2380" tenía `fotos:[]` real en Mongo (el panel mostraba una foto de stock — `SAMPLE_FOTOS`, fallback genérico de diseño, no un dato real de la propiedad, fuente de confusión al inicio). Se agregó una foto de prueba (URL de Unsplash ya usada como sample en el propio código) a la propiedad y una foto de asesor al usuario dueño, directo en Mongo, para poder confirmar visualmente el hero de portada y la hoja de contacto contra el diseño de referencia. Confirmado en el camino que `GET .../promo-publica` sigue sin exponer `user_id`/email (whitelist `_PROMO_CAMPOS` + proyección de `users` acotada).

**Efectos secundarios de la sesión (no relacionados al feature, resueltos en el camino):** la IP pública del usuario rotó y volvió a bloquear el backend local contra Mongo Atlas (TLS handshake failed) — se re-agregó al Network Access. Un rabbit-hole largo de teclado (el usuario reportaba que `@`/`#` no funcionaban en Chrome) resultó ser causado por el propio asistente: al intentar ayudar con un problema de teclado NO relacionado a este proyecto, se agregó un layout de Windows adicional (Inglés US) que hizo que Chrome empezara a pedir `AltGr+2` en vez de `AltGr+Q` para `@` — revertido a solo Español (Latinoamericano), el layout original.

**Plan grande aprobado y guardado, NO implementado** (sesión cerrada por presupuesto de contexto, con `/end-session` como cierre de seguridad) — completo en `C:\Users\pedru\.claude\plans\pure-wobbling-dongarra.md`:
- **Hojas opcionales** (Descripción / Puntos destacados-características especiales / Galería de fotos), insertadas dinámicamente antes de la hoja de Contacto, apareciendo automático solo si hay datos llenados (sin toggle manual — el propio usuario "decide cuántas agregar" con solo llenar o no esos campos). Investigación clave: nada de lo que se edita en las secciones Descripción/Puntos/Amenidades del panel del reel se persiste hoy en el backend (es estado de React efímero, solo para preview/export de esa sesión) — `descripcion` y `fotos` SÍ ya están persistidos y en la whitelist pública (cero trabajo extra), pero `puntos_libres` no tiene ningún input en el formulario "Subir propiedad" (solo se edita en el panel efímero) y falta en la whitelist `_PROMO_CAMPOS` junto con `puntos_propvalu` (ya calculado gratis server-side comparando contra `mercado_props`).
- **Botón de descarga PDF con navegación interna cliqueable** entre hojas. El mecanismo de PDF que ya existe en la app (botón "PDF" del toolbar, `exportarFicha` en `PromocionesTab.jsx`) usa `window.print()` nativo del navegador — sin links cliqueables, solo páginas secuenciales. El usuario pidió explícitamente la versión con anclas navegables (más portable/offline que el link HTML, y se ve como archivo con preview nativo si se manda por WhatsApp en vez de como link sin miniatura). Confirmado que `jsPDF` ya está instalado en el proyecto (`^4.2.1`) y `html2canvas` ya se usa (export "JPG" existente) — combinar ambos (capturar cada hoja a imagen + `doc.link()` de jsPDF apuntando a la página interna correspondiente), cero dependencias nuevas, 100% client-side, sin guardar nada en el servidor.

---

## 23 Jul 2026 (noche #7) — Feature "Promo Interactiva": link público compartible (reel EstateElite con datos reales)

Sesión de feature, distinta a las anteriores (motor/scraper). El usuario venía trabajando en un diseño propio de reel inmobiliario "EstateElite" (9:16, 3 hojas: Portada + Características + Contacto, paleta verde `#051b12` + dorado `#e9c176`, tipografías Noto Serif + Work Sans) que primero prototipamos como HTML suelto FUERA del repo (`C:\Users\pedru\valuation-ai\disenos-tiktok\`, no git). El pedido final: llevarlo a la app en Promociones como una opción que genere un **link público** para compartir por WhatsApp — porque un archivo `.html` adjunto no siempre lo reconoce el celular del destinatario como abrible.

**Aclaración clave a mitad de camino:** el usuario recalcó que el diseño debe ser SU EstateElite exacto (respetado al 100%), NO el `LayoutStitchTikTok`/"Obsidian" que ya trae el app (ese "le faltan elementos y está desacomodado"), y con datos REALES de la propiedad. Eso cambió el plan aprobado originalmente (que reusaba el layout del panel): como el diseño pasa a ser fijo, se DESCARTÓ toda la capa de persistencia de config (`promo_config` PUT) y el refactor `renderPromo.js` — innecesarios. El link se arma solo con `propiedad_id`; la página pública recomputa desde el registro de la propiedad + asesor.

**Implementación (5 archivos, aditiva, no toca nada existente):**
- `PromoReelEstateElite.jsx` (`frontend/.../promociones/`): port fiel del reel, **autocontenido** — `<style>` scoped bajo `.ee-reel` + iconos SVG inline en un objeto `ICONS`. No depende del `tailwind.config` del app (sus tokens `primary/surface/etc.` colisionarían). Navegación con los botones propios de cada hoja (Ver características → Ver contacto → Atrás y los puntos de arriba), heurística `iconoDe()` para elegir icono de cada amenidad, fallback de foto de asesor a inicial, CTA de contacto como `tel:` si hay teléfono. Basado en un HTML que ya se validó visualmente (fuentes/imágenes incrustadas) durante la sesión.
- `backend/routers/inmobiliaria.py`: `GET /api/inmobiliaria/propiedades/{id}/promo-publica` PÚBLICO (sin `require_auth`, patrón de `directorio.py`). Whitelist `_PROMO_CAMPOS` de la propiedad (nunca `user_id`/`activo`/`fuera_de_mercado`/internos) + asesor `{nombre, foto, telefono}` resuelto del `User` dueño con proyección acotada (nunca el doc completo ni email).
- `frontend/src/pages/PromoPublicPage.jsx` + ruta `/promo/:propiedadId` en `App.js` (sin auth shell, patrón de `/reporte/:valuationId`): fetch al endpoint, render del reel a 9:16 centrado (letterbox en desktop), 404 → "promoción no disponible". Inyecta Noto Serif + Work Sans por CDN (web normal, permitido — a diferencia del artifact con CSP).
- `PromocionesTab.jsx`: pill "Promo Interactiva" junto a PDF/JPG + handler `handlePromoInteractiva` (si hay `propiedad_id` copia `${origin}/promo/{id}` al portapapeles; si no, toast pidiendo guardar la propiedad).

**Verificación:** frontend compila limpio (`Compiled successfully!`), backend importa y la ruta queda registrada como GET público (confirmado por OpenAPI). **NO se pudo probar end-to-end con datos reales** porque el entorno no alcanza el cluster de staging (`cluster1.avle5ez`, bloqueado por IP allowlist de Atlas — falla el handshake TLS, esperado). Se entregó al usuario un prompt para verificar desde una terminal con acceso a la DB (backend local + `/promo/:id` en incógnito + pill). Commit `f86ab06`, pusheado a `main` (Railway + Vercel auto-deploy). El push de esta sesión de memoria lo hace el usuario desde terminal.

**Nota de higiene:** el usuario insistió en NO commitear plasmic/storybook/temporales ni los HTML de prueba sueltos — se respetó (solo los 5 archivos de la feature entraron al commit; el resto quedó sin tocar y los prototipos viven fuera del repo git).

**Limitación conocida:** el link solo funciona hoy para propiedades manuales/Subidas (las que tienen `propiedad_id`), no para avalúos OPI. Pendientes: verificar en prod, soporte OPI, botón WhatsApp `wa.me` en el CTA, preview del reel dentro del panel.

---

## 23 Jul 2026 (noche #6) — PINCALI bug de raíz (m²c+título) + limpieza retroactiva Mongo + Ixtepete portado al motor + ads/gamificación/edades

Sesión de "los dos" pendientes de la noche anterior (PINCALI en la fuente + subir cobertura de colonias débiles), que fue creciendo por interrupciones del usuario con hallazgos en vivo mientras trabajaba en la app.

**PINCALI m²c y título, root cause real:** investigado con ejemplos vivos de Mongo (los mismos que quedaron corruptos tras la limpieza de 744 celdas de la sesión #5). Confirmado fetcheando páginas en vivo: la página de LISTADO que se escanea es siempre `/en/` (inglesa) porque la ruta española `/propiedades/` da 403 — no existe forma de listar en español. Su m²c sale de un regex de prosa sin ancla sobre el texto concatenado de la tarjeta (primer "N m²" que encuentre), que en preventas/desarrollos agarra a veces un número ajeno (terraza, "desde X m²"). El DETALLE sí trae el dato estructurado y confiable (JSON-LD `floorSize` + bloque escapado `Area M2`, mismo bloque de donde ya se sacaba `Parking Spaces`) — el enricher ahora lo usa como autoritativo y SIEMPRE pisa lo que puso la tarjeta para PINCALI (antes solo rellenaba si faltaba, por eso el bug nunca se corregía solo). Se agregó también un guardia de $/m² físicamente plausible (3,000-200,000) directo en `pincali.py`, antes de guardar en Mongo — "limpiar antes de insertar" en vez de solo después, a pedido explícito del usuario.

**Título en español:** mismo problema — la tarjeta que se escanea es inglesa, así que el título de PINCALI salía siempre en inglés pese a la regla "PINCALI solo español" (que hasta hoy solo cubría colonia/año, extraídos del detalle en español, no el título de la tarjeta). Fix: el detalle SÍ trae `Property Type` y `Operation Type` estructurados (mismo bloque) → se sintetiza un título limpio en español ("Casa en venta en X") y se pisa siempre el de tarjeta para PINCALI.

**Limpieza retroactiva en Mongo (sin scraping, solo transformación local de datos ya guardados):** 38,726 títulos regenerados a español desde `tipo_propiedad`+`tipo_operacion`+`colonia` (deterministico) + 5,729 `m2_construccion` corruptos identificados y limpiados (`$unset`) en PINCALI casa/depto activos con $/m² imposible — mucho más volumen que los 744 de la sesión #5, porque esa limpieza fue solo en el caché del motor (celdas de `cache_index.json`), nunca tocó `mercado_props` crudo. Se reseteó `enrich_last_attempt` de los 6,548 docs afectados (5,729 limpiados hoy + ~819 que ya estaban vacíos) para forzar su re-paso por el enricher ya arreglado. La corrida se lanzó (`enricher.py --tab PINCALI --max 6600 --mongo`) pero se PAUSÓ a los 503/6,600 porque PINCALI empezó a responder HTTP 202 (soft-block) en ~98% de las últimas peticiones — el volumen de la sesión (scraping de 165 colonias + este re-enrich) agotó la cortesía del portal. Queda pendiente retomar cuando el rate-limit se enfríe.

**Scraper on-demand de colonias débiles:** recalculadas con el umbral real del motor (≥5 comparables con edad, no "0 con edad" como se contó la sesión pasada) → 165 colonias, no 86. Corrida en 3 tandas (94+7+64 colonias) porque el proceso en background se mató solo ("killed") dos veces sin traceback ni causa identificable en el código — posible límite del harness, no se investigó a fondo. Resultado: 334 comparables nuevos entre NOCNOK/CASAS_Y_TERRENOS/PROPIEDADES_COM, la mayoría enriquecidos con año en la misma sesión (usando el `min_id` scoped agregado la sesión #2 de esta noche).

**Ixtepete — comps de zonas lejanas, causa real en el motor (no solo el panel del reporte):** el usuario reportó que seguían saliendo comparables de Tesistán/Valle Imperial/Unidad Díaz Ordaz/Villas la Loma/Arenales Tapatío en un avalúo de Villas del Ixtepete. Investigado: el bug ya estaba documentado y arreglado en `mongo_comparables.py` (el panel de "comparables reales" que se muestra en el reporte), pero ese fix NUNCA se portó al motor de valuación real — `buscarCompsConWeb` (`motor_remi_api.js`) deja que DeepSeek extraiga comparables de snippets de Google sin validar la colonia que devuelve contra el sujeto o sus similares, así que cualquier ruido del buscador (colonias chicas con poco contenido propio hacen que Google devuelva resultados de todo el municipio) entraba sin filtro geográfico alguno a la valuación. Se agregó un guardia `normCol()` contra sujeto+similares, mismo patrón que ya existía en el otro archivo.

**Ads:** el video de la campaña de slot1 no se veía (usuario: "solo sale el counter y el botón de WhatsApp") — causa: `autoPlay` + `muted={false}` en el `<video>`, que los navegadores bloquean (política de autoplay), dejando el video en negro mientras el overlay (contador, CTA) se renderiza normal por no depender del video. Fix: arranca muted con botón toggle de sonido. También se bajó la duración de slot1 de 60 a 30 segundos (frontend fallback + default del backend, que es el que manda cuando hay campaña activa).

**Equipamiento del reporte:** el usuario notó que faltaba un cuadro después de "plazas" — el dato de bancos (`nearby_places.py`, tipos `bank`/`atm`) ya se traía de Google Places, pero el reporte nunca le construyó una tarjeta (el grid tenía 4 categorías fijas + plazas suelta al final). Agregada `bancos_card` con el mismo patrón que plazas.

**Gamificación:** el usuario pidió "divertido" — se agregó un sonidito (dos tonos ascendentes con Web Audio API, sin archivos de audio) y 20 combinaciones de emoji+texto elegidas al azar al sumar una propiedad verificada (antes siempre el mismo "+1 🎯"). Se agregó también el conteo de "avalúos ganados" (`total // 150`) junto a Récord y Meta — al investigar dónde se veía este premio en el saldo de créditos real del usuario, se encontró que **no está conectado a nada**: `session.credits` es solo el saldo del plan pagado mensual, completamente separado del sistema de puntos. El "🏆 ¡Vas por el premio del trimestre!" es puramente decorativo hoy. Se dejó anotado como pendiente de decisión, no se integró (requeriría lógica backend de acreditación idempotente, fuera de alcance de esta sesión).

**Edades — Preventa:** agregada la opción "Preventa (aún no construida)" al selector de edad, con un campo de año probable de terminación a futuro (hasta +6 años) en vez de forzar una edad cronológica que la propiedad no tiene todavía.

**Bug de UX del filtro de colonias:** el usuario reportó que el combo de colonias del verificador "ya no aparecía todas, solo las que inician con A". Investigado: el componente (`ColoniaCombo`) siempre tuvo un tope fijo de 30 items — invisible mientras los municipios tenían menos de 30 colonias distintas. Al crecer la base con el scraping de hoy, municipios grandes (Zapopan, Guadalajara) ya pasan de 30, exponiendo por primera vez el tope (la lista viene ordenada alfabéticamente del backend, así que siempre se veían las primeras — "la A"). Subido el tope a 300 (sin costo de performance real: es una lista simple sin cálculo pesado, aislada del resto de la página) y el aviso "+N más — sigue escribiendo" se hizo mucho más visible (antes gris casi invisible).

Commits: `80efa8d` (m2c PINCALI autoritativo), `8ae0c42` (video ads + duración + bancos + guardia Ixtepete), `36e2d19` (título PINCALI español + guardia $/m²), `3ac523f` (sonidito + 20 animaciones gamificación), `8778746` (preventa + año terminación), `04a99f9` (avalúos ganados + fix filtro colonias). Todos pusheados.

---

## 23 Jul 2026 (noche #5) — Motor JS mejorado (piso espejo + limpieza masiva PINCALI) + NSE nuevo/usado cerrado (8 variantes) + hallazgo de mecanismo del caché

Sesión arrancó retomando el blocker NSE nuevo/usado de la noche anterior. Se confirmó con Mongo real (vía `railway run`) que el fenómeno de mercado es real (nuevo +16-19% mediana vs usado en colonias con volumen), pero el motor JS ya subvalúa en la mediana (-11.3% baseline) y el único punto de inyección probado hasta entonces (un techo/cap) es unidireccional — solo puede bajar el valor. Probadas 3 variantes de cap-mirror (forzado ciego a edad, subject-aware, umbral n≥8) — las 3 regresan el validador ~7pp por igual.

Se exploró la ruta "estilo portal" (mediana simple de N comps más cercanos, sin cascada NSE/IDX/Ross-Heidecke) — medida y descartada: pierde en los 5 criterios contra el motor actual, ni siquiera agregando factor de conservación o negociación por separado cierra la brecha. Confirma que la cascada gana su lugar, no es sobre-ingeniería.

Se aplicaron y validaron los 3 casos de referencia (Cuarzo/Ixtepete/La Calma) contra el motor JS directamente (specs sacadas de Mongo `valuations` real vía `railway run` — son avalúos del pipeline de reportes Python, alimentarlos al motor JS fue solo de referencia). Investigando por qué el piso-techo no cambiaba nada en Cuarzo se encontró que la celda de "Bosques de la Victoria" (depto, Guadalajara) solo tenía 9 comparables en el caché del motor mientras Mongo real tiene 62 registros de esa colonia — el caché estaba desactualizado, no había bug de normalización (`normCol()` fusiona bien las variantes de mayúsculas). Al revisar esos 62 registros se encontró un bug de datos nuevo y grande: 13 de ellos (y, escalando la búsqueda a TODO el caché, 744 registros casa/depto en total, 95% PINCALI) tienen `m2c` mal capturado en listados de preventa — departamentos de 5-55 m² a precios de millones de pesos, dando $/m² físicamente imposibles (hasta $60 millones por m²). Root cause probable: la extracción de PINCALI confunde número de unidad/modelo con metros cuadrados en ciertos formatos de ficha de preventa.

El primer intento de arreglar solo Cuarzo (agregar comps limpios + quitar los rotos, luego correr `build_cache_index.js` completo) pareció regresar el validador global (98→96 en ±10%, afectando incluso OPIs de colonias sin relación como Tonalá/Tlajomulco/Zapopan) y se revirtió por la regla dura del proyecto. Pero al investigar el mecanismo se encontró la causa real: reconstruir `cache_index.json` completo desde el `cache_consolidado.json` actual, AUNQUE NO SE CAMBIE NINGÚN DATO, ya reproduce esa misma "regresión" — el índice commiteado en git ya estaba desalineado del consolidado commiteado (deriva acumulada de sesiones/enrichers previos que nunca disparó un rebuild completo). La solución real: parchar la celda específica directo en `cache_index.json` (recalculando solo esa colonia), sin correr el builder completo — así se evita arrastrar toda la deriva no relacionada. Con esta técnica, el fix de Cuarzo se aplicó limpio (validador idéntico al baseline, Cuarzo en el motor JS de $2.83M a $1.94M) y la limpieza de los 744 registros rotos a escala (309 celdas) mejoró las 4 métricas del validador sin ninguna regresión (98→103 en ±10%, 124→131 en ±15%, 147→152 en ±20%, errAbs 15.0%→15.2%).

Con el nuevo mecanismo entendido, se retomó el split NSE nuevo/usado desde ambos ángulos posibles: como cap (probado con corte de 2, 5 y 6 años — negativo en los 3, mismo patrón, confirma que el problema es el mecanismo unidireccional, no el umbral de años) y como filtro de SELECCIÓN de comparables (metido en el mecanismo de franja de edad ya existente #121b, probando corte binario nuevo/usado de 2/3/4/5/8 años a pedido del usuario) — los 5 cortes dieron resultados prácticamente idénticos entre sí y casi iguales al baseline, porque el mecanismo de franja requiere ≥5 comps de la MISMA colonia Y la MISMA franja de edad para activarse, condición que casi nunca se cumple en los 207 OPIs. Conclusión final tras 8 variantes probadas en total (5 en sesiones previas + 3 hoy con distintos cortes): el blocker no es de fórmula ni de umbral, es de VOLUMEN DE DATOS por colonia — no hay con qué segmentar. No reintentar sin antes subir la cobertura de edad por colonia (scraper dirigido, ya identificado en sesiones previas).

Aparte: se sumaron los avalúos completados de junio y julio 2026 al validador (cerebro 751→1000), investigando y confirmando que la mayoría de folios "No hallado" son avalúos genuinamente sin terminar por el perito (fórmula `#DIV/0!`, tabla de comparables vacía — verificado directo en la hoja de Sheets), no bugs del extractor; unos pocos sí eran cuota de Google Sheets saturada, recuperados con reintento. También llegó un email de error de deploy de Render (`valuation-ai-1`, backend Node viejo de respaldo) — diagnosticado (sin confirmar con logs) como probable causa `puppeteer` en el build, el usuario decidió dejarlo en pausa por ahora.

Commits: `052d029` (fix Cuarzo sin regresión), `1a87870` (piso espejo graduado), `e36b2a6` (limpieza 744 registros), `b391518` (infra menor), `6e43010` (backup sesión). Todo pusheado a `origin/main`.

## 23 Jul 2026 (noche #3) — Video de anuncios arreglado + investigación profunda caso Cuarzo (causa raíz real)

Sesión arrancó con reporte del usuario: el video de anuncio en los slots del avalúo no salía, solo el texto de fallback. Investigación: no había ningún creativo de VIDEO aprobado en el sistema (14 creativos existentes, todos imagen). `AdOverlay.jsx` (componente real en producción) estaba bien codeado con lógica correcta de video/imagen/fallback; `AdRenderer.jsx` resultó ser código muerto sin usar en ninguna página. Se subió un video de Remotion (`propvalu-emo-horizontal.mp4`) a las campañas de slot1 y slot2 del anunciante "avaluos y arquitectura" (mismo usuario, cuenta separada con otro email), vía token de sesión generado directo en Mongo (sin tocar contraseña) porque las credenciales que el usuario recordaba no funcionaron.

Al agregar un volumen persistente de Railway (pedido explícito del usuario para que el upload sobreviva deploys — el storage era efímero, correcto que lo pidiera) se disparó un redeploy automático que BORRÓ todo lo que había en `/uploads/ads` sin backup, incluyendo 14 creativos de imagen de otras campañas del mismo anunciante. Se repusieron los videos (los tenía localmente) pero las imágenes viejas se perdieron — anotado como pendiente si el usuario tiene copias. Lección para la próxima vez: antes de adjuntar un volumen nuevo a un path con archivos existentes, respaldar el contenido primero (Railway monta el volumen vacío, no copia lo que ya había).

El usuario notó que subí el mismo video 2 veces (por el incidente del volumen) y preguntó por qué no había compresión automática — resultó que el "achicador" que recordaba (`compressFile.js`) solo cubre imágenes/PDF, nunca video. Se agregó `_compress_video()` en `backend/routers/ads.py` (ffmpeg, 720p CRF26 faststart) + ffmpeg al Dockerfile — probado en vivo: 12.2MB→1.5MB. Desplegado (`railway up` manual, no hay auto-deploy desde GitHub en este proyecto).

De ahí la sesión giró al pendiente real: usar los comps nuevos del enricher en el avalúo Cuarzo, segmentando por edad. Se validó primero que el enricher scoped de la sesión anterior sí terminó (401 docs, 0 errores) y que los datos nuevos están en Mongo correctos (605 docs tocados, sin colonias vacías) — pero NO forman parte del NSE/IDX (snapshot precalculado, no vivo). Se construyó una metodología manual (segmentar nuevo/usado por edad + homologar con Ross-Heidecke) que dio ~$2.29M para Cuarzo, dentro del rango que el usuario esperaba ($2-2.5M).

El usuario pidió guardar esa metodología para un LAB y compararla contra el validador — ahí surgió el hallazgo importante: ya existía un experimento casi idéntico (`#101(a) PER-COMP POR EDAD`, medido 17-jul, "NEUTRO-NEGATIVO", descartado), pero resultó ser una fórmula distinta (lineal) a un experimento hermano que SÍ estaba validado positivo y nunca se había mencionado en esa conversación: `LAB_EDADSEG` (bucket 3-escalones, medido 06-jul, +2pp ±15/cero regresión, "lista para pagar cuando suba cobertura de edad"). Se re-corrió el validador de 103 OPIs con datos de hoy: reconfirmó el +2.4pp ±15 sin regresión. Se agregó Ross-Heidecke como variante nueva del mismo flag (`LAB_EDADSEG_MODE=rh`) y se midió contra el mismo validador — perdió en todos los K probados, descartada.

El usuario pidió entonces correr el motor "como debería, sin atajos" en 3 casos reales ya guardados en producción (Cuarzo, Ixtepete, La Calma) — encontrados vía Mongo por folio/dirección. Resultado sorpresa: Ixtepete y La Calma dieron valores cercanos a un cálculo manual equivalente (−3%, −10%), pero Cuarzo real dio $3,931,206 — 42% arriba de mi estimado manual. Investigando el código real (`calculate_valuation` en `server.py`), se descubrió que el pipeline de reportes reales es **código Python completamente separado** del motor JS validado con los 103 OPIs — nunca se benefició de `LAB_EDADSEG` ni de nada de lo medido esa tarde. La causa raíz: `mongo_comparables.py::_similarity_score()` nunca usa edad en el ranking de comparables, solo m² y precio (precio inactivo en el caso normal sin valor de referencia previo). El pool de Bosques de la Victoria tiene mediana $64,511/m²C en crudo, dominado por obra nueva PINCALI.

Se probaron 2 fixes de bajo riesgo (ninguno tocó prod sin medir primero): (1) portar la homologación EDADSEG a `calculate_valuation` — NO-OP, los comps guardados casi no traen edad; (2) agregar edad al `_similarity_score` — empeoró (subió el precio, no bajó), por la misma razón de cobertura. Ambos descartados y documentados en `MOTOR_ANTECEDENTES.md`. Conclusión: el problema requiere un filtro DURO nuevo/usado (categórico), no un reajuste de pesos — reconfirma que el NSE nuevo/usado sigue siendo el blocker real, ahora con evidencia directa del pipeline de reportes, no solo del motor de validación.

Se aplicó un fix real de bajo riesgo sin necesitar medición: `land_area` en `ValuationForm.jsx` ya no defaultea a `construction_area` para departamentos sin terreno capturado — ahora defaultea a 0 (el fallback viejo asumía que CUALQUIER depto, sin importar el piso, tenía terreno propio igual a su construcción completa, absurdo salvo para plantas bajas con patio real — caso que el usuario confirmó que existe y por eso se agregó el fallback originalmente, pero de forma demasiado amplia). Commit `db7d218`, pusheado.

De paso, corregido un error propio: se re-flageó como "hallazgo nuevo" que el 75% de docs PINCALI históricos están en inglés — el usuario aclaró (molesto, con razón) que eso ya se sabía y ya se había corregido hacia adelante hace varias sesiones, documentado en `feedback_pincali_solo_espanol`. Se actualizó esa memoria con una nota explícita para no repetirlo: el idioma en sí no es el problema, solo cuando trae datos mal mapeados.

---

## 23 Jul 2026 (noche) — Validador post-merge, enricher scoped por lote (min_id), research IMEPLAN Zoom
Continuación de la sesión de la mañana (bug SEPOMEX + scraper 86 colonias). Primero se corrió el validador offline (`validar_40_opis.js`) sobre el maestro ya con el merge SEPOMEX aplicado: 34 OPIs candidatos, ±10% 61.8%, ±15% 79.4%, ±20% 88.2%, error abs 10.0%/mediana -7.3% — dentro de rango normal, sin baseline pre-merge guardado para un antes/después directo (el pendiente lo tenía anotado como "400 OPIs", pero el script real solo genera 34 candidatos con los filtros actuales; el nombre en el backlog estaba desactualizado, no el resultado).

Se revisaron los resultados reales de los 2 scrapes corridos en la mañana (`colonias_debiles_progreso.md` + `ixtepete_calma_progreso.md`): ~301 comps nuevos insertados en `mercado_props` (41 del cluster La Calma/Ixtepete, ~260 de las 86 colonias débiles). 39 de las 86 colonias débiles terminaron en 0 comps nuevos, pero casi todos por "0 resultados" del scraper (el portal no tuvo match para esa colonia+tipo en ese momento) — el propio log de la corrida confirma "ninguna bloqueo hasta ahora", así que no fue anti-bot.

Al preguntar si esos comps nuevos ya estaban enriquecidos (año de construcción, etc.), se encontró que **no** — `insertar_comparables_ondemand.py` reusa el chokepoint de guardado (`_guardar_en_mongo`, mismo que usa `scheduler.py`) pero nunca dispara el auto-enricher, porque ese trigger vive dentro del loop normal de scraping de `scheduler.py`, no dentro de `_guardar_en_mongo`. Los docs nuevos sí traían `m2_construccion` (lo pone `buscar_comparables_browser.js` directo al guardar) pero `anio_construccion` solo cuando el portal lo exponía en el listado mismo (PINCALI/NOCNOK a veces, CasasYTerrenos nunca) — sin pasar por el enricher, que sí visita el detalle de cada propiedad.

Correr el enricher completo por portal (`enricher.py --tab X --mongo`) habría barrido TODO el backlog pendiente de cada portal en orden de inserción (los más viejos primero), tocando miles de props ajenas a esta tarea — contraviene la regla dura del proyecto de nunca hacer scraping/validación online en lote fuera de scope. En vez de eso se agregó un parámetro opcional `min_id` (ObjectId) a `obtener_props_mongo` y `enriquecer_mongo` en `enricher.py` — default `None` preserva el comportamiento existente para todo caller previo, pero permite acotar una corrida a solo los docs insertados después de cierto punto. Verificado antes de correr en real: 407 docs pendientes detectados en los 4 portales del batch (CASAS_Y_TERRENOS 205, PROPIEDADES_COM 195, NOCNOK 2, PINCALI 5), sin tocar backlog viejo. Corrida lanzada en background con Monitor persistente; CASAS_Y_TERRENOS terminó (201 enriquecidas, 0 errores) antes del cierre de sesión, PROPIEDADES_COM quedó en curso (~85/195), NOCNOK y PINCALI pendientes de arrancar.

En paralelo se investigó el pendiente de IMEPLAN Zoom (`Modulo Drive IA/FUENTES_EXTERNAS_METROPOLITANAS.md`, punto 4 del backlog original): confirmar si expone un endpoint REST/WMS/WFS antes de asumir que hace falta scraping visual del visor. Inspeccionando el bundle JS Angular del sitio (`zoom.imeplan.mx`) se encontraron dos bases: un backend REST propio (`zoom.imeplan.mx/rest/v1/`, Django REST Framework browsable, sin auth) que expone el árbol completo categoría→tema→subtema→capa con el nombre real de cada layer de GeoServer, y el propio GeoServer (`geoserver.imeplan.mx/geoserver/sigmetro`, con espejo en `geoserver2`) sirviendo WMS y WFS 2.0.0 públicos sin login. Se confirmó en vivo descargando una capa real: "Estrategia POTmet 2024 → Zonificación Primaria" = `sigmetro:vwZonificacion_primaria_POTmet_2024`, 10,590 polígonos del AMG con clasificación de uso de suelo (área urbanizada/urbanizable/no urbanizable, con subclasificación hasta "Conservación", "Aprovechamiento rústico", etc.) — dato de riesgo/restricción legal por ubicación que el motor hoy no captura en absoluto.

El mismo WFS resultó tener un catálogo mucho más amplio de lo que la sesión de la mañana había concluido (esa sesión había cerrado con "solo IIEG vale la pena integrar" tras revisar el visor estatal de Jalisco, que sí está pobre en capas). Revisando el `GetCapabilities` completo se identificaron, entre decenas de capas POTmet: índice de marginación urbana por AGEB/localidad (señal NSE independiente de los comps de mercado, potencialmente útil para colonias sin datos), riesgo industrial/químico (`vwARQuimico*`, exactamente el caso de vivienda conviviendo con industria peligrosa), islas de calor y proyecciones de temperatura a 2039/2069, densidad poblacional por manzana (`AMG_densidad_2020_sm`), e infraestructura de movilidad muy completa — estaciones de transporte masivo, cobertura real de todas las rutas de camión activas del AMG, infraestructura ciclista, y un indicador ya resuelto de "suficiencia de transporte público por zona". Esto último se identificó como el hallazgo de mayor impacto potencial: distancia/suficiencia de transporte masivo es un factor de plusvalía real que hoy no existe en el motor de ninguna forma. Todo quedó documentado con URLs exactas y ejemplos de query en `FUENTES_EXTERNAS_METROPOLITANAS.md`, sin tocar el pipeline — es research, no implementación.

Adicionalmente se identificó INEGI DENUE (directorio de negocios/equipamiento urbano) como alternativa gratuita a Google Places para el conteo de POIs, pero requiere un token de registro que no se probó en vivo (el endpoint sin token devolvió 404, no se confirmó la sintaxis correcta de la URL con token).

## 23 Jul 2026 — Bug sistémico SEPOMEX corregido y aplicado, scraper ampliado a 86 colonias débiles + La Calma/Ixtepete
Arrancó de la sesión previa, que había flagueado (sin corregir) un bug sistémico en `generar_similares_sepomex.js`: leía `sepomex_jalisco.json`, un catálogo viejo con 1 sola entrada por nombre de colonia normalizado, que colisiona cuando el mismo nombre existe en 2+ municipios/estados (medido: 15.5% de los nombres). Al investigar a fondo para corregirlo, se encontró que el diagnóstico apuntaba al script equivocado: `generar_similares_sepomex.js` sí carga `sepomex_jalisco.json` en una variable (`SEP`), pero **nunca la usa** en ningún otro lugar del archivo — es un import muerto, su matching real es 100% vía `cache_index.json` (pm2c por municipio+tipo). El culpable real es `enriquecer_colonias_ia.js`, que usa DeepSeek+Gemini para generar pares de colonias similares por NSE y sí depende de `sepomex_jalisco.json` para enumerar las colonias residenciales de cada municipio antes de mandarlas al LLM — ahí es donde la colisión de municipio realmente contamina el resultado (ej. "Loma Bonita" resolvía a Tecomán/Colima en vez de Zapopan).

El fix fue mecánico una vez identificado el archivo correcto: cambiar la fuente a `sepomex_v2.json` (que sí preserva TODAS las entradas por nombre como un array, construido por `construir_sepomex_v2.js` en una sesión anterior pero nunca conectado al pipeline de similares) y agregar `.flat()` antes de filtrar por municipio, ya que ahora cada key del catálogo es un array de candidatos en vez de un solo objeto. No hizo falta ninguna lógica de desambiguación adicional para colisiones DENTRO del mismo municipio (ej. "Loma Bonita Ejidal" vs "Loma Bonita Sur" en Zapopan) — son colonias reales distintas con nombres normalizados distintos, no una colisión, así que filtrar por municipio ya las trae correctamente a todas. Se limpió también el import muerto en `generar_similares_sepomex.js` para no confundir a futuras investigaciones. Verificado con dry-run antes de gastar ningún crédito de API: +52 a +93 colonias residenciales visibles por municipio AMG (Zapopan 610→703, Guadalajara 370→429, Tlaquepaque 193→251, Tonalá 288→340, Tlajomulco 228→286), y el caso concreto de "Loma Bonita" en Zapopan confirmado correcto.

Con el fix verificado, se corrió `enriquecer_colonias_ia.js` real (no dry-run) para los 7 municipios AMG — proceso de ~50 min con llamadas DeepSeek (primaria, 100% de cobertura) + Gemini (validación cada 2 chunks, con errores transitorios de rate-limit/fetch que el catch absorbe cayendo a solo-DeepSeek sin pérdida de datos). Resultado: `colonias_similares_enriquecido.json` con 2,349 colonias con similares, 15,756 pares nuevos. Una muestra de calidad (30 pares de 6 colonias) confirmó que la gran mayoría respeta la metodología (mismo CP o CP vecino), con algún outlier ocasional donde el LLM iguala por nombre/tipo ignorando distancia real de CP — mismo nivel de imperfección que ya tenía el pipeline en producción, no una regresión.

El paso final — aplicar este archivo regenerado a `colonias_maestro.json`, que es lo que el motor realmente lee — casi se hizo de la forma equivocada. El flujo "oficial" es `construir_maestro.js`, que hace un rebuild COMPLETO desde 6 archivos fuente. Antes de correrlo contra producción, se probó en una copia lab (`colonias_maestro.lab.json`) y se comparó: el rebuild completo **pierde 647 colonias con similares** que existen en el `colonias_maestro.json` de producción pero nunca se alimentaron de vuelta a ninguno de los 6 archivos fuente — acumulado de años de ediciones in-place directas sobre el maestro (scripts como `backfill_cp_maestro.js`, y las ediciones manuales de esta misma sesión y de la sesión anterior con los similares de Bosques de la Victoria). Ejemplos concretos medidos: "Jardines de Ixtepete" pasaba de 14 similares a 0, "Villas del Ixtepete" de 9 a 1, "La Calma" de 9 a 3. Esto confirma y cuantifica por primera vez la regla que ya estaba anotada ("Motor: NO reconstruir el caché a ciegas") — no es una preocupación teórica, es un patrón real y medible de divergencia entre producción y el pipeline de build.

La solución fue escribir un merge ADITIVO nuevo (`merge_simIA_a_maestro.js`) que nunca reemplaza ni quita nada existente — solo hace UNION de los pares de similares nuevos con los que ya había, y agrega colonias completamente nuevas cuando no existían. Probado en dry-run primero (51 colonias nuevas, 1,805 ampliadas, 11,852 pares, cero pérdidas verificadas contra los casos problemáticos de arriba) y luego aplicado real con backup previo. Commit `6bd75c9`, pusheado.

En paralelo a esta investigación, el usuario pidió agregar manualmente "La Calma" y aclarar la fragmentación de "Ixtepete" en el maestro: resultó que "Jardines del/de Ixtepete" y "Villa/Villas del Ixtepete" eran la misma colonia real partida en 2 entradas por una variante de nombre ("del" vs "de", singular vs plural) entre distintas fuentes (una con datos de mercado reales, otra con similares generados por IA) — se fusionaron preservando ambos, verificando el nombre oficial contra SEPOMEX antes de decidir cuál mantener como key canónica. "La Calma" se amplió con 6 similares que el usuario conocía de la zona (El Colli Ejidal, El Colli Urbano 1a, Arboledas 1a Secc, Loma Bonita, Las Águilas, Pinar de la Calma), cada uno verificado contra `sepomex_v2.json` filtrando por municipio=Zapopan antes de asumir cuál de los homónimos era el correcto — mismo patrón que el fix sistémico de arriba, aplicado a mano.

También se completó, en background durante toda la sesión, el trabajo pendiente de la sesión anterior: los 3 enrichers de año (PROPIEDADES_COM, INMUEBLES24, NOCNOK, CASAS_Y_TERRENOS) terminaron sin bloqueos (CASAS_Y_TERRENOS con 1,906/2,000 enriquecidas, 94 errores normales de listados caídos), y el scraper on-demand se amplió a las 86 colonias débiles de `colonias_debiles_scraper.md` (identificadas en la sesión del 22-jul-c) más el cluster de La Calma/Ixtepete y sus nuevos similares — 96 corridas en total, con throttle de 8-15s entre colonias para no sumar carga a los enrichers corriendo simultáneamente sobre los mismos portales.

Un incidente de proceso vale la pena anotar: al notar que el loop de scraping de las 86 colonias estaba generando demasiadas notificaciones de "sigo esperando" sin trabajo real (un agente re-invocándose por cada evento de su propio Monitor, ~64k tokens por ciclo sin avanzar), se lanzó un segundo agente para corregirlo — pero ese segundo agente, en vez de inspeccionar y arreglar el loop YA corriendo, construyó y lanzó uno nuevo desde cero, duplicando el trabajo. Se detectó por timestamps repetidos en el log de progreso (colonias #7, #8, #9 procesadas dos veces cada una) y se mató el proceso duplicado a tiempo (`taskkill /T /F`) — sin daño real gracias al dedup por `id_unico` en Mongo, solo 3 colonias con carga extra innecesaria sobre los portales.

Por fuera del trabajo de colonias, el usuario compartió 5 fuentes externas para evaluar como posible insumo de avalúos (equipamiento, calificación de zona, seguridad): IIEG Jalisco (Datos Abiertos, nivel colonia/AGEB, descargable — la única con valor real de integración), el visor `mapa.jalisco.gob.mx/mapa/zapopan` (revisado en vivo con el navegador Chrome del usuario — capas de cartografía base/ambiental/salud/ordenamiento territorial, sin equipamiento/uso de suelo/seguridad como capas nombradas), MapaLab (mismo organismo IIEG, no revisado a fondo), un post de X (no se pudo cargar), y AlertaRoja (reportes ciudadanos de seguridad en tiempo real — útil solo como consulta manual puntual por caso, no como fuente sistemática por falta de datos descargables y granularidad de colonia).

## 22 Jul 2026 (d) — Scraper on-demand por colonia arreglado, aplicado al caso Cuarzo/Bosques de la Victoria
Arrancó de la sesión previa: `buscar_comparables_browser.js` (el scraper on-demand por colonia, pensado para llenar el hueco en las 86 colonias con pool débil) daba 0 resultados o "nav-error". Diagnóstico con pruebas reales (no narrativa): usaba `agent-browser` (Chrome headless) sin Chrome instalado — cada intento de abrir una página colgaba 20s y el catch lo tragaba silenciosamente, así que SIEMPRE devolvía vacío. Se instaló Chrome, pero incluso así los 3 portales que usaba el script estaban mal enfocados: Inmuebles24 bloquea con Cloudflare a cualquier navegador headless simple (el scraper real, `inmuebles24.py`, usa Playwright con browser fresco por página — más pesado de lo que vale la pena para un script on-demand, se dejó fuera); CasasYTerrenos usaba una URL HTML inventada que da 404 real (el scraper real pega directo a la API MeiliSearch); Propiedades.com con navegador headless dispara el reto de Akamai (`ERR_HTTP2_PROTOCOL_ERROR`) — el scraper real usa fetch nativo de Node sin Chrome. Se reescribió el script completo reusando exactamente el método que cada `scrapers/*.py` ya tenía probado: PINCALI y NOCNOK con requests simple (confirmado que PINCALI bloquea navegador pero no requests con UA real), CasasYTerrenos con la API MeiliSearch (con filtro `neighborhood` server-side, case-insensitive, confirmado con pruebas reales — mucho más preciso que filtrar cliente-side sobre un listado municipal completo), Propiedades.com con fetch nativo de Node. Se descubrió además que Propiedades.com expone una URL de listado directo POR COLONIA (`propiedades.com/{colonia}-{municipio}/tipo-venta`) — confirmada real con dos pruebas independientes (Independencia-Guadalajara, Seattle-Zapopan) y con 404 real (no fallback silencioso) cuando el slug no existe, así que se volvió el método principal para ese portal.

El usuario recordó que ya había pasado los links de comparables de Cuarzo (el avalúo de Calle Cuarzo 2380, Bosques de la Victoria, depto ~45 años) tres veces en sesiones distintas sin que se guardaran — quedaron solo como "pendiente: pedir al usuario" en el cierre de sesiones previas. Se corrigió guardándolos de inmediato en `Modulo Drive IA/CUARZO_BOSQUES_VICTORIA.md` y se agregó memoria de feedback sobre esto. Con el scraper arreglado se corrió sobre Bosques de la Victoria (m²=75, departamento venta) y salieron 8 comparables reales. Al revisar el estado real en Mongo antes de insertar, se encontró que la premisa de sesiones anteriores ("BV tiene ~0 deptos de venta") estaba obsoleta: la colonia ya tenía 35 documentos depto-venta, de los cuales ~28 son PINCALI con `anio_construccion=2026` (obra nueva/preventa) — confirma que el problema real no es falta de datos sino que el pool está inundado de obra nueva, exactamente el caso que justifica el split de NSE nuevo/usado ya aprobado en sesiones previas mais nunca implementado.

El usuario pidió ampliar a las 16 colonias "similares" de Bosques de la Victoria que ya vivían en `colonias_maestro.json`, resolviendo el municipio de cada una en vez de asumirlo. Dos de las 16 ("Loma Bonita", "La Estancia") resolvían mal en el maestro — a Tapalpa y Tala, pueblos a ~100km, por colisión de nombre con otro municipio del mismo nombre. En vez de omitirlas o adivinar el municipio correcto, se verificó contra `sepomex_v2.json` (el catálogo SEPOMEX que preserva TODAS las entradas por nombre de colonia, a diferencia del viejo `sepomex_jalisco.json` que solo guarda una) — ambas tenían coincidencia exacta en Zapopan, y se corrieron ahí. Esto llevó a descubrir un bug sistémico más grande: `generar_similares_sepomex.js` (el script que construye el campo `similares` de TODA colonia en `colonias_maestro.json`) lee el catálogo viejo colisionable, no el nuevo. Medido: de 6,542 nombres de colonia en Jalisco+Nayarit+Colima, **1,017 (15.5%) tienen 2+ municipios/estados distintos** con el mismo nombre — potencialmente contamina el campo `similares` de cualquier colonia del sistema, no solo BV. Documentado, NO corregido (requiere migrar el script + decidir desambiguación cuando hay 2+ candidatos en el mismo municipio + regenerar y re-validar el maestro completo — hilo de trabajo aparte).

Se construyó `insertar_comparables_ondemand.py` para subir los comparables encontrados a `mercado_props` PROD, reusando el chokepoint real de guardado (`scheduler._guardar_en_mongo`) en vez de reinventar el upsert/dedup. Al revisar por qué el enricher de año solo encontraba 9 propiedades pendientes de Propiedades.com (quedando 0 enriquecidas), se descubrió un bug propio recién introducido: `buscarEnPropiedadesCom` usaba la URL de la página de LISTADO (compartida por toda la corrida de una colonia) como `url_original` de cada comparable, así que `id_unico = md5(url)` salía idéntico para todos los comps de esa colonia+tipo — cada upsert pisaba al anterior y solo sobrevivía el último. Afectó 9 colonias. Se corrigió capturando el href real de cada anuncio (`/inmuebles/...`) por tarjeta; con autorización del usuario se borraron los 9 documentos corruptos (verificados 1 a 1 por URL exacta antes de borrar, sin tocar datos legítimos) y se re-corrieron las 9 colonias con el fix — se recuperaron 74 comparables reales que antes se habían perdido silenciosamente. Verificado al final: 0 documentos sospechosos restantes en 18,621 docs de Propiedades.com.

Se lanzaron enrichers de año en background para los 4 portales relevantes (CASAS_Y_TERRENOS, PROPIEDADES_COM, NOCNOK, INMUEBLES24), acotados por `--max` para no correr indefinidamente. PROPIEDADES_COM terminó dentro de la sesión (74/74 enriquecidas, 0 errores); los otros 3 seguían corriendo al cierre — son procesos de sistema independientes (`disown`), sobreviven al cierre de la sesión de Claude Code. Commit `5b8243a` pusheado a `main`.

## 22 Jul 2026 (c) — Fix comparables de zona (Ixtepete) + equipamiento, DESPLEGADO; investigación a fondo del rebuild del motor
Arrancó de una queja real: un avalúo de prueba en la cuenta inmobiliaria de pedrucus (Villas del Ixtepete, Zapopan) mostró comparables de El Fortín (8km), Tesistán y Jocotán — colonias que comparten municipio pero están lejos, coladas porque `mongo_comparables.py` solo filtraba por banda de precio, no geografía. **Fix desplegado** (commit `8fe0946`, Railway): se conectó `Modulo Drive IA/_geo/proximidad.py` (centroides SEPOMEX, construido en junio y nunca usado) — ahora la función prioriza colonia exacta y solo amplía a colonias VECINAS GEOGRÁFICAS reales (`colonias_cercanas`, km_max 2.5→5), nunca todo el municipio. Verificado en prod: Villas del Ixtepete ahora trae Santa Ana Tepetitlán (0.0 km, mismo CP — ahí está el Aurrera que el usuario mencionó) en vez de colonias a 8km+. En el camino se detectó y corrigió un bug propio: nombres cortos genéricos ("Del Sur") hacían match por substring dentro de "Lomas del Sur" (Tlajomulco, ~13km) — se cambió a match exacto anclado. Cada comp ahora trae `zona_tag` visible en el reporte (repurpuso el slot `tenure_tag`, muerto para comps reales de mercado_props).

El usuario también reportó que el "Equipamiento y servicios" del reporte se sentía genérico y con errores: faltaba la Preparatoria 9, la "Escuela de Fútbol Atlas" salía aunque ya cerró, y no aparecían Merkabastos/plazas de Prol. Mariano Otero pese a estar cerca. Se corrigió en el mismo commit: `nearby_places.py` ahora pide `businessStatus` y filtra negocios cerrados; usa `rankPreference:POPULARITY` y muestra top 8 (antes 5) para que cadenas conocidas (Súper Bara) no queden tapadas por abarrotes chicos; el radio de búsqueda se amplió solo para educación (1500m) y plazas (2000m), ya que 800m dejaba fuera prepas y centros comerciales de avenida. Se encontró además un bug real: si Google Places fallaba TOTAL, el código dejaba los nombres INVENTADOS por Gemini como si fueran reales (probable causa de la "Escuela de Fútbol Atlas" fantasma) — ahora se limpian a "sin datos verificados".

El usuario pidió medir si valía la pena reconstruir el caché del motor (congelado desde el 7-jul) para que reflejara las correcciones del verificador. **Se midió con el validador offline (103 OPIs) y dio negativo en las 5 métricas** (±10 63.1→59.2%, ±15 75.7→70.9%, ±20 84.5→82.5%, errAbs 10.6→11.8%, mediana -7.1%→+8.8%) — confirma la decisión ya tomada el 13-14-jul de no reconstruir reactivamente. Pero el usuario cuestionó la explicación de "deriva de mercado" (con razón: no hay ventana de fecha en el builder, y sospechaba que se estaban descartando comparables válidos). Se investigó hasta el fondo con Mongo en vivo (solo lectura): del pool de 21,562 comps del 7-jul, **73.7% sigue existiendo hoy** (colonia+área); del 26.3% restante que "desapareció" de esa llave, **98.4% es PINCALI** — una muestra de 200 mostró 0% marcados duplicado/remate y 46% siguen activos y normales, confirmando que la causa es que **la corrección de m²/colonia del propio verificador en PINCALI cambia la llave de dedup `colonia+m²` del builder**, no que las propiedades se vendieron. Y el motivo de que eso mueva el pass-rate: los 18 OPIs (de 103) que cruzaron la línea ±10% son casi todos de pools chicos (n=3-7 comparables) — con tan poca muestra, que entre o salga un solo comp mueve la mediana 10-40 puntos. Caso verificado con datos reales: "Álamo Industrial" (San Pedro Tlaquepaque, sin ancla NSE propia) tiene solo 7 anuncios con precios de $10k a $43k/m²C. Caché restaurado al baseline del 7-jul, nada tocado en prod.

De esa misma investigación salió una lista accionable: **86 colonias con pool débil (n≤6)**, extraídas de `cerebro_datos.json` (municipio+colonia+n), guardadas en `Modulo Drive IA/colonias_debiles_scraper.md` como objetivo prioritario para la tarea pendiente de "scraper por colonia" (#1b) — son las que más se benefician de más comps reales, porque su pool actual es el más volátil. **Próxima sesión:** arrancar directamente con el scraper por colonia usando esa lista.

## 22 Jul 2026 (b) — Deploy de la rama + fix similares BV al maestro + dirección NSE nuevo/usado
Se **mergeó ff-only** la rama `fix/flujo-avaluo-reporte-jul20` (26 commits: flujo web, ads, reporte/PDF, motor Opción C + parche gap6) a `main` y se **desplegó**: backend Railway (deployment `0d42b1d8` RUNNING, health 200) + frontend Vercel (READY, alias `frontend-rosy-six-74`). **Fix real de Bosques de la Victoria:** las 16 colonias manuales que el usuario dio la sesión pasada estaban solo en el legacy `colonias_similares.enriquecido.v2.json`, que el motor **ignora** cuando existe `colonias_maestro.json` (fuente única consolidada). Se movieron al maestro donde `getSimilares`/`getNSE` sí leen (commit `1722bcc`, backup `colonias_maestro.PRE_BV_MANUAL.bak.json`). 11/16 sobreviven el filtro NSE±1; ancla BV = `nse.v1` medio-medio nseIdx 3 (n=90, robusto) — el idx casa=4 sale de solo 3 listados y NO se usa como ancla (`getNSE` prioriza v1). **Commit BV en main pero NO redesplegado** (vino después del deploy) → falta `railway up`. **Discusión metodológica:** el usuario llegó por su cuenta al root cause ya documentado en `MOTOR_ANTECEDENTES` (deptos NUEVOS inflan el NSE de la colonia por valor de nuevos no usados; casa≠depto; remodelado≠viejo). Confirmado: NO se arregla con motor (ya se intentó, regresa) sino con DATO (año/scraper). **Dirección aprobada:** separar NSE nuevo/usado × tipo en el mismo maestro (hipótesis línea 303), blocker = cobertura de año. **Próxima sesión (3 hilos paralelos):** (1) usuario da links directos de comparables de Cuarzo para ese avalúo; (2) arreglar scraper por colonias `buscar_comparables_browser.js` (URLs por colonia, anti-bot, +PINCALI ES +nocnok) + enricher PINCALI capture "Year Built = New/Preventa→año actual"; (3) lab `motor_simple` minimalista (mediana $/m²C de N comps cercanos × m²C, sin cascada NSE/IDX/Ross-Heidecke) vs motor actual con validador 400 OPIs.

## 22 Jul 2026 — Revisión a fondo flujo web + ads + valor físico + investigación comparables/scraper (rama, SIN desplegar)
Sesión larga de revisión del flujo público (form→comparables→reporte→PDF→gracias) con el usuario probando en localhost. **Flujo:** mapa centraba en CDMX por carrera async del init de Google Maps (fix `coordsRef`); botón "Paso anterior" en cabecera sticky + quitado el "Anterior" del cuadro blanco; modal informativo parpadeaba en refresh (`!user` true mientras `/auth/me` resolvía → gate `authChecked`); círculo cosmético "Comparables" en el stepper (form + comparables); auto-descarga PDF en Gracias (`autoDownload`) + botón "Ver reporte"; fotos 12→15 + auto-fachada atómica. **Ads:** popup excluyente pagada-ó-cajón (violaba DOCUMENTO_MAESTRO §139-155); AdOverlay fullscreen→popup adaptado a formato V/H; sin loop; cuenta arranca al `onPlaying`; no repite slot1 tras análisis. **Bug backend crítico:** `/uploads` servía 200 sin Range → Chromium no reproduce `<video>` (negro); fix ruta Range 206 + métricas BaseHTTPMiddleware→ASGI puro. Prueba: anunciante "Avaluos y Arquitectura" (staging) con Remotion en slots 1/2/3. **Reporte/PDF:** template es A4 pero generaba en carta (cortaba) → fix A4; régimen legible, orden min/medio/máx (invertido), ícono 🏁→🎯, "M.N.", fotos altura dinámica que llenan la hoja, fachada junto al mapa (fallback 1ª foto), 6º apartado "Bancos". **Valor físico #11/#12 RESUELTO:** `calculate_remi` deriva enfoque físico (terreno ratio + construcción paramétrica CMIC − Ross-Heidecke), lógica del `/calculate` viejo. Mapeo recámaras/baños/estac desde `mercado_props`. **Investigación Cuarzo (depto Bosques de la Victoria):** BV ~0 deptos venta en BD (1 renta PINCALI inglés); GDL 1547 deptos → hueco de COLONIA. **Tavily VIVA** (Serper muerta) encuentra ~19 deptos BV, pero el motor usa pool "exacta" y no dispara similares/Tavily. **PINCALI se scrapea del listado INGLÉS** (viola regla) → ~60% deptos sin año. `colonias_similares.v2` mayormente SEPOMEX geográfico (36% flacas); override manual similares BV (neutral). Scrape on-demand = `Modulo Drive IA/buscar_comparables_browser.js` (ubicado vía graphify; dio 0/nav-error). **Meta:** reclamo justo del usuario por re-derivar/grepear teniendo el índice → protocolo graphify-first en CLAUDE.md + ARQUITECTURA_MAPA + memoria `feedback_usar_graphify_ubicacion`. Rama `fix/flujo-avaluo-reporte-jul20` pusheada, **NO mergeada/desplegada**.

## 20 Jul 2026 — Fixes flujo de avalúo/reporte + folio nuevo + mapa de avalúos (DESPLEGADO)

Sesión de fixes reportados por el usuario probando prod. Todo commiteado, pusheado (`0215f1a`) y desplegado (frontend Vercel + backend Railway `railway up`).

**Mapa del formulario en blanco:** el env `REACT_APP_GOOGLE_MAPS_API_KEY` en Vercel estaba guardado como cadena vacía `""` → el script cargaba con `?key=` vacío y Google devolvía su error. Diagnóstico: la key no aparecía en el chunk `5777` (ValuationForm es lazy-loaded, no en `main.js`). Fix: **fallback hardcodeado** de la key en `ValuationForm.jsx` (es pública por diseño, prefijo `REACT_APP_`, protegida por dominio en Google Cloud, no por secreto). El usuario habilitó **Geocoding API** en `propvalu-mexico` (la ubicación caía al default CDMX 19.4326,-99.1332 porque el geocoding estaba deshabilitado). Nota: el classifier de modo auto bloquea `vercel env`/`deploy` de forma inconsistente e independiente del allowlist.

**Entorno del reporte con números REALES:** los "15+/10+ cercanos" los inventaba Gemini. Nuevo módulo `backend/nearby_places.py` que cuenta POIs reales vía **Google Places API (New)** `searchNearby` (la legacy no se puede activar; la New devuelve varios tipos por llamada → 5 requests/reporte). Radio **800m** (1500m saturaba todo en "20+"). Categorías: educación(school,university) · salud(hospital,pharmacy,doctor) · comercio(supermarket,grocery) · **recreación(park,gym,stadium = parques+deportivas)** · plazas(shopping_mall). Wire en `server.py generate_report` (sobreescribe `count` del perfil_entorno); fallback a estimado IA si Places falla. ~$0.16/reporte, bajo el crédito gratis de Google. El usuario habilitó Places API (New) + legacy.

**PDF del reporte a 4 hojas (recurrente):** el reporte se imprime con `window.print()` sobre `reportHtml` (footer "about:blank" lo confirma). Faltaba `@page`/`@media print` → el navegador metía sus márgenes default (~12mm) y la `.page` de 297mm no cabía → desbordaba a 6 hojas + márgenes laterales grandes. Fix: `@page { size:A4; margin:0 }` + reset de `.page` (margin/shadow/radius) en print. Verificado: el reporte tiene exactamente 4 divs `.page` → 4 hojas A4. **Plusvalía:** `bar_heights` 103→82 max (contenedor 110px) para que el `+%` de arriba no se salga del recuadro.

**Folio nuevo `EST-YYMMDD-TIPO-SIGLAS-NN`:** helpers `build_folio`/`user_siglas`/`tipo_code` en `report_generator.py`. Tipo IN(realtor)/PE(appraiser)/AD(super_admin)/PU(público) + 2 iniciales de empresa/nombre (stopwords y/de/la…) + consecutivo `folio_seq`. **Estable por avalúo** (se calcula una vez en server.py con `get_current_user`, se incrementa el contador del usuario y se guarda `valuation.folio`; al regenerar se reutiliza). Público: `EST-YYMMDD-PU-NN` (NN del valuation_id). Campos nuevos en `User`: `siglas`, `folio_seq`. **Dirección** de la propiedad tras el Folio en el header. **Nombre de archivo** del PDF = `<title>` = `PropValu {folio} - {addr_full}` (empieza con PropValu). Formato de folio confirmado con el usuario; contador = acumulado por usuario (reinicio por presupuesto comprado queda pendiente opcional).

**Reseñas/CTAs (`ReportPage.jsx`):** el guard comparaba rol `'valuador'`/`'inmobiliaria'` pero los reales son `'appraiser'`/`'realtor'` → el perito veía "califica valuador" y el modal en cada reporte. Ahora `isPro` (appraiser/super_admin/valuador) NO ve la CTA de calificar valuador ni la CTA de inmobiliarias (solo público); ambas CTAs solo en el reporte final (`!isGenerating && reportHtml`), no durante el cálculo. Modal "¡Gracias por tu reseña!" con `bg-white` (salía transparente porque `bg-background` no tiene la var de tema definida). Se agregó fetch de `currentUser` vía `/auth/me`.

**Página "gracias" (`ThankYouPage.jsx`):** intercambiadas las tarjetas — **calificar arriba, descargar abajo** (objetivo: que califiquen antes de bajar el PDF). Nombre de archivo (extraído del `<title>` de reportHtml) visible en la tarjeta de descarga.

**Mapa de mis avalúos:** usaba coordenadas FALSAS (centro del municipio + jitter senoidal), no la ubicación real. Ahora usa **lat/lng real** (`property_data.latitude/longitude`, agregadas a los normalizadores de ambos dashboards), **centrado en el avalúo más reciente**, y **click abre un modal grande** interactivo (drag + zoom) con **filtro por tipo de propiedad**. Nuevo componente compartido `frontend/src/components/MapaAvaluos.jsx`, usado en el resumen del dashboard de **inmobiliaria** (que importaba leaflet pero no tenía la sección); en **valuador** quedó la versión inline (ya desplegada y funcionando, no se tocó por la regla de no romper lo aprobado).

**Favicon:** el edificio se veía muy chico (escala 1.5 con mucho margen). `favicon.svg` a escala 2.0 centrado, trazo 2→2.2, amarillo `#D9ED92`; regenerados `.ico`/`-32`/`-64` con PIL. Los navegadores cachean favicons agresivamente.

Commits: `431eec0` (form+entorno+PDF+reseñas), `64b0006` (folio+dirección+gracias), `658f9ed` (mapa valuador), `218c3ed` (mapa inmobiliaria), `0215f1a` (favicon).

---

## 18-20 Jul 2026 — Diseñador de promocionales "Just Listed" (frontend, local, NO desplegado)

Sesión larga y muy iterativa construyendo el **generador de fichas/promocionales** en el tab **Promociones** del panel inmobiliaria (`InmobiliariaDashboardPage` → `PromocionesTab.jsx`). Todo es frontend React; **backend/motor no se tocaron**. El trabajo quedó commiteado por el hook de auto-backup y **pusheado** (`19a115e`), pero **NO desplegado a Vercel** — el usuario lo revisa en local (`localhost:3000`, backend local apuntando a staging).

**Concepto (Magic Switch tipo Canva):** un solo objeto de datos (ficha del avalúo o propiedad subida + sesión del asesor) → cada formato se recompone automáticamente. Estilo nuevo **"Just Listed"** (`promociones/LayoutJustListed.jsx`): identidad editorial crema/serif Didot/dorado. Formatos: **A4** (1 hoja: precio junto/encima del logo, características 2 col, amenidades 4 col, 6 fotos, footer delgado con tel/email bajo el CTA) · **Post 1:1** · **Post·3slides (1:1)** · **Story/Reels 9:16** · **TikTok·3slides** · **Facebook**.

**Rail tipo Canva** (secciones: Estilo·Color·Fotos·Precio·Texto·Puntos·Extras·**Asesor**·Mostrar) con controles que aplican a todos los formatos:
- **Color:** theming por paleta (9 paletas; "verde"=editorial por default) + **color por elemento** (fondo/texto/acento/recuadros/texto-sobre-oscuro) via `c.priceBg`/overrides.
- **Precio:** fondo **sólido / transparente (50%) / sin fondo**.
- **Texto:** **20 tipografías** de sistema (headings vía `c.serif`), **CTA editable**, idioma **ES/EN** con **i18n completo** (títulos, specs, secciones, footer, y **amenidades** traducidas por mapa ES→EN), **contador de palabras** (60) en la descripción + más separación entre cajas.
- **Fotos:** selector con pool (fotos de la propiedad + 12 de muestra), incluir/orden/portada, y **stepper de slides de fotos en mosaico** (2×2) para Story/TikTok/Post3.
- **Puntos:** botón **"+"** para agregar/quitar; salen en **todos** los formatos.
- **Asesor:** datos **precargados de la cuenta**, pills **Asesor / Empresa** (nombre+tel+foto según fuente) + toggles Mostrar foto / Mostrar nombre. (Se quitó el editable/upload manual por pedido del usuario.)
- **Mostrar:** toggles de qué bloques se ven (descripción/características/amenidades/puntos).

**Export** (`SecuenciasJustListed.jsx` + `secuenciasGif.js`): reproductor animado 9:16 (ken-burns + crossfade) con salida **GIF** (gifenc), **JPG** (3 imágenes) y **MP4/WebM** (MediaRecorder + captureStream).

**Hallazgos importantes:** (1) los specs del avalúo viven anidados en `property_data` con nombres en inglés (`bedrooms`/`bathrooms`/`parking_spaces`) → se arregló `mapValuacionBackend` (`InmobiliariaDashboardPage`) para cargarlos + fotos; antes salían "Sin datos". (2) `session` es **plano** (`session.phone`, no `session.user.phone`) → helpers de asesor corregidos. (3) Sin la cookie de sesión el front cae a **datos MOCK** (por eso al inspeccionar por curl-login salía "Sin datos" — no era bug real). (4) Verificado en vivo con **claude-in-chrome** (login por cookie en el navegador → datos reales): A4/Post/Story se ven bien con specs, iconos, colonia y fotos.

**Otros:** nav del dashboard más compacto ("Verificación por Zona"), banner de documentos con menos margen, etiqueta **"Manual" → "Subida"**, sección **"Mis propiedades (subidas)" siempre visible** con guía + botón "Subir propiedad" + empty-state. Login de prueba local (staging): `pedrucus@gmail.com` / `PropValu2026!` (reset directo en Mongo staging).

**PENDIENTE:** seguir puliendo por feedback del usuario (afinar márgenes/tamaños por formato); desplegar a Vercel cuando lo apruebe. El motor y el backend siguen intactos (caché 7-jul desplegado).

---

## 17 Jul 2026 (2ª sesión) — Aplicada la auditoría (los 5 pendientes) + deploy

Se aplicaron y desplegaron los 5 arreglos que la auditoría de la 1ª sesión había dejado propuestos (el usuario dio "dale a todo"). Commit `d8c0a40`, pusheado, Railway + Vercel desplegados y verificados (backend `/api/health` 200, Vercel READY).

1. **ValuationForm (pérdida de datos):** el payload leía `formData.parking_spaces`/`floor_number` (campos inexistentes) → estacionamiento y piso se guardaban SIEMPRE null. Se mapeó a los campos reales: `parking_spots` (fallback a cubiertos+descubiertos) y `property_level` con `"PB"→0`.
2. **Geocode 404:** el buscador de dirección del mapa llamaba `/geocode` (endpoint inexistente). Como el componente ya carga Google Maps JS, se reemplazó por `new google.maps.Geocoder().geocode({address, componentRestrictions:{country:"MX"}})` en el navegador.
3. **gamificacion.py 500:** `$toDate` **lanza** si `edad_fecha` no es fecha válida → tumbaba el pipeline entero (`/leaderboard` y `/mis-puntos`). Fix nativo Mongo: `$dateFromString` con `onError:None, onNull:None` en ambos pipelines → los docs corruptos se ignoran solos (no try/except, que no puede envolver un doc dentro de una aggregation).
4. **ESLint:** react-scripts 5 espera eslint 8 pero el devDep era `9.23.0` (flat config) → el plugin del build no corría (0 warnings falsos). Se fijó `eslint@^8.57.1`, `npm install --legacy-peer-deps` (el repo tiene conflicto preexistente date-fns/react-day-picker). Build CRA verde, sin warnings (confirmó que la limpieza de imports sirvió).
5. **Bajos:** (a) **doble contador UTC vs MX** — `edades.py` bucketeaba el día en UTC (`datetime.now(timezone.utc).strftime`) mientras `gamificacion.py` agrupa en `America/Mexico_City` → discrepaban cerca de medianoche. Se alineó `edades.py` a día local MX (fuente de verdad = MX/gamificacion). (b) `static_map.py` armaba el `iframe_url` con la key aunque estuviera vacía → embed roto; ahora `None` si no hay `GOOGLE_MAPS_API_KEY` (el base64 es el principal). (c) Imports/estado muertos fuera de `EdadesZonaPage` (`Command*`, `Award`, `tipoEdit`, y el path viejo `puntos`/`diaStats` superseded por `gamiRef.registrarPunto`). (d) pytest instalado.

**No tocados, con justificación ponytail (documentada):** `/api/valuations` sin sesión devuelve `[]` 200 — no filtra datos y el dashboard espera un array, un 401 lo rompería. Confeti sin `cancelAnimationFrame` — es un loop **auto-terminante** (~110-160 frames, 2-3s) y `clearRect` sobre un canvas desmontado no lanza; el plumbing de cancelación es más código que el riesgo.

**pytest:** instalado (era el bloqueo citado). El crash restante (`ValueError: I/O operation on closed file`) es incompatibilidad de pytest+Python 3.14 en los 6 tests de integración viejos (v22-v26), que además requieren servidor vivo. No es código del proyecto; no se persiguió.

---

## 17 Jul 2026 — Flujo de avalúo: comparables reales, mapas Google, verificador, gamificación + auditoría

**Diagnóstico raíz de la OPI de Ixtepete (`val_6fc91c341233`, prod):** salía con "Av. Vallarta" y comparables sin URL porque corrió en modo **`simulated`** (server.py "LAST RESORT: SIMULATED DATA"). Causa: `search_comparables_from_mongo` (mongo_comparables.py) comparaba `tipo_propiedad="Casa"` (mayúscula, como llega de `PROPERTY_TYPES`) contra el pool que guarda `"casa"` (minúscula) + `municipality="Zapopan "` (con espacio) → **0 resultados** → IA falló → simulated. El propio código lo tenía anotado ("inerte por casing Casa/casa"). El pin además cayó en el **default CDMX (19.4326,-99.1332)** porque el geocode falló y el iframe embed no se puede arrastrar.

**Comparables (fix estrella, desplegado):** en `mongo_comparables.py` — normalizar tipo a minúscula + `strip()` municipio; **preferencia por misma colonia** (`_col_eq` tolerante a truncamientos) + **filtro de banda $/m²** derivado de la mediana de la colonia (descarta zonas de precio dispar: Chapalita $175k/m² fuera, Ixtepete/Tepetitlán/Fortín ~$25k/m² dentro). Solo aplica banda si quedan ≥4 comps. `server.py` pasa `colonia=prop.neighborhood`. Medido offline antes de wirear (primera medición mostró zonas dispares → se agregó la banda). Prod: Ixtepete → 6 reales de la zona, mediana ~$27.8k/m². El scraper SÍ tiene la zona (11,301 casas Zapopan, 23 Ixtepete).

**Mapas Google (odisea de billing, resuelta):** el mapa del formulario era iframe embed estático (no arrastrable) → agente lo cambió a **Google Maps JS interactivo con marcador arrastrable + click-to-place**, loader singleton, fallback a embed si la JS API no carga. `static_map.py` (reporte) de OSM → **Google Static Maps** con fallback OSM. La key requería billing; las keys de avaluos (`propvalu-mexico`) estaban atoradas en verificación de documento. Se probó pedrucus: proyecto `fair-geography-430001-k8` tenía billing activo → **creé key "PropValu Maps"** ahí (restringida a Maps JS/Static/Embed, sin restricción de app) y migré. Al final el billing de avaluos (`013DD6`, tarjeta Banamex ...5099) se destrabó → **migré a la key de avaluos `AIzaSyB0OMqh…`** (cuenta de negocio), con pedrucus `AIzaSyCgsW8…` de respaldo. Ambas verificadas 200 servidor+navegador. Registro en `credentials_registry.md`. Nota: Google me rate-limitó la consola por navegación automatizada; la cuenta avaluos vive en un perfil Chrome distinto (se conectó vía `switch_browser`).

**Verificador (`EdadesZonaPage.jsx` + `routers/edades.py`):** el guardado de terreno fallaba cuando solo se agregaba **conjunto** — `conjunto` y los m² no contaban como cambio válido en la validación (front L454 + back L382). Arreglado. **Editar m² de terreno/construcción a mano** (el scraper, sobre todo PINCALI, guarda el número equivocado del título, ej "1,595 M2 of land, 689 construction" → guarda 689): inputs nuevos + `m2_terreno`/`m2_construccion` en payload/validación/persistencia (`*_fuente="perito_correccion"`). Detección de terreno case-insensitive (`esTerrenoSinConstruccion`). Dropdown de tipo con `max-h-[55vh] overflow-y-auto`, texto 13px, "Conjunto mini apartamentos"→"Conjunto aparta estudios".

**Gamificación (construida — idea parqueada #80/G, versión INTERNA):** nuevo `frontend/src/components/GamificacionVerificador.jsx` (confeti canvas sin librerías, `forwardRef`/`useImperativeHandle` para disparar festejo desde `guardar`) + nuevo router `backend/routers/gamificacion.py` (`/api/gamificacion/mis-puntos` + `/leaderboard`, aggregation sobre `mercado_props` por `edad_estimador`, timezone México). Barra Hoy/Récord/Meta-150 en el verificador, confeti al récord del día y **fiesta a los 150 pts**, panel con historial diario (mini-barras) + leaderboard/concurso (mes/trimestre/año/histórico).

**Otros:** `InmobiliariaDashboardPage.jsx` usaba `MOCK_VALUACIONES` y nunca pedía `/valuations` → ahora fetch real (mapea status EN→ES) — explicaba "la valuación no aparece en el panel" (sí se guardaba, estaba `completed`). Reporte 8/9/10px→11px (agente). Video promo Remotion horizontal como anuncio en el modal "Generando valuación" con audio + botón de sonido (recomprimido 12MB→1.4MB, en `frontend/public/ads/`). Favicon PropValu (edificio amarillo/círculo verde) + manifest. Textos formulario. Se exploró rehacer las **plantillas promocionales white-label multiformato** (prototipo Artifact "Just Listed", NO integrado).

**Auditoría (2 agentes paralelos + runtime):** todo compila, 22 routers montados, mapas 200, comparables sin regresión. Pendientes NO aplicados (esperando go del usuario): (1) `ValuationForm.jsx:976,979` envía `formData.parking_spaces`/`floor_number` **inexistentes** → estacionamientos y piso se guardan `null` (pérdida de datos); (2) **geocode 404** (no existe endpoint, el buscador del mapa lo llama); (3) `gamificacion.py` sin try/except (500 con dato sucio); (4) **ESLint deshabilitado** (react-scripts 5 vs eslint 9); bajos: doble contador puntos UTC/México, `/api/valuations` sin gate, imports sin usar, confeti sin cancelAnimationFrame, pytest no instalado.

---

## 15 Jul 2026 — Data Exchange UI + plantilla ampliada + videos promo (Remotion)

**Data Exchange (#133) — rediseño de UI, solo frontend salvo columnas:**
- El promo **-50% / Programa Data Exchange** se movió del banner grande (debajo) a la **misma fila del cuadro Plan Pro** en el header (`InmobiliariaDashboardPage.jsx`, condicional `activeTab === "data_exchange"`), con **alto contraste** (título blanco, "50% descuento vitalicio" en lima, badge lima). Se quitó el banner del cuerpo del tab.
- Nueva **tabla tipo Excel "Mi inventario subido"** en `DataExchangeTab.jsx`: fetch a `GET /api/inmobiliaria/propiedades` (endpoint que YA existía en `inmobiliaria.py:226`, devuelve `propiedades_inmobiliaria` del user → ahí caen las subidas por Data Exchange), se refresca al confirmar una subida. Muestra **4 filas de ejemplo** (con badge "Ejemplo — así se verá") cuando el inventario está vacío, para que sepan qué datos poner. **Sin scroll horizontal**: `table-fixed w-full` + `<colgroup>` con anchos %, `text-xs`, encabezados cortos, truncado con tooltip.
- **Barra de subida delgada** (reemplazó las 2 cajas grandes de Instrucciones + dropzone): Descargar Plantilla a la izquierda + texto "Descarga la plantilla para subir tus propiedades"; a la derecha **contador "N útiles · sin repetir"** (= `inventario.length`, ya deduplicado por el backend al subir) + **ícono de subir clicable** (label con input file, drag-drop sobre toda la barra). Debajo, **nota de condiciones**: valida obligatorios por tipo + "cada mes verificamos que no haya duplicadas" (texto/política; el anti-dup al subir ya es real, el job mensual sería trabajo extra).
- **Plantilla ampliada — 3 columnas OPCIONALES nuevas** (`core/data_exchange.py`): **Coto / Edificio** y **Amenidades** (texto libre) + **Piso** (numérico, distinto de "Niveles" que ya existía = niveles de la casa). Agregadas a `COLUMNAS`, `EJEMPLO`, al loop de texto de `normalizar_fila` y (Piso) a `NUMERICOS`. NO se agregaron a `REQUERIDOS_POR_TIPO` → no rompen subidas existentes. Se guardan al CRM en `routers/data_exchange.py` (`confirmar`). El scraper NO captura amenidades estructuradas (van en descripción libre); "coto" ya era concepto (`catalogo_cotos.json`). Test `test_data_exchange.py` **10/10 OK**.
- Commits `5c9b59d` (promo+tabla+columnas) y `b58ba71` (barra delgada+condiciones+contador) pusheados a `main` → auto-deploy Vercel (frontend) + Railway (backend).
- **Método de revisión:** se corrió el frontend local en `localhost:3001` apuntando al backend de prod (`REACT_APP_BACKEND_URL`), para que el usuario revisara antes de desplegar.

**Videos promo con Remotion (proyecto aparte, NO PropValu, NO repo git):**
- Ubicación `C:\Users\pedru\remotion-video` (creado con `create-video`). Videos verticales (1080×1920 IG/TikTok) + horizontales (1920×1080). Dos conceptos: **beneficios 20s** y **emocional 44s** (hook → dolor del inmobiliario → miedo a quedarse atrás → solución "Valúas/Publicas/Vendes con el cómo" → beneficios → descuento → CTA). Marca PropValu (verdes, lima, ícono Building2, fuente Outfit vía `@remotion/google-fonts`).
- **Voz:** Gemini TTS (`gemini-2.5-flash-preview-tts`, voz Aoede) con la key de PropValu — funciona. **Imágenes/Veo NO** (404 en tier gratis, requiere billing). **Música:** Pixabay (descargada). **Fotos:** Pexels (caras a cámara, con velo verde de marca + Ken Burns). Transiciones `@remotion/transitions` + fade-out por escena para no traslapar textos. Textos editables en vivo en el Studio (props + `zod`/`zTextarea`, `defaultProps` inline para que "Save" escriba al código).
- **Skill genérica creada:** `~/.claude/skills/remotion/SKILL.md` (`/remotion`) — encapsula el método de un tutorial (referencia→recreación, preguntar antes de construir, cambios con valores numéricos) + recetas de código (fondos animados, pills escalonadas, rebote+subrayado que se dibuja, texto que se escribe, resaltado, contador, render transparente).
- **Open Design (nexu-io)** instalado (app escritorio) y su **MCP agregado a `~/.claude.json`** (command = `Open Design.exe` + `daemon-cli.mjs mcp`, con `OD_DATA_DIR`/`OD_SIDECAR_IPC_PATH`/`ELECTRON_RUN_AS_NODE`). `claude mcp list` = Connected. **Requiere reiniciar Claude Code** para que cargue sus tools (y la app abierta = daemon en :17500). Es agent-native (no editor visual). Remotion Studio NO permite arrastrar/redimensionar texto en el lienzo (se probó); edición fina = panel de props o Penpot→portar.

---

## 13-14 Jul 2026 — Data drift RECONCILIADO + deploy menor del verificador
Sesión pedida como "retomar pendientes". Se atacó la deriva de datos 7→13 jul (lo más caliente del motor) **midiendo offline, sin tocar prod**. Rebuild del caché desde Mongo de hoy vs baseline 7-jul: reproduce el regreso del headline (+7.2→−7.4 mediana), pero al reconciliar sobre los 103 puntuados exactos (dump gateado por env `DUMP` en el validador, ya revertido) la regresión REAL es solo ~3pp en pass-rates (±10 63.1→60.2, ±15 74.8→71.8, ±20 plano, errAbs +0.9). El mercado NO bajó: pool $/m²c global −2.9%, 75% de OPIs se mueven ≤3pp, movers mixtos (10↑/7↓), varios mejoran. **Hallazgo clave (a raíz de que el usuario insistió "¿no verificas más?"): la "mediana" del validador (`validar_40_opis.js:249-250`) NO es mediana de diffs — ordena por |diff| y devuelve el signo del ÚNICO OPI en rango 52/103; el +7.2→−7.4 es qué OPI cae en ese puesto, ruido de rango.** Corregidas 3 imprecisiones propias en el camino (concluir por descarte; población 131 vs 103; "cluster de 15 celdas"). Decisión: mantener 7-jul, pero rebuild NO es catástrofe (~3pp). Docs en `MOTOR_ANTECEDENTES` (RECONCILIACIÓN + COMPARACIÓN DE PRECIOS + ⚠️ métrico engañoso). Commits `2d85a6a`/`1e4dd23`/`874ac97`. **Segunda parte:** el usuario aclaró que las filtraciones+corrección de nombres eran para el VERIFICADOR por zona, no el motor → verificado que TODO sigue intacto en Mongo (es_remate 1,517 / duplicado 3,734 / es_canonico 2,113 / colonia ia 3,746 / set limpio 97,274) y que MEJORÓ la revisión. **Desplegado el deploy menor** (`bcaa0eb` edades.py filtra dup/remate de la lista + `331b095` revert mongo_comparables) a Railway prod (deployment `f25b7269`, healthy) — corrido vía `!railway up` porque el candado de deploy-a-prod de Claude Code exige acción humana (no se agregó regla de permiso, el usuario lo halló engorroso). Nada más tocado; caché motor sigue 7-jul.

## 13 Jul 2026 — Verificador A–G desplegado + H (datos) cerrado + auditoría a fondo del motor

**Verificador "Verificación de Datos por Zona" (A–G, commit `9909fd9`, desplegado Vercel+Railway):** conservación reordenada arriba de remodelación y "Excelente"→"Muy Bueno" (empata `muy_bueno`=1.05 del motor, verificado; motor no tiene "Excelente"); +9 tipos de propiedad + check "uso mixto" fuera del dropdown; terreno sin construcción omite edad/conservación/remodelación; pill VENTA/RENTA; la ficha refleja colonia/tipo corregidos tras guardar; "aplicar a otras" ahora agrupa por **coto real** (no colonia) + aviso preventivo; **récord del día** bajo la medallita (`dias_edad` en users).

**H — datos (PINCALI/scraper), TODO medido read-only antes de aplicar:** Marcado en prod (reversible, `activo` intacto): **remate 1,517** (`es_remate=true`+`activo=false`, regex afinado remate/recuperación bancaria/dación en pago/cesión); **dedup cross-portal estricto 3,734** (precio+m² EXACTOS + guarda título-Jaccard≥0.45 o mismo agente; campos canónicos `duplicado`/`es_canonico`/`grupo_id`/`canonico_id`; keeper=más completo con preferencia perito/ia). Descartado dedup oficial crudo (52k, huella laxa) y "media" (13.5k, falsos positivos por títulos genéricos de Inmuebles24). **Automatizado post-scrape** SIN timer nuevo (usa el `lanzador_scraper_mensual` día 2-10 → `scheduler.py`): remate-al-vuelo en `_guardar_en_mongo` + `dedup_estricto.py` (standalone idempotente) enganchado tras enricher y al final del full-run. Enricher (`obtener_props_mongo` carpeta MAIN) + `monitor_local` sincronizados para skip `duplicado`. **PINCALI ya estaba hecho** (backfill año corrido: colonia 99.3%, año 45.1% data-capped por dato faltante del vendedor); **J ya cubierto** (guardia SEPOMEX `_colonia_valida_sepomex` #135 + QA previa ~62% match, ~10% err).

**Reglas duras nuevas documentadas:** PINCALI solo español `/inmueble/` (nunca inglés — memoria `feedback_pincali_solo_espanol` + INDICE_SCRAPER); TODO en Mongo, Sheets descartado; carpeta scraper canónica = `Pagina-…/scraper-inmuebles`.

**AUDITORÍA DEL MOTOR (medida, NADA implementado):** El motor usa el CACHÉ (`cache_index.json ← builder`), no la función Python `search_comparables_from_mongo` (inerte por casing Casa/casa). Probado en lab con backup+validador offline (103 OPIs): (1) filtrar dup/remate del caché = **NEUTRO** (A/B preciso 13-jul) — antes lo reporté negativo por medir mal (bundled + baseline confundido con caché 7-jul); remates YA fuera del motor vía `activo=false` (0 en el pool). (2) quitar el dedup crudo del builder (colonia+m2c) = peor (óptimo local). (3) **Matriz 4 versiones:** el **caché 7-jul (63.1/74.8/83.5, +7.2 mediana) es el mejor y está DESPLEGADO**; reconstruir con Mongo de hoy regresa a 60.2/−7.4. Aislamiento: NO es la limpieza de colonias (revertir colonias recupera solo ~1pp) ni dedup/remate → es **DERIVA DE DATOS general 7→13 jul** (precios/medianas más bajos). **Decisión: NO reconstruir el caché.** Backup `cache_*.BASELINE_07JUL*` guardado. Todo en `MOTOR_ANTECEDENTES`. **Lección meta:** medí de más "opinando" antes de verificar antecedentes; el usuario lo señaló; las conclusiones finales se corrigieron con medición aislada.

---

## 12 Jul 2026 — Verificador: fix persistencia + acentos + municipio/población + tipos + compendio de tareas
**Bug crítico de persistencia:** `_base_usables` no filtraba `activo` → las propiedades marcadas Retirado/Info incorrecta/Juicio (activo=False) reaparecían en la lista, dando la impresión de que "no se guardó" (SÍ se guardaba). Fix: `activo != False`. Verificado: las 25 guardadas del usuario persistieron bien (17 exclusiones + 8 correcciones edad/colonia con perito_correccion). **Acentos:** quitados de todas las colonias (16,667 docs, backup) — el usuario los quiere sin acento para matchear. **Municipio + Población:** corrector de municipio + campo nuevo `poblacion` (pueblo, ej. Cajititlán; MongoDB schemaless, sin migración) por ficha. **Tipos:** agregado Rancho + tipo ahora es multi-select en dropdown con checkboxes (casa+rancho; guarda tipo_propiedad=primario + tipos[] array). **Análisis por portal:** PINCALI = el desmadre (2,619 de 3,747 fixes IA; descripcion vacía, sin dirección/CP/geo → mejorar enricher). **Verificación derivación IA (3,747):** ~62% match SEPOMEX exacto, mayoría de no-match son falsos negativos (abreviaturas, nombre municipio, fraccs no-SEPOMEX); errores reales ~10%; NET mejora clara, NO revertir. 4 operaciones de colonias con backup reversible en scratchpad. **COMPENDIO de ~10 tareas pendientes del verificador → guardado en ESTADO.md** (conservación, más tipos, terrenos sin edad, pills venta/renta, corrección en título, aviso aplicar-a-otras, record del día, PINCALI remate/dedup/enricher).

## 10-11 Jul 2026 — Infra pública + herramienta Verificación por Zona + limpieza masiva de colonias con IA
Railway trial venció → **Hobby pagado** (backend revivió). Frontend Vercel **público** (quitada deployment protection) + **proxy `/api` en `frontend/vercel.json`** (rewrite a Railway) → mismo-origen. **Render** `valuation-ai-1` (backend Node viejo) reconfigurado de respaldo: rama `master`→`main`, faltan env vars (#29). **Auth Bearer**: la cookie SameSite=None cross-dominio la bloqueaban los navegadores (fallaba en 3 navegadores/2 PCs; curl no); `/auth/login` ahora devuelve `session_token` en body, frontend lo guarda (localStorage `pv_token`) y manda `Authorization: Bearer` — arregló logout-en-reload y dropdowns vacíos. **Herramienta "Verificación de Datos por Zona"** (rename de "Verifica y Gana"): autocompletado propio (input nativo + top-12, sin cmdk/datalist=1700 que causaban el lag); botones exclusión un-clic Retirado/Info incorrecta/Juicio-remate (→activo=False, salen de comps, cuentan como revisión); botón Editar + historial en fichas guardadas; filtro "datos raros" (`comps-sin-edad?colonia_rara=true`). **LIMPIEZA COLONIAS (grande):** portales no-PINCALI metían dirección/CP como colonia; **DeepSeek derivó 3,747** (3,247 desde dirección con coma + 500 desde `titulo`), tag `colonia_fuente=ia_derivada`; + backfill 20,804 (case/acento/mojibake); raras 4,030→337 (Cancún/Toluca/calles → manual). Backend edades: `_dedup_colonias` + `_es_junk_colonia`. Índices #65 desplegados, #27 motor+TAVILY (typo corregido), #133 Data Exchange verificado en vivo, #25 catálogo cotos (wiring NEUTRAL→no wireado). Docs de estado unificados (ESTADO.md único; end-session ahora pushea). Recuperación de contraseña sin SMTP (#34) → link JWT manual. Regla nueva: medir antes de implementar (dry-run+validador before/after).

## 03 Jun 2026 (noche) — Lote infra/obs/scraper: #66.5, #66.3, #105 + diagnósticos #66.1/#101
Rama `main` (5 commits). (1) **#66.5 métricas HECHO**: `core/metrics.py` (registro in-memory sin deps: conteo/latencia/errores por ruta + uptime) + middleware HTTP + `/api/metrics` admin-only (snapshot + ping Mongo + conteos) + se eliminó un `/health` duplicado muerto. (2) **#66.3 jobs externos HECHO**: `require_admin_or_job` acepta `X-Job-Token`(==env `JOBS_SECRET`) o sesión admin → el cron mensual ya no depende de la sesión admin rotatoria; workflow `scraper_mensual` ahora dispara sync **y** snapshot. Arreglé bug: el curl del workflow usaba `ADMIN_SECRET` que `require_admin` ya NO acepta post-#66.2 (estaba roto). Config pendiente: secret `JOBS_SECRET` en GitHub + env en Railway. (3) **#105 HECHO**: propiedades.com ya usa plain_fetch (sin Chrome) en el path vivo → el conflicto con el navegador personal ya estaba resuelto; alineé los 2 defaults CDP legacy 9222→9333. (4) **#85 ya estaba hecho** (item obsoleto). (5) **#66.1 DEFERIDO** (decisión usuario): extract del router valuations es factible pero es core de avalúos bajo calibración — sesión propia con verificación de avalúo real. (6) **#101(a) BLOQUEADO POR DATOS**: el caché del motor tiene 0% de comps con `an` → per-comp por edad es no-op hoy; requiere `an` en caché (enrichers + rebuild Mongo / #107). Diagnóstico en MOTOR_ANTECEDENTES.

## 03 Jun 2026 (tarde) — Dedup cross-portal + confiabilidad de comparables + staging (#66.4) + responsive (#60)
Rama `feature/dedup-cross-portal` (8 commits). (1) Enricher: delays calibrados por portal (PINCALI/CYT ~8-12x) + auto-marca 404 muertos. (2) `fusionar_duplicados.py`: agrupa duplicados cross-portal NO destructivo (750 maestros, 1042 secundarios); motor **Python** deduplicado (`mongo_comparables.py`). (3) Snapshot mensual: `total_real` vs bruto, % duplicadas por zona, ranking calidad por portal (CYT 75>VIVA 71>...>PROPIEDADES_COM 37). (4) Reporte: links multi-portal por comparable + confiabilidad Alta/Media/Baja + banda "Confianza del avalúo" (estilo Romina). (5) **#66.4 staging HECHO**: cluster M0 separado `cluster1.avle5ez`, seed prod→staging (10k), backend local→staging, `db_target.py` fail-closed. (6) **#60 responsive HECHO**: grid fotos + tabla anunciantes. (7) Migración cache motor Sheet→Mongo INTENTADA pero REGRESA (validador ±20% 100%→83.3%); diagnóstico: NO es tiempo, el **Sheet está incompleto ~2x** por save-errors; es proyecto de re-calibración (MOTOR_ANTECEDENTES #107). Baseline restaurado.

## 03 Jun 2026 (mañana) — Captura de edad/datos de detalle en el scraper + flywheel de comps web
Enricher arreglado para sacar AÑO de los 6 portales (PROPIEDADES_COM, CYT, INMUEBLES24, VIVANUNCIOS, PINCALI, MITULA) — la premisa "los portales no publican año" era bug del extractor. Nuevo modo `enricher.py --mongo` (escribe directo a `mercado_props.anio_construccion`, Mongo es base oficial; Sheets en pausa). También captura recámaras/baños/estacionamientos/teléfono/inmobiliaria/email donde el JSON los trae (CYT completo; tel en CYT+VIVANUNCIOS). Fix clave: propiedades.com scrapeaba SIN colonia → ahora la extrae (desbloquea edadMedianaZona). Reporte: columna Fuente ahora clickeable al anuncio real. Motor: comps web de Serper se enriquecen (edad/m²) vía enrich_urls.py y se guardan a mercado_props (flywheel #101). Puente edad Mongo→caché construido (overlay edadMedianaZona en build_cache_index, sin tocar precios) — listo para activar+validar cuando suba cobertura. `anio_construccion`: 58 → ~1000 y subiendo. Ver MOTOR_ANTECEDENTES.md (#90/#91 desbloqueo).

## 02 Jun 2026 — Seguridad (#64)
Recuperado del corte de sesión y verificado el cierre de IDOR en avalúos + escalada de privilegios en upgrade-role (commit 4e15e10). #66.2 y #66.6 ya estaban hechos (commit c9bdaf4). Nuevo doc maestro `SEGURIDAD_ARQUITECTURA_ANTECEDENTES.md`. Pendientes S1–S6 catalogados ahí.

## 01 Jun 2026 — Motor Remi: consolidación en archivo maestro
6 fuentes→1, capas ganada/derivada + temporalidad, flywheel del perito, acumulación de comps web + score de confianza, cap por calidad de pool, y fixes de bugs reales (municipio mal capturado, gate suma_partes con clave exacta). Benchmark honesto **2025-2026 (153 OPIs): 69.9% ±10%, 81.7% ±15%, 91.5% ±20%**; curado 93: 76.3/81.7/94.6. (El 85.4% del 26-May era pre-rebuild de caché; el set 2025-2026 es más amplio y honesto.) Ver MOTOR_ANTECEDENTES.md.

---
# 📸 Snapshots retirados (consolidación 10-Jul-2026)
> Contenido de archivos de estado antiguos, movido aquí para no perder historial al unificar en un solo `ESTADO.md` vigente.

## ESTADO.md — snapshot 27-Jun / 06-Jul 2026 (retirado)
**Enriquecimiento (27-Jun):** ~99,904 docs activos, 8,135 secundarios (dedup). PROPIEDADES_COM ✅ ~93% año · INMUEBLES24 ✅ ~98% · VIVANUNCIOS ✅ ~93% · CASAS_Y_TERRENOS ✅ 191/379 ~50% · PINCALI ✅ techo ~24.4% · MITULA ✅ parcial · NOCNOK ✅ nuevo.
**Motor (06-Jul, baseline offline):** caché reconstruido colonias PINCALI limpias (25,556 comps), ±20 75.7% / mediana −8.7. Ancla segmentada tipo+edad → PROD (`b5430fc`) 75.7→76.7. MITULA envenena pool (lab): sacándolo ±20 76.7→**79.6**, errAbs 13.2→11.6 (mayor salto). Consolidación colonias fragmentadas (`774b016`): 537 llaves, 2,772 comps recuperados, ±15 68.0→70.9. Prevención fragmentación (`d33f075`). Estado motor: ±20 79.6 / ±15 70.9 / errAbs 11.7 (desde 73.8). 4 palancas de fórmula probadas, ninguna mueve ±20 → cuello es DATO. Diagnóstico 3-columnas: el MÉTODO es correcto, falla la SELECCIÓN de comps. Validador SIEMPRE offline.
**Score histórico validador:** ±15% ~86%, ±20% ~89.7% (post geoproximidad #114, baseline distinto al offline de lab).
**Seguridad (06-Jul):** fuga commit `7e90cf1` (Gemini key + MONGO_URL prod), de-hardcodeado + ambas keys rotadas + pre-commit hook. 9 archivos de-hardcodeados (`ab7ad50`). Incidente cerrado.
**Índice motor (06-Jul):** `Modulo Drive IA/INDICE_MOTOR.md` — canónico vs experimento, dónde OPIs/comps/IDX/NSE.
**Pendientes de entonces:** #21 backfill año PINCALI (~25k, proxy-free en terminal real), #22 selección comps por segmento, #14 scraper mensual julio, #16 gancho de precios, #17 Monopolio (diferido).

## CURRENT_STATE.md — snapshot 10-Mar 2026 (retirado, era arquitectura Node.js pre-migración a FastAPI)
Estado de marzo, cuando el stack era Node.js/Express (`server.js`, `aiSearch.js`) antes de migrar a FastAPI/Python. Features de entonces: edición manual de factores INDAABIN para valuadores (`ComparablesPage.jsx` + endpoint `select-comparables` con `custom_factors`); mejoras estéticas PDF (desglose amenidades 🛏️🚿🚗, diseño 3 columnas, advertencias predial/mercado, clamp 5 líneas análisis IA, densidad real "121+ Propiedades"); filtros estrictos extracción web (prompt Gemini "no inventes propiedades/links vivos"). Arquitectura: React :3001 → Express :3000 → Google Maps + Gemini. Próximo paso de entonces: migrar recolector de comparables de Vertex Grounding a SerpAPI/Google Custom Search (evolucionó luego a Serper→Tavily). Obsoleto: todo el backend se reescribió a FastAPI.

## 21 Jul 2026 — Calibración motor deptos (gap6) + investigación datos i24
Sesión de motor sobre deptos. Diagnóstico: los deptos se sobre-valúan sistemáticamente en pool `exacta` (medido 60-68% ±20 vs ~83% global) porque el pool suele ser obra nueva/preventa y el `factorEdad` asume comps de edad similar → un depto viejo no se deprecia. **Parche graduado a prod** (`motor_remi_api.js`): `_gapEdad = tipo==='depto' ? 6 : 25` (menos gracia de edad para depto). Validado OFFLINE (keys en blanco, determinista): deptos 68→76% ±20, global --n400 69.3→70.7%, cero regresión en casas. Cuarzo 2380 (depto 75m² 45a Bosques Victoria) $3.55M→$2.80M. Commit `e8ed0fd`, rama `fix/flujo-avaluo-reporte-jul20`, backup `motor_remi_api.PRE_DEPTOEDAD.bak.js`. Flag lab `LAB_EDAD_DEPTO`.

**Investigación i24 (pushback del usuario):** confirmado que i24/prop.com traen mucho año (92%/91%) que el motor tira — i24 mete el área del depto en `m2_terreno` (m2_construccion null) y el builder filtra `m2c>0` (i24 venta: 966→26 llegan al cache). Se probó recuperarlo (relajar query + mapear t→c depto en `actualizar_cache_consolidado_mongo.py`): año depto 44.2%→63.5%, pero el validador salió **NEGATIVO** (deptos 76→60%, global 70.7→68.8%) porque **64% de lo recuperable es obra nueva** → inunda el pool y sube medianas. NO implementado, documentado en `MOTOR_ANTECEDENTES.md`. Lección: el lever es inventario de deptos USADOS (escaso), no cobertura de año. El método correcto a futuro (banda por edad del sujeto, `LAB_ANCLA_SEG`) sale inerte hoy por falta de año en usados.

**Otros:** creado `DICCIONARIO_ARCHIVOS.md` (qué hace cada archivo del motor + paso del pipeline, IDX vs NSE) + memoria `reference_diccionario_archivos_motor`. Regla clave reforzada: correr el validador SIEMPRE OFFLINE (los barridos online dieron números no deterministas + quemaron crédito). Limpiados todos los artefactos LAB/scratch de la sesión; cache y motor canónicos verificados intactos.
